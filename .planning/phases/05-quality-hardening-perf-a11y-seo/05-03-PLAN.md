---
phase: 05-quality-hardening-perf-a11y-seo
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - data/site.json
  - schema/site.schema.json
  - scripts/quality/perf.ts
  - scripts/quality/lighthouse-config.cjs
  - scripts/quality/types.ts
  - scripts/quality/report.ts
  - scripts/quality/run-quality-checks.ts
  - package.json
  - .github/workflows/ci.yml
files-modified:
  - data/site.json
  - schema/site.schema.json
  - scripts/quality/perf.ts
  - scripts/quality/lighthouse-config.cjs
  - scripts/quality/types.ts
  - scripts/quality/report.ts
  - scripts/quality/run-quality-checks.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - QUAL-04
  - QUAL-05
user_setup: []
objective: Add dual-target performance budgets and integrate full quality gating into CI required/strict lanes with remediation-first diagnostics
must_haves:
  truths:
    - Performance budgets are defined for both mobile and desktop targets.
    - Performance gate includes top-level pass/fail plus metric-level checks and configurable measured routes.
    - Standard mode warns on non-blocking budget drift while blocking thresholds fail as configured.
    - Required CI lane executes baseline quality gate and blocks on configured domain failures.
    - Strict quality checks run on mainline push in non-blocking strict lane and emit detailed remediation artifacts.
  artifacts:
    - path: scripts/quality/perf.ts
      provides: Performance budget evaluation logic for mobile/desktop profiles
      min_lines: 100
    - path: scripts/quality/lighthouse-config.cjs
      provides: Deterministic runtime audit configuration for measured routes/profiles
      min_lines: 40
    - path: scripts/quality/run-quality-checks.ts
      provides: Aggregated seo/a11y/perf gate orchestration and exit policy
      min_lines: 130
    - path: .github/workflows/ci.yml
      provides: Required-lane quality gate + strict-lane quality signal integration
      min_lines: 220
    - path: package.json
      provides: Stable quality command surface for local + CI execution
      contains: "quality:check"
  key_links:
    - from: package.json
      to: scripts/quality/run-quality-checks.ts
      via: npm quality command entrypoints for standard and strict modes
      pattern: "quality:check"
    - from: .github/workflows/ci.yml
      to: package.json
      via: required and strict lane quality command execution
      pattern: "npm run quality"
    - from: scripts/quality/perf.ts
      to: data/site.json
      via: budget target/profile/route configuration resolution
      pattern: "performance"
---

<objective>
Complete quality hardening by enforcing performance budgets and CI quality gates.

Purpose: Ensure regressions in perf/a11y/seo are detected with clear policy semantics before merge/deploy.
Output: Performance budget checks, unified quality command surface, and CI gating integration.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-quality-hardening-perf-a11y-seo/05-CONTEXT.md
@.planning/phases/05-quality-hardening-perf-a11y-seo/05-RESEARCH.md
@.planning/phases/05-quality-hardening-perf-a11y-seo/05-01-PLAN.md
@.planning/phases/05-quality-hardening-perf-a11y-seo/05-02-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add configurable dual-target performance budget policy</name>
  <files>data/site.json, schema/site.schema.json, scripts/quality/perf.ts, scripts/quality/lighthouse-config.cjs, scripts/quality/types.ts</files>
  <action>Define performance policy configuration for mobile/desktop targets, metric thresholds, and measured routes, then implement deterministic budget evaluation with warning/failure semantics aligned to standard/strict mode policy.</action>
  <verify>Run quality checks against configured routes and confirm metric-level and aggregate budget results are reported with actionable remediation hints.</verify>
  <done>Performance quality contract supports dual-target budgets and route configurability.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Finalize unified quality report and command surface</name>
  <files>scripts/quality/run-quality-checks.ts, scripts/quality/report.ts, package.json</files>
  <action>Finalize aggregated quality runner behavior across seo/a11y/perf domains, including human summary + JSON artifact output, per-domain remediation checklists, and stable npm scripts for required/strict execution modes.</action>
  <verify>Run standard and strict quality commands locally and confirm output structure, exit codes, and remediation detail levels match phase decisions.</verify>
  <done>Quality checks are deterministic, scriptable, and CI-ready.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Integrate quality gating into CI required and strict lanes</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <action>Wire baseline quality checks into required CI lane as blocking gate, keep strict quality checks on main push strict lane as non-blocking signal, and ensure diagnostics artifacts + summary-first remediation output remain consistent with Phase 4 patterns.</action>
  <verify>Validate CI workflow structure and local parity commands for required vs strict quality modes; confirm quality-domain failure behavior matches configured blocking policy.</verify>
  <done>CI enforces quality hardening policies while preserving lane semantics decided in Phase 5 context.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Dual-target performance budgets exist and are configurable by route/profile.
- [ ] Unified quality runner provides human + JSON outputs with remediation checklists.
- [ ] Required CI lane blocks on configured quality failures.
- [ ] Strict quality checks run on mainline strict lane as non-blocking signal.
</verification>

<success_criteria>
- All tasks completed.
- QUAL-04 and QUAL-05 are satisfied with enforceable CI-integrated quality checks.
- Phase 5 quality bar is measurable and repeatable for fork maintainers.
</success_criteria>

<output>
After completion, create .planning/phases/05-quality-hardening-perf-a11y-seo/05-03-SUMMARY.md
</output>
