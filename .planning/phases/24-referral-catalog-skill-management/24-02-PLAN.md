---
phase: 24-referral-catalog-skill-management
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/lib/content/referral-catalog.ts
  - src/lib/content/referral-catalog.test.ts
  - src/lib/content/referral-fields.ts
  - src/lib/content/referral-fields.test.ts
  - src/lib/content/load-content.ts
  - src/lib/content/load-content.referral.test.ts
files-modified:
  - src/lib/content/referral-catalog.ts
  - src/lib/content/referral-catalog.test.ts
  - src/lib/content/referral-fields.ts
  - src/lib/content/referral-fields.test.ts
  - src/lib/content/load-content.ts
  - src/lib/content/load-content.referral.test.ts
autonomous: true
requirements: []
user_setup: []
objective: Resolve catalog-backed referral data in runtime content loading with explicit catalog refs, matcher fallback, and widened provenance while preserving manual-over-generated precedence and the existing card/render contract
must_haves:
  truths:
    - Catalog loading merges the shared catalog with the optional `data/policy/referral-catalog.local.json` overlay when that fork-local file exists.
    - Runtime referral resolution prefers manual fields first, then explicit catalog refs, then deterministic matcher-backed catalog lookup, and only then generated/public fill-ins.
    - The resolved referral object consumed by cards stays the same outward contract even though catalog data now seeds and normalizes it.
    - Provenance can distinguish catalog-derived values from manual and generated values so maintainers can explain where a field came from.
    - Links with no catalog refs or matcher hits keep their pre-Phase-24 behavior.
    - Catalog lookup remains deterministic and explainable; weak or conflicting matches do not guess.
  artifacts:
    - path: src/lib/content/referral-catalog.ts
      provides: reusable catalog loading, id lookup, matcher resolution, and normalization helpers
      min_lines: 180
    - path: src/lib/content/referral-catalog.test.ts
      provides: focused tests for explicit refs, matcher resolution, and no-guess fallback behavior
      min_lines: 160
    - path: src/lib/content/load-content.ts
      provides: runtime merge order that keeps manual overrides authoritative over catalog and generated data
      min_lines: 620
    - path: src/lib/content/load-content.referral.test.ts
      provides: regression coverage for manual/catalog/generated precedence and non-catalog fallback behavior
      min_lines: 100
  key_links:
    - from: src/lib/content/referral-catalog.ts
      to: src/lib/content/load-content.ts
      via: runtime referral loading delegating deterministic catalog lookup to a dedicated module instead of ad hoc inline logic
      pattern: "catalog"
    - from: src/lib/content/referral-catalog.ts
      to: data/policy/referral-catalog.local.json
      via: runtime catalog loading honoring the concrete fork-local overlay file when present
      pattern: "referral-catalog.local.json"
    - from: src/lib/content/referral-fields.ts
      to: src/lib/content/load-content.referral.test.ts
      via: widened referral provenance and field precedence proven through real merge scenarios
      pattern: "manual"
    - from: src/lib/content/referral-catalog.test.ts
      to: src/lib/content/referral-fields.test.ts
      via: catalog reference types and matcher semantics staying aligned across the data contract and runtime helper layer
      pattern: "matcher"
---

<objective>
Make the catalog real at runtime without changing what the rest of the app renders.

Purpose: resolve shared referral knowledge into the existing `links[].referral` contract while keeping manual truth and deterministic provenance.
Output: catalog resolver module plus runtime merge and test coverage.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-referral-catalog-skill-management/24-RESEARCH.md
@.planning/phases/24-referral-catalog-skill-management/24-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Build a dedicated catalog resolver for explicit refs and deterministic matcher lookup</name>
  <files>src/lib/content/referral-catalog.ts, src/lib/content/referral-catalog.test.ts, src/lib/content/referral-fields.ts, src/lib/content/referral-fields.test.ts</files>
  <action>Create a dedicated runtime module that loads the shared catalog plus the optional `data/policy/referral-catalog.local.json` overlay when present, resolves explicit `catalogRef` references, applies declarative matcher logic when no explicit ref exists, and exposes explainable source metadata. Keep the logic data-driven and deterministic; if multiple or weak matches remain, return no inferred catalog match rather than guessing.</action>
  <verify>Run `bun test src/lib/content/referral-catalog.test.ts src/lib/content/referral-fields.test.ts` and `bun run typecheck`.</verify>
  <done>The repo has one reusable catalog-resolution seam instead of scattering link-shape logic across load-time call sites.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Merge catalog-backed referral defaults into load-content with widened provenance</name>
  <files>src/lib/content/load-content.ts, src/lib/content/load-content.referral.test.ts, src/lib/content/referral-fields.ts</files>
  <action>Update referral merge/load behavior so catalog data can seed or fill referral fields before generated/public data, while manual link fields still win field-by-field. Widen provenance so resolved referral output can explain `manual`, `catalog`, and `generated` sources without changing the outward card/runtime contract.</action>
  <verify>Run `bun test src/lib/content/load-content.referral.test.ts` and `bun run typecheck`.</verify>
  <done>The runtime can consume catalog-backed referral data while preserving existing manual precedence and non-catalog fallback behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Runtime referral resolution supports explicit catalog refs plus deterministic matcher fallback.
- [ ] Runtime catalog loading honors `data/policy/referral-catalog.local.json` when present without changing shared-file defaults.
- [ ] Manual referral fields still override catalog and generated values field-by-field.
- [ ] Provenance can distinguish catalog-derived fields from manual and generated fields.
- [ ] Links with no catalog match keep their prior behavior.
</verification>

<success_criteria>
- All tasks completed.
- Phase 24 gains runtime catalog lookup without altering the downstream-visible `links[].referral` render contract.
- The repo has a clean abstraction for later enrichment and skill work to rely on.
</success_criteria>

<output>
After completion, create .planning/phases/24-referral-catalog-skill-management/24-02-SUMMARY.md
</output>
