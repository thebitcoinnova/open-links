---
phase: 24-referral-catalog-skill-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/policy/referral-catalog.json
  - data/policy/referral-catalog.local.json
  - schema/referral-catalog.schema.json
  - schema/links.schema.json
  - src/lib/content/referral-fields.ts
  - src/lib/content/referral-fields.test.ts
  - src/lib/fork-owned-paths.ts
  - scripts/validate-data.ts
  - scripts/validate-data.test.ts
  - scripts/reset-fork-template.test.ts
  - scripts/lib/upstream-sync.test.ts
  - config/fork-owned-paths.json
files-modified:
  - data/policy/referral-catalog.json
  - data/policy/referral-catalog.local.json
  - schema/referral-catalog.schema.json
  - schema/links.schema.json
  - src/lib/content/referral-fields.ts
  - src/lib/content/referral-fields.test.ts
  - src/lib/fork-owned-paths.ts
  - scripts/validate-data.ts
  - scripts/validate-data.test.ts
  - scripts/reset-fork-template.test.ts
  - scripts/lib/upstream-sync.test.ts
  - config/fork-owned-paths.json
autonomous: true
requirements: []
user_setup: []
objective: Introduce a first-class referral catalog data model, a concrete optional fork-local overlay at `data/policy/referral-catalog.local.json`, and explicit link-level catalog reference contract without changing the existing `links[].referral` runtime/render shape
must_haves:
  truths:
    - Shared referral knowledge lives in a schema-validated upstream catalog under `data/policy/` with stable family, offer, and matcher ids.
    - Links can opt into catalog-backed authoring through `links[].referral` without losing the existing manual referral contract or breaking non-catalog links.
    - Optional fork-local catalog overlay support is pinned to `data/policy/referral-catalog.local.json`, schema-validated when present, and marked fork-owned so upstream PR hygiene stays intact.
    - Validation errors make catalog shape, id, and link-reference failures actionable instead of silently ignoring bad catalog data.
    - Repo-local downstream/fork verification covers the shared-vs-fork-owned split through the fork-reset and upstream-sync surfaces rather than leaving overlay safety as documentation-only intent.
    - Downstream-safe defaults preserve the current behavior for forks and `open-links-sites` consumers that have not adopted catalog refs yet.
  artifacts:
    - path: data/policy/referral-catalog.json
      provides: shared seeded catalog with family, offer, and matcher layers plus stable ids
      min_lines: 80
    - path: data/policy/referral-catalog.local.json
      provides: concrete optional local overlay contract for fork-owned referral additions and overrides
      min_lines: 10
    - path: schema/referral-catalog.schema.json
      provides: canonical schema for catalog families, offers, matchers, and optional overlay validation
      min_lines: 140
    - path: schema/links.schema.json
      provides: optional link-level catalog reference contract nested under `links[].referral`
      min_lines: 720
    - path: scripts/validate-data.test.ts
      provides: validation coverage for catalog schema failures, bad ids, and `data/policy/referral-catalog.local.json` overlay handling
      min_lines: 240
    - path: scripts/reset-fork-template.test.ts
      provides: fork-reset coverage proving the local overlay path is treated as fork-owned instead of shared starter data
      min_lines: 260
    - path: scripts/lib/upstream-sync.test.ts
      provides: upstream-sync coverage proving the local overlay path stays on the fork-owned side of shared-vs-local alignment
      min_lines: 200
    - path: config/fork-owned-paths.json
      provides: fork-owned protection for the concrete `data/policy/referral-catalog.local.json` overlay path
      min_lines: 30
  key_links:
    - from: data/policy/referral-catalog.json
      to: schema/referral-catalog.schema.json
      via: seeded shared catalog remaining machine-validated and downstream-copy-safe
      pattern: "familyId"
    - from: data/policy/referral-catalog.local.json
      to: config/fork-owned-paths.json
      via: concrete local overlay file being treated as fork-owned rather than shared upstream policy data
      pattern: "referral-catalog.local.json"
    - from: schema/links.schema.json
      to: src/lib/content/referral-fields.ts
      via: link-level catalog reference typing staying aligned with the runtime referral contract
      pattern: "catalogRef"
    - from: config/fork-owned-paths.json
      to: scripts/lib/upstream-sync.test.ts
      via: shared-vs-fork-owned overlay behavior being regression-tested through the upstream alignment surface
      pattern: "referral-catalog.local.json"
---

<objective>
Define the authoring contract for shared referral knowledge before runtime or skill work builds on it.

Purpose: give Phase 24 one canonical catalog model and explicit link reference seam that preserve the current referral runtime contract.
Output: catalog schema/data, the concrete `data/policy/referral-catalog.local.json` overlay seam, and validation plus downstream/fork verification coverage.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-referral-catalog-skill-management/24-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add the shared referral catalog schema and seeded upstream catalog file</name>
  <files>data/policy/referral-catalog.json, data/policy/referral-catalog.local.json, schema/referral-catalog.schema.json</files>
  <action>Create the shared catalog model under `data/policy/` with schema support for family, offer, and matcher layers plus stable ids. Add the concrete optional overlay file at `data/policy/referral-catalog.local.json` as the fork-local contract for local additions or overrides, and seed the shared catalog with at least one real proving family/offer/matcher entry that Phase 24 can migrate later. Keep matcher fields declarative and explainable rather than script-like.</action>
  <verify>Review the seeded catalog against the research model, then run `bun run validate:data`.</verify>
  <done>The repo has one canonical shared referral catalog plus one explicit fork-local overlay contract instead of a vague overlay idea.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add link-level catalog reference typing, concrete overlay validation, and fork-owned sync coverage</name>
  <files>schema/links.schema.json, src/lib/content/referral-fields.ts, src/lib/content/referral-fields.test.ts, src/lib/fork-owned-paths.ts, scripts/validate-data.ts, scripts/validate-data.test.ts, scripts/reset-fork-template.test.ts, scripts/lib/upstream-sync.test.ts, config/fork-owned-paths.json</files>
  <action>Add an optional nested catalog reference under `links[].referral`, keep local manual fields intact, and teach validation about the shared catalog plus the exact `data/policy/referral-catalog.local.json` overlay path. Mark that overlay path fork-owned, update any helper or sync surface that consumes the fork-owned contract, and add regression coverage proving reset/upstream-sync flows preserve the shared-vs-fork-owned split correctly.</action>
  <verify>Run `bun test src/lib/content/referral-fields.test.ts scripts/validate-data.test.ts scripts/reset-fork-template.test.ts scripts/lib/upstream-sync.test.ts`, `bun run test:deploy`, and `bun run typecheck`.</verify>
  <done>The catalog contract is typed, validated, and protected by executable fork/downstream checks before runtime lookup logic is added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] A shared `data/policy/referral-catalog.json` exists with family, offer, and matcher layers plus stable ids.
- [ ] `links[].referral` can carry an explicit catalog reference without replacing the existing manual referral fields.
- [ ] Validation covers catalog structure, id/reference integrity, and the exact `data/policy/referral-catalog.local.json` overlay handling.
- [ ] Fork-local overlay support is explicit and aligned with `config/fork-owned-paths.json`.
- [ ] Repo-local downstream verification proves the local overlay stays on the fork-owned side of reset/upstream-sync behavior.
</verification>

<success_criteria>
- All tasks completed.
- Phase 24 has a concrete catalog authoring contract that is upstream-friendly and downstream-safe.
- Later runtime and skill work can build on the catalog without reopening base schema shape decisions.
</success_criteria>

<output>
After completion, create .planning/phases/24-referral-catalog-skill-management/24-01-SUMMARY.md
</output>
