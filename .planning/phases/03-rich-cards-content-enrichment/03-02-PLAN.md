---
phase: 03-rich-cards-content-enrichment
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - scripts/enrich-rich-links.ts
  - scripts/enrichment/fetch-metadata.ts
  - scripts/enrichment/parse-metadata.ts
  - scripts/enrichment/report.ts
  - scripts/enrichment/types.ts
  - scripts/validate-data.ts
  - scripts/validation/format-output.ts
  - schema/links.schema.json
  - src/lib/content/load-content.ts
  - package.json
  - data/site.json
  - data/generated/.gitkeep
files-modified:
  - scripts/enrich-rich-links.ts
  - scripts/enrichment/fetch-metadata.ts
  - scripts/enrichment/parse-metadata.ts
  - scripts/enrichment/report.ts
  - scripts/enrichment/types.ts
  - scripts/validate-data.ts
  - scripts/validation/format-output.ts
  - schema/links.schema.json
  - src/lib/content/load-content.ts
  - package.json
  - data/site.json
  - data/generated/.gitkeep
autonomous: true
requirements:
  - DATA-03
  - UI-03
user_setup: []
objective: Implement build-time rich metadata enrichment with strict-mode-aware diagnostics and fallback reporting
must_haves:
  truths:
    - Build-time enrichment runs for rich links by default with per-link opt-out support.
    - Metadata fetch uses a moderate timeout plus one retry strategy.
    - Enrichment is fresh each run (no persistent cache reuse behavior).
    - Strict mode fails on enrichment errors while non-strict mode emits warnings and keeps fallback rendering viable.
    - A structured enrichment/fallback report artifact is generated with per-link reasons and remediation hints.
  artifacts:
    - path: scripts/enrich-rich-links.ts
      provides: Build-time enrichment entrypoint that orchestrates fetch, parse, and report output
      min_lines: 80
    - path: scripts/enrichment/fetch-metadata.ts
      provides: Timeout + single-retry fetch helper
      min_lines: 45
    - path: scripts/enrichment/report.ts
      provides: Structured enrichment diagnostics and artifact writer
      min_lines: 45
    - path: scripts/validate-data.ts
      provides: Strict-mode escalation path for enrichment failures
      min_lines: 120
    - path: package.json
      provides: Commands wiring for enrichment flow and build integration
      contains: "enrich:rich"
  key_links:
    - from: package.json
      to: scripts/enrich-rich-links.ts
      via: npm script invocation for build-time enrichment
      pattern: "enrich:rich"
    - from: scripts/enrich-rich-links.ts
      to: scripts/enrichment/fetch-metadata.ts
      via: metadata retrieval pipeline
      pattern: "fetchMetadata"
    - from: scripts/enrich-rich-links.ts
      to: scripts/enrichment/report.ts
      via: diagnostics artifact generation
      pattern: "writeEnrichmentReport"
---

<objective>
Add build-time metadata enrichment and diagnostics for rich cards.

Purpose: Make rich previews more complete while preserving deterministic fallback and strictness guarantees.
Output: Enrichment scripts, strict-mode integration, and structured report artifacts.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rich-cards-content-enrichment/03-CONTEXT.md
@.planning/phases/03-rich-cards-content-enrichment/03-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Build enrichment pipeline with timeout and retry controls</name>
  <files>scripts/enrich-rich-links.ts, scripts/enrichment/fetch-metadata.ts, scripts/enrichment/parse-metadata.ts, scripts/enrichment/types.ts, package.json</files>
  <action>Implement build-time enrichment for rich links, including timeout + one retry fetch behavior, metadata parsing, and script wiring through package commands. Respect per-link opt-out configuration.</action>
  <verify>Run enrichment command and confirm output includes attempted rich links with deterministic completion behavior.</verify>
  <done>Enrichment can execute locally/CI as a standalone build-time step without runtime fetch dependencies.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Emit structured diagnostics report and integrate fallback metadata loading</name>
  <files>scripts/enrichment/report.ts, src/lib/content/load-content.ts, schema/links.schema.json, data/site.json, data/generated/.gitkeep</files>
  <action>Generate a structured report artifact for enrichment/fallback events, and wire content loading to consume generated metadata when present while preserving rich-shell fallback behavior when absent.</action>
  <verify>Simulate missing/partial metadata and confirm report contains per-link reason codes plus remediation-friendly context.</verify>
  <done>Generated diagnostics artifact provides machine-readable enrichment status and rendering remains resilient.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Enforce strict-mode error policy with warning path for standard mode</name>
  <files>scripts/validate-data.ts, scripts/validation/format-output.ts, scripts/enrich-rich-links.ts, package.json</files>
  <action>Integrate enrichment outcomes into validation/build flow so strict mode fails on enrichment errors while normal mode warns and continues with fallback-safe output.</action>
  <verify>Run normal and strict flows against controlled enrichment failure scenarios and verify exit behavior + diagnostics formatting.</verify>
  <done>Enrichment reliability policy is enforceable and transparent for developer and CI usage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build-time enrichment command exists and uses timeout + single retry.
- [ ] Structured report artifact is emitted with per-link diagnostics.
- [ ] Strict mode fails on enrichment errors while standard mode warns and continues.
</verification>

<success_criteria>
- All tasks completed.
- Phase 3 enrichment requirements are delivered without runtime dependency on metadata fetch.
- Fallback and diagnostics behavior match locked context decisions.
</success_criteria>

<output>
After completion, create .planning/phases/03-rich-cards-content-enrichment/03-02-SUMMARY.md
</output>
