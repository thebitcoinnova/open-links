---
phase: 03-rich-cards-content-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/links.schema.json
  - src/lib/content/load-content.ts
  - src/lib/ui/rich-card-policy.ts
  - src/components/cards/RichLinkCard.tsx
  - src/components/cards/SimpleLinkCard.tsx
  - src/routes/index.tsx
  - src/styles/base.css
  - data/site.json
  - data/links.json
files-modified:
  - schema/links.schema.json
  - src/lib/content/load-content.ts
  - src/lib/ui/rich-card-policy.ts
  - src/components/cards/RichLinkCard.tsx
  - src/components/cards/SimpleLinkCard.tsx
  - src/routes/index.tsx
  - src/styles/base.css
  - data/site.json
  - data/links.json
autonomous: true
requirements:
  - DATA-03
  - UI-03
user_setup: []
objective: Extend rich-card data/rendering contract with resilient fallback behavior and global override controls
must_haves:
  truths:
    - Links can remain `simple` or `rich` through JSON configuration without changing canonical ordering.
    - Rich cards render title, description, image, icon, and configurable source/domain labeling when metadata exists.
    - Rich links with missing or partial metadata still render a stable rich-shell fallback.
    - Developers can globally force rich links to render as simple cards while preserving source data.
    - Rich cards respect the same link target/open policy used by simple cards.
  artifacts:
    - path: schema/links.schema.json
      provides: Extended rich metadata and opt-out/override config contract
      min_lines: 80
    - path: src/components/cards/RichLinkCard.tsx
      provides: Rich-card component with fallback-safe rendering surface
      min_lines: 60
    - path: src/lib/ui/rich-card-policy.ts
      provides: Central variant resolution and source-label visibility rules
      min_lines: 35
    - path: src/routes/index.tsx
      provides: Mixed simple/rich rendering flow with order-preserving behavior
      min_lines: 110
  key_links:
    - from: src/routes/index.tsx
      to: src/lib/ui/rich-card-policy.ts
      via: card variant + global override resolution
      pattern: "resolveRichCard"
    - from: src/routes/index.tsx
      to: src/components/cards/RichLinkCard.tsx
      via: rich variant rendering branch
      pattern: "RichLinkCard"
    - from: src/lib/content/load-content.ts
      to: schema/links.schema.json
      via: typed link metadata fields matching schema shape
      pattern: "metadata"
---

<objective>
Deliver the core rich-card rendering contract and fallback model.

Purpose: Enable per-link simple/rich behavior without breaking existing ordering and interaction policy.
Output: Extended schema/types/config plus rich component rendering with resilient fallback and global override mode.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rich-cards-content-enrichment/03-CONTEXT.md
@.planning/phases/03-rich-cards-content-enrichment/03-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend rich-card schema and typed content contract</name>
  <files>schema/links.schema.json, src/lib/content/load-content.ts, data/site.json, data/links.json</files>
  <action>Expand the links/site contract for rich-card display preferences, source/domain label visibility, and global rich-as-simple override behavior while keeping `type=rich` optional metadata semantics intact.</action>
  <verify>Run validation and typecheck to confirm new contract fields are accepted without warnings for starter data.</verify>
  <done>Schema and TypeScript models encode Phase 3 rich-card/fallback controls with backward compatibility for existing links.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Build rich-card renderer with fallback-first view model</name>
  <files>src/components/cards/RichLinkCard.tsx, src/lib/ui/rich-card-policy.ts, src/routes/index.tsx, src/styles/base.css</files>
  <action>Implement a rich-card component that defaults to title/description/image/source presentation and gracefully degrades when metadata is missing or partial. Route rendering must preserve configured order and branch by resolved card variant.</action>
  <verify>Manual UI check confirms rich cards render fallback shell instead of collapsing or crashing when preview fields are absent.</verify>
  <done>Rich card rendering is stable across complete, partial, and missing metadata inputs.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Wire global rich-as-simple mode and target policy parity</name>
  <files>src/lib/ui/rich-card-policy.ts, src/components/cards/SimpleLinkCard.tsx, src/routes/index.tsx, data/site.json</files>
  <action>Add a global config switch that forces rich links through the simple renderer while preserving data. Ensure rich cards (when enabled) follow the same target/open policy as simple cards.</action>
  <verify>Toggle global override in site config and confirm rich links render as simple while order and navigation behavior stay unchanged.</verify>
  <done>Developer can safely disable rich rendering globally without data migration and without interaction regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Schema/type updates allow `type=rich` with optional metadata and new policy controls.
- [ ] Rich cards render resilient fallback output for missing/partial metadata.
- [ ] Global rich-as-simple switch preserves ordering and link navigation parity.
</verification>

<success_criteria>
- All tasks completed.
- DATA-03 and UI-03 rendering/fallback requirements are established in app code.
- Plan creates stable foundation for enrichment pipeline in 03-02.
</success_criteria>

<output>
After completion, create .planning/phases/03-rich-cards-content-enrichment/03-01-SUMMARY.md
</output>
