---
phase: 18-referral-contract-link-plumbing
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - scripts/validation/rules.ts
  - scripts/validation/rules.test.ts
  - scripts/validate-data.ts
  - data/examples/minimal/links.json
  - data/examples/grouped/links.json
files-modified:
  - scripts/validation/rules.ts
  - scripts/validation/rules.test.ts
  - scripts/validate-data.ts
  - data/examples/minimal/links.json
  - data/examples/grouped/links.json
autonomous: true
requirements:
  - REF-03
  - REF-02
user_setup: []
objective: Add referral-specific warnings, generated/manual conflict reporting, and example coverage so the new contract stays flexible without becoming opaque or error-prone
must_haves:
  truths:
    - Soft referral markers such as `referral: {}` remain valid but produce informative warnings when no meaningful disclosure fields are present.
    - `kind` alone is not treated as sufficient disclosure content.
    - Referral links on supported social/profile domains can explicitly opt into `enrichment.profileSemantics="non_profile"`, and validation nudges maintainers toward that safer default for promo/referral links.
    - Manual/generated referral mismatches surface as warning-level diagnostics rather than silent drift.
    - Example data demonstrates both a disclosure-light referral marker and a fuller referral shape without breaking the current build.
  artifacts:
    - path: scripts/validation/rules.ts
      provides: referral-specific warning rules for disclosure completeness and supported-profile defaults
      min_lines: 950
    - path: scripts/validation/rules.test.ts
      provides: regression coverage for referral warnings and non_profile nudges
      min_lines: 180
    - path: scripts/validate-data.ts
      provides: generated/manual referral conflict reporting in the full validation pass
      min_lines: 1500
    - path: data/examples/minimal/links.json
      provides: one additive example of a soft or simple referral shape in the minimal example set
      min_lines: 10
    - path: data/examples/grouped/links.json
      provides: one fuller referral example, including a supported-family non_profile case where appropriate
      min_lines: 40
  key_links:
    - from: scripts/validation/rules.ts
      to: scripts/validate-data.ts
      via: warning surfaces for manual contract completeness versus generated/manual conflicts
      pattern: "referral"
    - from: scripts/validation/rules.test.ts
      to: scripts/validation/rules.ts
      via: referral warning expectations including non_profile guidance
      pattern: "warning"
    - from: data/examples/grouped/links.json
      to: schema/links.schema.json
      via: additive example contract proving the new referral shape is valid
      pattern: "referral"
---

<objective>
Finish Phase 18 by making the new contract understandable and safe for maintainers before execution moves on to extraction or card UI.

Purpose: keep referral support flexible while surfacing the warnings and example coverage maintainers need to use it confidently.
Output: referral warning rules, conflict diagnostics, and example link data that prove the contract is additive.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-referral-contract-link-plumbing/18-CONTEXT.md
@.planning/phases/18-referral-contract-link-plumbing/18-RESEARCH.md
@.planning/phases/18-referral-contract-link-plumbing/18-01-PLAN.md
@.planning/phases/18-referral-contract-link-plumbing/18-02-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add referral-specific validation warnings for disclosure-light entries</name>
  <files>scripts/validation/rules.ts, scripts/validation/rules.test.ts</files>
  <action>Add warning-level validation rules for referral markers with no meaningful disclosure fields, for `kind`-only referral objects, and for supported social/profile-family referral links that should usually set `enrichment.profileSemantics=\"non_profile\"`. Keep the warnings informative and non-blocking so soft referral entries remain allowed.</action>
  <verify>Run `bun test scripts/validation/rules.test.ts` and `bun run validate:data`.</verify>
  <done>Maintainers receive clear guidance about incomplete referral disclosures and safer defaults without losing the flexibility approved in Phase 18 context.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Surface manual/generated referral conflicts in the full validation pass</name>
  <files>scripts/validate-data.ts, scripts/validation/rules.ts</files>
  <action>Extend the full validation flow to compare manual referral fields against generated referral augmentation data where available and emit warning-level mismatch diagnostics. Keep the behavior aligned with the approved policy: manual remains authoritative, but drift should not stay silent.</action>
  <verify>Add or extend validation tests where practical, then run `bun run validate:data` and `bun run typecheck`.</verify>
  <done>The repo can flag referral disclosure drift before later phases start relying on generated referral data more heavily.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Seed example data and regression coverage for the additive referral contract</name>
  <files>data/examples/minimal/links.json, data/examples/grouped/links.json, scripts/validation/rules.test.ts</files>
  <action>Update the example link datasets so maintainers can see both a lightweight referral marker and a fuller referral disclosure shape. Include at least one supported-family referral example that uses `profileSemantics: \"non_profile\"` to demonstrate the approved contract path for offer links that would otherwise read like profiles.</action>
  <verify>Run `bun run validate:data`, `bun run build`, and the targeted validation tests covering the new examples.</verify>
  <done>Phase 18 ends with concrete example data and regression coverage showing maintainers how the contract should be used safely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Soft referral markers warn rather than fail.
- [ ] `kind` alone is not treated as sufficient disclosure.
- [ ] Supported-profile referral links can use `enrichment.profileSemantics="non_profile"` and validation nudges maintainers toward it.
- [ ] Manual/generated referral mismatches surface as warnings.
- [ ] Example data demonstrates the additive contract without breaking existing builds.
</verification>

<success_criteria>
- All tasks completed.
- REF-03 and the warning/conflict half of REF-02 are covered before later extraction/UI phases begin.
- Maintainers have both warnings and examples that make the new referral contract understandable in practice.
</success_criteria>

<output>
After completion, create .planning/phases/18-referral-contract-link-plumbing/18-03-SUMMARY.md
</output>
