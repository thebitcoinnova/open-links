---
phase: 18-referral-contract-link-plumbing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/links.schema.json
  - src/lib/content/load-content.ts
  - src/lib/content/referral-fields.ts
  - src/lib/content/referral-fields.test.ts
files-modified:
  - schema/links.schema.json
  - src/lib/content/load-content.ts
  - src/lib/content/referral-fields.ts
  - src/lib/content/referral-fields.test.ts
autonomous: true
requirements:
  - REF-01
  - REF-02
user_setup: []
objective: Define the first-class manual `links[].referral` contract, runtime link types, and shared referral field helpers without introducing a new link type or rich-only authoring path
must_haves:
  truths:
    - `links[].referral` exists as an optional top-level link object rather than a new `link.type` or a field nested under rich-card metadata.
    - The manual contract uses neutral names and supports `kind`, `visitorBenefit`, `ownerBenefit`, `offerSummary`, `termsSummary`, `termsUrl`, `code`, and `custom`.
    - `referral: {}` is valid as a soft marker, and one-sided benefit disclosures remain valid.
    - No dedicated `disclosureLabel` field is added in Phase 18.
    - Runtime `OpenLink` typing and shared helper logic can carry referral data for both `simple` and `rich` links.
  artifacts:
    - path: schema/links.schema.json
      provides: the additive manual referral contract on link entries
      min_lines: 320
    - path: src/lib/content/load-content.ts
      provides: runtime types carrying `link.referral` through loaded content
      min_lines: 300
    - path: src/lib/content/referral-fields.ts
      provides: shared referral field lists, normalization helpers, and merge primitives for later phases
      min_lines: 90
    - path: src/lib/content/referral-fields.test.ts
      provides: focused coverage for the additive contract, one-sided disclosures, and soft-marker semantics
      min_lines: 120
  key_links:
    - from: schema/links.schema.json
      to: src/lib/content/load-content.ts
      via: `LinkReferralConfig` / runtime referral typing parity
      pattern: "referral"
    - from: src/lib/content/referral-fields.ts
      to: src/lib/content/load-content.ts
      via: shared field names and merge helpers reused by the runtime model
      pattern: "referral"
    - from: src/lib/content/referral-fields.test.ts
      to: src/lib/content/referral-fields.ts
      via: additive soft-marker and one-sided-benefit contract coverage
      pattern: "visitorBenefit"
---

<objective>
Define the referral contract before generated augmentation or validation policy starts layering on top of it.

Purpose: make referral disclosure a stable, typed part of the link model without forcing maintainers into a new link type or a rich-only path.
Output: schema updates, runtime typing, and shared referral helpers that later plans can build on.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-referral-contract-link-plumbing/18-CONTEXT.md
@.planning/phases/18-referral-contract-link-plumbing/18-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend the manual link schema and runtime types for `links[].referral`</name>
  <files>schema/links.schema.json, src/lib/content/load-content.ts</files>
  <action>Add an optional top-level `referral` object to the link contract with the approved neutral field names and no new `link.type`. Ensure the runtime `OpenLink` / `LinksData` surfaces can carry referral data for both `simple` and `rich` links without moving the feature under `metadata`.</action>
  <verify>Run `bun run typecheck` and `bun run validate:data` to prove the additive schema accepts current content without breakage.</verify>
  <done>The repo has one first-class manual referral contract that does not require a new card type or a rich-only authoring path.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Create shared referral field and merge helpers</name>
  <files>src/lib/content/referral-fields.ts, src/lib/content/load-content.ts</files>
  <action>Create a small shared helper module that defines canonical referral field lists, normalization helpers, and merge primitives that later generated-data and validation work can reuse. Keep the helpers centered on the approved semantics: neutral names, soft markers, and one-sided benefits.</action>
  <verify>Add focused unit coverage in `src/lib/content/referral-fields.test.ts`, then run `bun test src/lib/content/referral-fields.test.ts` and `bun run typecheck`.</verify>
  <done>Referral contract semantics live in one reusable seam instead of being duplicated across schema, runtime, and future enrichment code.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Prove the contract is additive and soft-marker-safe</name>
  <files>src/lib/content/referral-fields.test.ts, schema/links.schema.json</files>
  <action>Add regression tests that prove `referral: {}` is accepted, one-sided benefit fields are valid, and the contract remains optional/additive for all current links. Keep the tests focused on schema/runtime semantics rather than later extraction or UI behavior.</action>
  <verify>Run `bun test src/lib/content/referral-fields.test.ts`, `bun run validate:data`, and `bun run build`.</verify>
  <done>Phase 18 has a stable contract baseline before any generated augmentation or warning logic is introduced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `links[].referral` exists as an optional top-level link object.
- [ ] The manual contract uses the approved neutral fields and omits `disclosureLabel`.
- [ ] `referral: {}` remains valid and one-sided benefits remain valid.
- [ ] Runtime types and shared helpers carry referral data for both `simple` and `rich` links.
</verification>

<success_criteria>
- All tasks completed.
- REF-01 and the manual-contract half of REF-02 are established before generated augmentation or warning logic begins.
- The repo has one stable, reusable referral contract seam for later phases.
</success_criteria>

<output>
After completion, create .planning/phases/18-referral-contract-link-plumbing/18-01-SUMMARY.md
</output>
