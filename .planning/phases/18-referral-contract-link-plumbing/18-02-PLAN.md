---
phase: 18-referral-contract-link-plumbing
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - scripts/enrichment/types.ts
  - scripts/enrichment/generated-metadata.ts
  - scripts/enrichment/generated-metadata.test.ts
  - src/lib/content/load-content.ts
  - src/lib/content/referral-fields.ts
  - src/lib/content/referral-fields.test.ts
files-modified:
  - scripts/enrichment/types.ts
  - scripts/enrichment/generated-metadata.ts
  - scripts/enrichment/generated-metadata.test.ts
  - src/lib/content/load-content.ts
  - src/lib/content/referral-fields.ts
  - src/lib/content/referral-fields.test.ts
autonomous: true
requirements:
  - REF-02
user_setup: []
objective: Extend the existing per-link generated augmentation path so referral data and per-field provenance can be merged safely with manual referral disclosures
must_haves:
  truths:
    - Existing generated metadata manifests remain valid when they only contain `metadata`.
    - Per-link generated entries can optionally carry a `referral` sibling without introducing a second generated manifest.
    - Runtime loading merges referral data field-by-field with manual values winning and generated values filling blanks only.
    - The merged runtime surface preserves enough referral provenance for later validation and UI work.
  artifacts:
    - path: scripts/enrichment/types.ts
      provides: generated referral augmentation and provenance typing
      min_lines: 70
    - path: scripts/enrichment/generated-metadata.ts
      provides: normalization and stabilization for optional per-link generated referral data
      min_lines: 180
    - path: scripts/enrichment/generated-metadata.test.ts
      provides: backward-compatible manifest and referral-entry regression coverage
      min_lines: 90
    - path: src/lib/content/load-content.ts
      provides: runtime merge of manual and generated referral data
      min_lines: 320
    - path: src/lib/content/referral-fields.ts
      provides: shared referral merge + provenance helpers reused by generation and runtime loading
      min_lines: 120
  key_links:
    - from: scripts/enrichment/generated-metadata.ts
      to: src/lib/content/load-content.ts
      via: optional generated `referral` entry support in the existing per-link augmentation manifest
      pattern: "referral"
    - from: src/lib/content/referral-fields.ts
      to: scripts/enrichment/generated-metadata.ts
      via: shared normalization / blank-fill-only merge semantics
      pattern: "provenance"
    - from: scripts/enrichment/generated-metadata.test.ts
      to: scripts/enrichment/generated-metadata.ts
      via: backward compatibility for metadata-only manifests plus new referral-entry coverage
      pattern: "metadata"
---

<objective>
Prepare the existing generated augmentation pipeline for referral data before Phase 19 starts extracting anything.

Purpose: create one safe merge path where manual referral fields remain authoritative and future generated data can fill gaps without hidden overrides.
Output: generated augmentation typing, backward-compatible manifest handling, and runtime merge behavior with provenance.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-referral-contract-link-plumbing/18-CONTEXT.md
@.planning/phases/18-referral-contract-link-plumbing/18-RESEARCH.md
@.planning/phases/18-referral-contract-link-plumbing/18-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend the generated per-link augmentation shape for referral data</name>
  <files>scripts/enrichment/types.ts, scripts/enrichment/generated-metadata.ts</files>
  <action>Extend the existing per-link generated metadata manifest so each link entry can optionally carry a `referral` sibling with generated referral fields and provenance. Keep metadata-only manifests valid and avoid introducing a second generated file or a breaking rename of the existing manifest path.</action>
  <verify>Add regression coverage in `scripts/enrichment/generated-metadata.test.ts`, then run `bun test scripts/enrichment/generated-metadata.test.ts` and `bun run typecheck`.</verify>
  <done>The generated augmentation format is ready for referral data without breaking current metadata-only enrichment flows.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Merge manual and generated referral fields with blank-fill-only behavior</name>
  <files>src/lib/content/referral-fields.ts, src/lib/content/load-content.ts</files>
  <action>Wire runtime loading so manual referral values win field-by-field while generated referral data fills blanks and carries per-field provenance where practical. Keep the merge behavior explicit and aligned with the approved auditability/debuggability goals.</action>
  <verify>Add focused merge/provenance coverage in `src/lib/content/referral-fields.test.ts`, then run `bun test src/lib/content/referral-fields.test.ts` and `bun run typecheck`.</verify>
  <done>Loaded links expose a stable merged referral surface that later validation and UI code can trust.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Prove backward compatibility and manual precedence</name>
  <files>scripts/enrichment/generated-metadata.test.ts, src/lib/content/referral-fields.test.ts, src/lib/content/load-content.ts</files>
  <action>Add regression tests proving that existing generated manifests without `referral` still normalize/load correctly, that manual referral fields stay authoritative, and that generated fields only fill blanks. Keep the assertions focused on merge semantics rather than later extraction behavior.</action>
  <verify>Run `bun test scripts/enrichment/generated-metadata.test.ts src/lib/content/referral-fields.test.ts`, `bun run typecheck`, and `bun run build`.</verify>
  <done>Phase 18 has a safe generated-data seam in place before any public referral extraction work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Generated metadata manifests remain backward compatible when they only contain `metadata`.
- [ ] Per-link generated entries can optionally carry `referral` data and provenance.
- [ ] Manual referral values win field-by-field and generated referral values fill only blanks.
- [ ] Runtime loaded links preserve enough referral provenance for later validation/UI work.
</verification>

<success_criteria>
- All tasks completed.
- REF-02 is materially advanced through a stable merge/provenance path for future generated referral data.
- Phase 19 can start extraction work without reopening the generated augmentation architecture.
</success_criteria>

<output>
After completion, create .planning/phases/18-referral-contract-link-plumbing/18-02-SUMMARY.md
</output>
