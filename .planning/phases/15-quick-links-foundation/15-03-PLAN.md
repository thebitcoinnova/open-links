---
phase: 15-quick-links-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - src/lib/ui/profile-quick-links.test.ts
  - src/components/profile/ProfileHeader.test.tsx
files-modified:
  - src/lib/ui/profile-quick-links.test.ts
  - src/components/profile/ProfileHeader.test.tsx
autonomous: true
requirements:
  - QLINK-02
  - QLINK-03
  - QLINK-04
  - MAINT-01
user_setup: []
objective: Lock in the Phase 15 derivation contract with focused regression coverage and keep the research-to-plan trace clear before Phase 16 starts visual quick-link work
must_haves:
  truths:
    - The derivation contract is proven through focused helper and header-facing tests rather than left implicit in implementation details.
    - Test coverage proves filtering, ordering, dedupe, canonical tie-breaking, and empty-state suppression together.
    - The phase ends with enough verification clarity that Phase 16 can safely build visible UI on top of the foundation.
    - No Phase 16-specific visual-density or interaction behaviors leak into the Phase 15 verification scope.
  artifacts:
    - path: src/lib/ui/profile-quick-links.test.ts
      provides: end-to-end helper coverage for eligibility, ordering, duplicate winners, canonical tie-breaking, and empty results
      min_lines: 220
    - path: src/components/profile/ProfileHeader.test.tsx
      provides: header-facing coverage that quick-link state reaches the component cleanly and suppresses placeholder chrome
      min_lines: 280
  key_links:
    - from: src/lib/ui/profile-quick-links.test.ts
      to: src/lib/ui/profile-quick-links.ts
      via: exhaustive regression coverage for the Phase 15 derivation contract
      pattern: "quick"
    - from: src/components/profile/ProfileHeader.test.tsx
      to: src/components/profile/ProfileHeader.tsx
      via: header-facing contract verification without Phase 16 layout commitments
      pattern: "quick"
---

<objective>
End the phase with a stable contract and clear verification surface.

Purpose: prevent the Quick Links foundation from becoming “whatever the current code happens to do” before the UI phase starts layering visible behavior on top.
Output: focused regression coverage and a clean foundation handoff into Phase 16.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quick-links-foundation/15-CONTEXT.md
@.planning/phases/15-quick-links-foundation/15-RESEARCH.md
@.planning/phases/15-quick-links-foundation/15-01-PLAN.md
@.planning/phases/15-quick-links-foundation/15-02-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Expand regression coverage until the derivation contract is explicit</name>
  <files>src/lib/ui/profile-quick-links.test.ts</files>
  <action>Finish the helper-level regression matrix so the Phase 15 contract is explicit: eligible versus ineligible platforms, priority ordering versus content order, one winner per platform, canonical same-platform tie-breaking, and empty output behavior all need focused assertions.</action>
  <verify>Run `bun test src/lib/ui/profile-quick-links.test.ts` and `bun run typecheck`.</verify>
  <done>The quick-link derivation layer is covered well enough that Phase 16 does not have to rediscover business rules through UI regressions.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Lock the header-facing contract with focused non-visual tests</name>
  <files>src/components/profile/ProfileHeader.test.tsx</files>
  <action>Add or refine header-facing tests so the component contract stays stable around empty-state suppression and quick-link state handoff, without leaking Phase 16 visual layout requirements into the Phase 15 foundation suite.</action>
  <verify>Run `bun test src/components/profile/ProfileHeader.test.tsx` and `bun run biome:check`.</verify>
  <done>The header-facing Quick Links contract is proven stable before the visible strip implementation begins.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Run the final focused verification bundle for the foundation contract</name>
  <files>src/lib/ui/profile-quick-links.test.ts, src/components/profile/ProfileHeader.test.tsx</files>
  <action>After helper and header-facing tests are in place, run the final focused command set for the foundation layer and make any small test-scope corrections needed so Phase 15 ends with a clear, verifiable handoff into Phase 16.</action>
  <verify>Run `bun test src/lib/ui/profile-quick-links.test.ts src/components/profile/ProfileHeader.test.tsx`, `bun run typecheck`, and `bun run build`.</verify>
  <done>The phase closes with a concrete verification path and no ambiguity about what the Quick Links foundation guarantees.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Helper tests cover eligibility, ordering, one-winner-per-platform, canonical tie-breaking, and empty output.
- [ ] Header-facing tests prove empty-state suppression and stable data handoff.
- [ ] The Phase 15 verification surface stays foundation-focused and does not absorb Phase 16 visual fit decisions.
- [ ] The final focused command set passes for the foundation layer.
</verification>

<success_criteria>
- All tasks completed.
- Phase 15 ends with a stable, well-tested Quick Links foundation contract.
- Phase 16 can focus on visible UI and accessibility instead of rediscovering data rules.
</success_criteria>

<output>
After completion, create .planning/phases/15-quick-links-foundation/15-03-SUMMARY.md
</output>
