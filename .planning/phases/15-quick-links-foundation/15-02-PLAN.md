---
phase: 15-quick-links-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/lib/ui/profile-quick-links.ts
  - src/routes/index.tsx
  - src/components/profile/ProfileHeader.tsx
  - src/components/profile/ProfileHeader.test.tsx
files-modified:
  - src/lib/ui/profile-quick-links.ts
  - src/routes/index.tsx
  - src/components/profile/ProfileHeader.tsx
  - src/components/profile/ProfileHeader.test.tsx
autonomous: true
requirements:
  - MAINT-01
user_setup: []
objective: Wire the ordered quick-link derivation into a header-facing model and ensure the profile header can cleanly suppress the section when no quick links qualify
must_haves:
  truths:
    - The header-facing Quick Links model is derived from the shared resolver rather than duplicated in route or component code.
    - Empty eligibility produces no Quick Links section state at all rather than placeholder markup or fallback chrome.
    - The integration stays foundation-focused and does not pre-empt the visual density or fit decisions reserved for Phase 16.
    - `ProfileHeader` receives a clean contract for future Quick Links rendering instead of recomputing eligibility internally.
  artifacts:
    - path: src/lib/ui/profile-quick-links.ts
      provides: any header-facing view-model helpers or empty-state semantics needed beyond the pure derivation logic
      min_lines: 180
    - path: src/routes/index.tsx
      provides: one route-level integration point that derives quick-link data from loaded content and passes it into the profile header seam
      min_lines: 540
    - path: src/components/profile/ProfileHeader.tsx
      provides: a prop/model seam for future Quick Links rendering without forcing Phase 16 layout work early
      min_lines: 160
    - path: src/components/profile/ProfileHeader.test.tsx
      provides: focused assertions that the header handles empty versus populated quick-link state without placeholder regressions
      min_lines: 260
  key_links:
    - from: src/routes/index.tsx
      to: src/lib/ui/profile-quick-links.ts
      via: route-level derivation of header quick-link state from loaded content
      pattern: "quick"
    - from: src/routes/index.tsx
      to: src/components/profile/ProfileHeader.tsx
      via: header-facing quick-link model passed through an explicit prop seam
      pattern: "ProfileHeader"
    - from: src/components/profile/ProfileHeader.test.tsx
      to: src/components/profile/ProfileHeader.tsx
      via: empty-state suppression and future-facing contract coverage
      pattern: "quick"
---

<objective>
Connect the derivation layer to the profile header seam without doing the visible strip design yet.

Purpose: make the Quick Links foundation consumable by the existing header/render path so Phase 16 can focus on layout and accessibility rather than rediscovering data flow.
Output: route-level quick-link derivation, a header prop/model seam, and empty-state suppression that leaves no placeholder chrome.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quick-links-foundation/15-CONTEXT.md
@.planning/phases/15-quick-links-foundation/15-RESEARCH.md
@.planning/phases/15-quick-links-foundation/15-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add a header-facing quick-link model and route integration</name>
  <files>src/lib/ui/profile-quick-links.ts, src/routes/index.tsx</files>
  <action>Build on the Phase 15 resolver by exposing a small header-facing quick-link model, then integrate it at the route level from the existing loaded content. Keep the route as the place where content becomes header props, and do not fork or duplicate the derivation logic there.</action>
  <verify>Run `bun run typecheck` and any focused quick-link helper tests needed after the route integration.</verify>
  <done>The profile header can receive stable quick-link state from one shared derivation path.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Extend the profile-header contract for quick-link state without visual overreach</name>
  <files>src/components/profile/ProfileHeader.tsx, src/routes/index.tsx</files>
  <action>Add the prop/model seam `ProfileHeader` will need for Quick Links while keeping Phase 15 foundation-only. The goal is to support a future strip render path and empty-state suppression without locking Phase 16 into premature layout choices.</action>
  <verify>Run `bun run typecheck` and `bun run build` to confirm the header contract remains stable.</verify>
  <done>The header is ready to consume Quick Links data in Phase 16 without recomputing or reshaping the derivation contract.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add focused tests for empty-state suppression and contract stability</name>
  <files>src/components/profile/ProfileHeader.test.tsx, src/lib/ui/profile-quick-links.test.ts</files>
  <action>Add targeted assertions that empty quick-link state produces no placeholder chrome and that populated quick-link state reaches the header seam in the expected shape. Keep the tests structural and foundation-focused rather than visual-density-focused.</action>
  <verify>Run `bun test src/components/profile/ProfileHeader.test.tsx src/lib/ui/profile-quick-links.test.ts`, `bun run typecheck`, and `bun run biome:check`.</verify>
  <done>Phase 16 inherits a stable header-facing contract and a proven empty-state behavior instead of guessing at it during UI work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Quick-link state is derived once and passed into the profile header via an explicit seam.
- [ ] No placeholder chrome appears when the quick-link result is empty.
- [ ] Phase 15 stops short of visual density and fit decisions reserved for Phase 16.
- [ ] `ProfileHeader` is ready for later Quick Links rendering without internal derivation duplication.
</verification>

<success_criteria>
- All tasks completed.
- The repo has a stable route-to-header Quick Links foundation.
- Empty-state suppression is locked down before visible UI work begins.
</success_criteria>

<output>
After completion, create .planning/phases/15-quick-links-foundation/15-02-SUMMARY.md
</output>
