---
phase: 15-quick-links-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ui/profile-quick-links.ts
  - src/lib/ui/profile-quick-links.test.ts
  - src/lib/content/social-profile-fields.ts
  - src/lib/content/load-content.ts
files-modified:
  - src/lib/ui/profile-quick-links.ts
  - src/lib/ui/profile-quick-links.test.ts
  - src/lib/content/social-profile-fields.ts
  - src/lib/content/load-content.ts
autonomous: true
requirements:
  - QLINK-02
  - QLINK-03
  - QLINK-04
  - MAINT-01
user_setup: []
objective: Define the pure Quick Links derivation rules, priority ordering, and same-platform tie-break behavior from enabled top-level links without introducing a second source of truth
must_haves:
  truths:
    - Quick Links derive from enabled `links[]` entries and never from `profileLinks` or another parallel registry.
    - Eligibility is limited to OpenLinks-supported social/profile-style platforms rather than the full known-site registry.
    - The derivation result orders winners by the locked priority list first, then existing content order.
    - At most one quick-link winner exists per platform.
    - A canonical quick-link marker only resolves same-platform ties and does not force inclusion outside the default eligibility rules.
  artifacts:
    - path: src/lib/ui/profile-quick-links.ts
      provides: the pure resolver for eligibility, platform priority, content-order tie-breaking, and canonical tie-break handling
      min_lines: 140
    - path: src/lib/ui/profile-quick-links.test.ts
      provides: focused coverage for filtering, ordering, same-platform dedupe, and canonical tie-breaking
      min_lines: 160
    - path: src/lib/content/social-profile-fields.ts
      provides: reusable helpers or exports that let quick-link derivation stay aligned with the repo's supported profile-platform boundary
      min_lines: 180
    - path: src/lib/content/load-content.ts
      provides: any type-level support needed for the chosen namespaced canonical marker extension path
      min_lines: 200
  key_links:
    - from: src/lib/ui/profile-quick-links.ts
      to: src/lib/content/social-profile-fields.ts
      via: supported social/profile platform eligibility instead of full known-site inclusion
      pattern: "profile"
    - from: src/lib/ui/profile-quick-links.ts
      to: src/lib/content/load-content.ts
      via: typed access to link-level extension data for canonical tie-breaking
      pattern: "custom"
    - from: src/lib/ui/profile-quick-links.test.ts
      to: src/lib/ui/profile-quick-links.ts
      via: pure helper verification for ordering, dedupe, and empty-result behavior
      pattern: "quick"
---

<objective>
Lock the derivation contract before any header-facing integration starts.

Purpose: create one pure, testable Quick Links resolver that downstream header/UI work can consume without re-litigating eligibility or ordering.
Output: a shared helper for eligibility/order/tie-break behavior plus enough typing support to read the chosen canonical marker path safely.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quick-links-foundation/15-CONTEXT.md
@.planning/phases/15-quick-links-foundation/15-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Create a pure resolver for quick-link eligibility and ordering</name>
  <files>src/lib/ui/profile-quick-links.ts, src/lib/content/social-profile-fields.ts</files>
  <action>Create a small pure helper that takes top-level links and returns the ordered Quick Links foundation set. Reuse the repo's supported social/profile platform boundary rather than the full known-site list, encode the locked platform priority list, and keep the result free of UI-specific fit or layout decisions.</action>
  <verify>Run focused quick-link helper tests if present and `bun run typecheck` once the helper compiles.</verify>
  <done>The repo has one reusable source of truth for which links qualify as Quick Links and what order they should appear in.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add same-platform tie-break support without creating a second registry</name>
  <files>src/lib/ui/profile-quick-links.ts, src/lib/content/load-content.ts</files>
  <action>Choose a namespaced extension path for the same-platform canonical marker, read it from the existing link model, and make the resolver honor it only when multiple links map to the same platform. Keep the marker strictly tie-break-only and avoid turning it into a separate quick-link inclusion list.</action>
  <verify>Run `bun run typecheck` and any focused helper tests covering duplicate same-platform links.</verify>
  <done>Execution can pick one stable winner per platform without introducing another authoring surface besides `links[]`.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add focused regression coverage for derivation, ordering, dedupe, and empty-state output</name>
  <files>src/lib/ui/profile-quick-links.test.ts</files>
  <action>Add pure-helper coverage proving that eligible platforms are filtered correctly, the locked priority list beats plain content order, one winner per platform is enforced, the canonical marker only breaks same-platform ties, and links outside the supported social/profile boundary produce an empty result.</action>
  <verify>Run `bun test src/lib/ui/profile-quick-links.test.ts`, `bun run typecheck`, and `bun run biome:check`.</verify>
  <done>The derivation contract is locked down before header-facing integration begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Quick Links derive from enabled `links[]`, not `profileLinks`.
- [ ] Eligibility stays limited to supported social/profile-style platforms.
- [ ] Ordering follows the locked priority list first, then content order.
- [ ] Canonical tie-breaking only affects duplicates within the same platform.
- [ ] The helper returns an empty result cleanly when nothing qualifies.
</verification>

<success_criteria>
- All tasks completed.
- The repo has a stable, pure Quick Links derivation helper.
- Same-platform tie-breaking works without introducing a duplicate quick-link registry.
</success_criteria>

<output>
After completion, create .planning/phases/15-quick-links-foundation/15-01-SUMMARY.md
</output>
