---
phase: 01-bootstrap-data-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/routes/index.tsx
  - src/lib/content/load-content.ts
  - data/profile.json
  - data/links.json
  - data/site.json
  - schema/profile.schema.json
  - schema/links.schema.json
  - schema/site.schema.json
files-modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/routes/index.tsx
  - src/lib/content/load-content.ts
  - data/profile.json
  - data/links.json
  - data/site.json
  - schema/profile.schema.json
  - schema/links.schema.json
  - schema/site.schema.json
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
  - DATA-01
  - DATA-02
user_setup: []
objective: Bootstrap SolidJS app skeleton with split data contract and schema foundations
must_haves:
  truths:
    - Fresh clone has a runnable SolidJS app scaffold configured for OpenLinks.
    - Split data files (`profile`, `links`, `site`) exist and are consumed by the app loader.
    - JSON schemas define required identity/link fields including hybrid grouping + ordering metadata.
  artifacts:
    - path: package.json
      provides: SolidJS app scripts and dependency declarations
      contains: "validate:data"
    - path: data/profile.json
      provides: Starter profile data in split-file model
      min_lines: 8
    - path: data/links.json
      provides: Starter link data with metadata-capable fields
      min_lines: 12
    - path: schema/links.schema.json
      provides: Link contract for id/label/url/type/group/order fields
      contains: "type"
    - path: src/lib/content/load-content.ts
      provides: Typed content loading from split data files
      min_lines: 20
  key_links:
    - from: src/lib/content/load-content.ts
      to: data/profile.json
      via: profile import/read path
      pattern: "profile"
    - from: src/lib/content/load-content.ts
      to: data/links.json
      via: links import/read path
      pattern: "links"
    - from: src/routes/index.tsx
      to: src/lib/content/load-content.ts
      via: page content loading call
      pattern: "loadContent"
---

<objective>
Create the baseline project and data-contract skeleton for OpenLinks.

Purpose: Deliver a template-ready foundation that contributors can fork and run.
Output: SolidJS scaffold, split data files, schema files, and content loader wired into the root route.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-data-contract/01-CONTEXT.md
@.planning/phases/01-bootstrap-data-contract/01-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Scaffold SolidJS workspace and split content structure</name>
  <files>package.json, tsconfig.json, vite.config.ts, src/routes/index.tsx, data/profile.json, data/links.json, data/site.json</files>
  <action>Create a minimal SolidJS project scaffold suitable for static-site generation. Add starter split data files with one profile and three links, include optional grouping and explicit order fields, and set one default active theme with one alternate theme in site config.</action>
  <verify>Install dependencies and run the baseline app build command without runtime errors.</verify>
  <done>Project boots and root route can read starter split data files in a deterministic structure.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Define schema contracts for profile, links, and site data</name>
  <files>schema/profile.schema.json, schema/links.schema.json, schema/site.schema.json</files>
  <action>Author JSON schemas for each split file. Enforce required core fields, include metadata-capable link shape, support hybrid grouping and ordering, and preserve extension pathways expected by Phase 1 context decisions.</action>
  <verify>Run a schema-check script or fixture validation against starter data and confirm all files pass contract checks.</verify>
  <done>Schema artifacts exist and clearly encode required fields expected by BOOT-02, DATA-01, and DATA-02.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add typed content loader and connect root route</name>
  <files>src/lib/content/load-content.ts, src/routes/index.tsx</files>
  <action>Implement a content loader module that reads split data files into typed structures, then wire the root route to consume loader output. Keep this phase focused on contract wiring, not final visual polish.</action>
  <verify>Run type-check/build commands and confirm route rendering uses loaded content without hardcoded placeholders.</verify>
  <done>Route consumes split-file data via loader abstraction and compiles cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build command succeeds for scaffolded app.
- [ ] Starter data matches schema structure for profile, links, and site files.
- [ ] Root route reads content through loader module rather than hardcoded constants.
</verification>

<success_criteria>
- All tasks completed.
- Split-file data model is established and routable.
- Contract artifacts exist for profile + links + site.
- Plan sets up dependencies required by Phase 1 validation and docs plans.
</success_criteria>

<output>
After completion, create .planning/phases/01-bootstrap-data-contract/01-01-SUMMARY.md
</output>
