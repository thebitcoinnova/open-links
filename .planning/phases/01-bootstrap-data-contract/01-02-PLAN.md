---
phase: 01-bootstrap-data-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/validate-data.ts
  - scripts/validation/format-output.ts
  - scripts/validation/rules.ts
  - package.json
  - schema/profile.schema.json
  - schema/links.schema.json
  - schema/site.schema.json
  - data/examples/invalid/conflict-keys.json
  - data/examples/invalid/bad-scheme.json
files-modified:
  - scripts/validate-data.ts
  - scripts/validation/format-output.ts
  - scripts/validation/rules.ts
  - package.json
  - schema/profile.schema.json
  - schema/links.schema.json
  - schema/site.schema.json
  - data/examples/invalid/conflict-keys.json
  - data/examples/invalid/bad-scheme.json
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
user_setup: []
objective: Implement enforceable validation pipeline with strict mode and policy checks
must_haves:
  truths:
    - Invalid data fails validation with explicit field-level remediation guidance.
    - Validation supports warnings and strict mode escalation behavior.
    - URL scheme policy allows `http`, `https`, `mailto`, and `tel` and flags disallowed schemes.
    - Custom/core key conflicts are detected and blocked.
  artifacts:
    - path: scripts/validate-data.ts
      provides: CLI entry point for data validation
      min_lines: 50
    - path: scripts/validation/rules.ts
      provides: project-specific policy checks for key conflicts and scheme allowlist
      min_lines: 30
    - path: scripts/validation/format-output.ts
      provides: dual human + machine-readable output formatting
      min_lines: 25
    - path: package.json
      provides: validate and strict-validate scripts wired for local prebuild use
      contains: "validate:data:strict"
  key_links:
    - from: package.json
      to: scripts/validate-data.ts
      via: npm scripts
      pattern: "validate-data"
    - from: scripts/validate-data.ts
      to: scripts/validation/rules.ts
      via: policy-check function calls
      pattern: "rules"
    - from: scripts/validate-data.ts
      to: scripts/validation/format-output.ts
      via: formatter invocation
      pattern: "format"
---

<objective>
Implement the Phase 1 validation engine and strictness model.

Purpose: Make the data contract enforceable with actionable feedback before Phase 4 CI hardening.
Output: Validation scripts, policy checks, dual output format, and build/prebuild script wiring.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-data-contract/01-CONTEXT.md
@.planning/phases/01-bootstrap-data-contract/01-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Build schema validation command with strict-mode support</name>
  <files>scripts/validate-data.ts, package.json</files>
  <action>Implement a validation CLI that loads split data files, validates against schemas, classifies issues into errors and warnings, and supports a strict mode that escalates warnings to failures. Expose commands in package scripts for normal and strict execution.</action>
  <verify>Run normal and strict validation commands against starter data and confirm both exit behavior and summary output are correct.</verify>
  <done>Validation can run in local prebuild workflows with predictable pass/fail semantics.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add policy checks for URL schemes and custom-key conflicts</name>
  <files>scripts/validation/rules.ts, schema/*.schema.json</files>
  <action>Implement policy-layer checks beyond structural schema validation: allowlist schemes (`http`,`https`,`mailto`,`tel`) and explicit detection of custom keys that collide with reserved core keys. Ensure collisions fail validation with direct remediation guidance.</action>
  <verify>Run validator against intentionally invalid fixture files and confirm expected error/warning categories.</verify>
  <done>Policy checks enforce DATA-05 and DATA-06 behaviors with clear diagnostics.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Provide human + machine-readable output formats</name>
  <files>scripts/validation/format-output.ts, scripts/validate-data.ts</files>
  <action>Implement output modes for developer-friendly console summaries and machine-readable JSON details. Include field path, issue category, and "how to fix" text in human output. Keep output deterministic for CI parsing.</action>
  <verify>Run validator in both output modes and confirm all issues serialize consistently.</verify>
  <done>Validation output is both actionable for humans and consumable by automation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run validate:data` exits non-zero on invalid fixtures.
- [ ] Strict mode escalates warnings as configured.
- [ ] Output includes remediation hints and machine-readable detail payload.
</verification>

<success_criteria>
- All tasks completed.
- DATA-04/DATA-05/DATA-06 are enforceable through scripts.
- Validation behavior matches phase context decisions (warnings + strict mode + dual output).
</success_criteria>

<output>
After completion, create .planning/phases/01-bootstrap-data-contract/01-02-SUMMARY.md
</output>
