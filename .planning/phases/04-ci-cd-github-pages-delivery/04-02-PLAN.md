---
phase: 04-ci-cd-github-pages-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .github/workflows/deploy-pages.yml
  - .github/workflows/ci.yml
  - vite.config.ts
files-modified:
  - .github/workflows/deploy-pages.yml
  - .github/workflows/ci.yml
  - vite.config.ts
autonomous: true
requirements:
  - DEP-03
  - DEP-04
user_setup: []
objective: Implement GitHub Pages deployment promotion with artifact reuse fallback and branch/path-safe configuration
must_haves:
  truths:
    - Deployment runs automatically on successful mainline promotion and supports manual dispatch.
    - Deployment uses official GitHub Pages actions and cancels older in-progress deploys.
    - Deployment prefers CI artifact reuse and falls back to rebuild when artifact retrieval is unavailable.
    - Base path behavior is project-page-friendly by default with configurable override support.
    - Pages misconfiguration paths fail with explicit remediation guidance.
  artifacts:
    - path: .github/workflows/deploy-pages.yml
      provides: Pages deployment workflow with trigger guards, permissions, concurrency, and deploy steps
      min_lines: 130
    - path: vite.config.ts
      provides: Build-time base-path mode support aligned with workflow-provided environment
      min_lines: 12
    - path: .github/workflows/ci.yml
      provides: Artifact contract and promotion handoff compatibility for deployment workflow
      min_lines: 130
  key_links:
    - from: .github/workflows/deploy-pages.yml
      to: .github/workflows/ci.yml
      via: artifact reuse from CI run using shared artifact naming contract
      pattern: "openlinks-dist"
    - from: .github/workflows/deploy-pages.yml
      to: vite.config.ts
      via: BASE_PATH and/or mode environment passed into build fallback path
      pattern: "BASE_PATH"
    - from: .github/workflows/deploy-pages.yml
      to: .github/workflows/deploy-pages.yml
      via: deploy concurrency policy
      pattern: "cancel-in-progress: true"
---

<objective>
Deliver automated GitHub Pages publishing and path-safe deploy behavior.

Purpose: Ensure validated mainline commits publish static artifacts reliably with clear operator feedback.
Output: Deployment workflow and base-path handling that support project-page defaults and manual recovery paths.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ci-cd-github-pages-delivery/04-CONTEXT.md
@.planning/phases/04-ci-cd-github-pages-delivery/04-RESEARCH.md
@.planning/phases/04-ci-cd-github-pages-delivery/04-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Implement Pages deploy workflow triggers, permissions, and concurrency</name>
  <files>.github/workflows/deploy-pages.yml</files>
  <action>Create Pages deployment workflow that promotes on successful mainline CI completion and supports `workflow_dispatch`. Configure least-privilege permissions, Pages environment usage, and concurrency that cancels stale in-progress deploys.</action>
  <verify>Review trigger and `if` guard expressions to confirm deploy cannot run from PR events and does run for mainline/manual pathways.</verify>
  <done>DEP-03 workflow structure exists with deterministic promotion controls.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add artifact-first deployment with rebuild fallback</name>
  <files>.github/workflows/deploy-pages.yml, .github/workflows/ci.yml</files>
  <action>Wire deploy workflow to prefer CI-produced artifact reuse (shared artifact contract from 04-01) and implement fallback rebuild/upload path when artifact download fails or is unavailable.</action>
  <verify>Inspect artifact retrieval conditions and fallback guards; confirm deploy job still publishes when artifact reuse path is intentionally unavailable (manual dispatch scenario).</verify>
  <done>Mainline deploys are reproducible and resilient to artifact-availability edge cases.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Enforce base-path mode behavior and misconfiguration remediation output</name>
  <files>vite.config.ts, .github/workflows/deploy-pages.yml</files>
  <action>Align deploy environment/base path signals with Vite base handling so project pages are default while user/base overrides remain configurable. Add explicit failure messaging for common Pages misconfiguration states.</action>
  <verify>Run local build parity with representative `BASE_PATH`/mode values and inspect workflow failure summary/remediation steps for misconfiguration branches.</verify>
  <done>DEP-04 path handling is deployment-safe across default project pages and configurable overrides.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Deploy workflow supports auto mainline deploy + manual dispatch.
- [ ] Official Pages actions flow is used with concurrency cancellation.
- [ ] Artifact reuse path exists with rebuild fallback.
- [ ] Base path/project-page handling supports configurable overrides.
- [ ] Misconfiguration output includes clear remediation guidance.
</verification>

<success_criteria>
- All tasks completed.
- DEP-03 and DEP-04 are satisfied with stable GitHub Pages promotion behavior.
- Deploy flow is ready for quality gates in Phase 5.
</success_criteria>

<output>
After completion, create .planning/phases/04-ci-cd-github-pages-delivery/04-02-SUMMARY.md
</output>
