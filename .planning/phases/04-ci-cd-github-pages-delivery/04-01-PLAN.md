---
phase: 04-ci-cd-github-pages-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - package.json
files-modified:
  - .github/workflows/ci.yml
  - package.json
autonomous: true
requirements:
  - DEP-01
  - DEP-02
user_setup: []
objective: Implement pull-request and push CI checks with summary-first diagnostics and non-blocking strict signals
must_haves:
  truths:
    - Pull requests targeting `main` run required CI checks automatically.
    - Direct pushes run full CI checks on all branches.
    - Required checks include `validate:data`, `typecheck`, and `build`.
    - Strict checks run in CI as non-blocking warnings when standard checks pass.
    - Failure output is summary-first, includes raw logs, and provides PR-facing diagnostics.
  artifacts:
    - path: .github/workflows/ci.yml
      provides: CI workflow with required checks, trigger matrix, diagnostics, and strict-warning lane
      min_lines: 120
    - path: package.json
      provides: Stable script surface for CI invocations
      contains: "validate:data"
  key_links:
    - from: .github/workflows/ci.yml
      to: package.json
      via: required validation/build/typecheck script execution
      pattern: "npm run validate:data"
    - from: .github/workflows/ci.yml
      to: package.json
      via: strict non-blocking signal execution
      pattern: "validate:data:strict"
    - from: .github/workflows/ci.yml
      to: .github/workflows/deploy-pages.yml
      via: deploy-consumable build artifact naming contract
      pattern: "openlinks-dist"
---

<objective>
Create CI workflow coverage for pull requests and pushes.

Purpose: Guarantee repository-integrated checks run consistently before merge/deploy.
Output: GitHub Actions CI workflow that executes required checks, exposes strict warnings, and emits actionable diagnostics.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ci-cd-github-pages-delivery/04-CONTEXT.md
@.planning/phases/04-ci-cd-github-pages-delivery/04-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Create required CI workflow for PR-main and push events</name>
  <files>.github/workflows/ci.yml</files>
  <action>Implement CI triggers for pull requests targeting `main` and all direct pushes. Add jobs/steps that execute `npm run validate:data`, `npm run typecheck`, and `npm run build` as required checks with deterministic Node setup and dependency install.</action>
  <verify>Inspect workflow trigger matrix and run local command parity (`npm run validate:data`, `npm run typecheck`, `npm run build`) to confirm workflow command path validity.</verify>
  <done>DEP-01 and baseline DEP-02 checks are represented in workflow with required check naming stability.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add strict non-blocking CI signal lane and summary-first reporting</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <action>Add strict validation/build checks as non-blocking CI signals (warnings only in Phase 4) and ensure job summaries place concise remediation-first output before raw command logs.</action>
  <verify>Run strict commands locally and validate workflow step configuration uses non-blocking semantics while preserving hard failure behavior for required checks.</verify>
  <done>Strict failures surface as visible warnings without failing the required CI status.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Emit failure diagnostics artifacts and PR comment feedback</name>
  <files>.github/workflows/ci.yml</files>
  <action>Upload diagnostic artifacts only on failure and publish PR-facing failure context via workflow summary and PR comment when in pull-request context.</action>
  <verify>Review workflow `if: failure()` and PR-context guards to confirm diagnostics run on failure paths only and avoid actor-gating assumptions.</verify>
  <done>CI failures produce actionable operator feedback (summary + logs + PR comment) aligned with context decisions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CI workflow triggers PR-main and all push events.
- [ ] Required checks run `validate:data`, `typecheck`, and `build`.
- [ ] Strict checks are visible but non-blocking.
- [ ] Failure diagnostics upload only on failure and include PR comment path.
</verification>

<success_criteria>
- All tasks completed.
- DEP-01 and DEP-02 CI-check expectations are implemented with clear diagnostics behavior.
- Workflow output is suitable for downstream deployment promotion in 04-02.
</success_criteria>

<output>
After completion, create .planning/phases/04-ci-cd-github-pages-delivery/04-01-SUMMARY.md
</output>
