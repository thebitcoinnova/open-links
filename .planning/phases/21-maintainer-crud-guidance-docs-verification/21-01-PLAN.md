---
phase: 21-maintainer-crud-guidance-docs-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/data-model.md
  - docs/openclaw-update-crud.md
  - docs/ai-guided-customization.md
  - docs/customization-catalog.md
  - docs/studio-self-serve.md
files-modified:
  - docs/data-model.md
  - docs/openclaw-update-crud.md
  - docs/ai-guided-customization.md
  - docs/customization-catalog.md
  - docs/studio-self-serve.md
autonomous: true
requirements:
  - MAINT-01
user_setup: []
objective: Make referral authoring explicit in the canonical contract and maintainer workflow docs so AI CRUD is the clear default path, Studio is an honest browser fallback, and direct JSON stays the manual escape hatch
must_haves:
  truths:
    - `docs/data-model.md` becomes the canonical `links[].referral` reference with a small curated example set.
    - Authoring docs explain referral precedence in two layers: short top-level rule plus deeper concise explanation.
    - Repo-native AI CRUD remains the primary recommendation, Studio is documented as an Advanced JSON path for referrals today, and direct JSON remains the fallback.
    - Maintainers can understand referral authoring without being told they need a bespoke extractor by default.
    - Generated referral fields are introduced lightly for authors while deeper debug detail is left to verification-oriented docs.
  artifacts:
    - path: docs/data-model.md
      provides: canonical referral contract, examples, precedence, and light generated-field explanation
      min_lines: 1200
    - path: docs/openclaw-update-crud.md
      provides: referral-aware day-2 CRUD workflow guidance and routing into the canonical contract docs
      min_lines: 260
    - path: docs/ai-guided-customization.md
      provides: referral-aware AI-maintainer workflow guidance aligned with the same path hierarchy
      min_lines: 320
    - path: docs/customization-catalog.md
      provides: referral field inventory notes aligned with the canonical contract
      min_lines: 360
    - path: docs/studio-self-serve.md
      provides: honest Studio positioning for current Advanced JSON-based referral editing
      min_lines: 220
  key_links:
    - from: docs/data-model.md
      to: docs/openclaw-update-crud.md
      via: canonical referral contract reference used by the primary maintainer CRUD path
      pattern: "referral"
    - from: docs/ai-guided-customization.md
      to: docs/studio-self-serve.md
      via: explicit path hierarchy between repo-native AI CRUD and browser-based Advanced JSON fallback
      pattern: "Studio"
    - from: docs/customization-catalog.md
      to: docs/data-model.md
      via: concise field inventory routing back to the canonical referral contract
      pattern: "referral"
---

<objective>
Document referral authoring where maintainers already look for contract and workflow guidance.

Purpose: make `links[].referral` understandable and usable through the preferred repo-native CRUD paths without forcing maintainers into raw JSON by default.
Output: canonical referral contract docs plus referral-aware CRUD/customization/studio guidance.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-maintainer-crud-guidance-docs-verification/21-CONTEXT.md
@.planning/phases/21-maintainer-crud-guidance-docs-verification/21-RESEARCH.md
@.planning/phases/20-referral-card-presentation-transparency-ux/20-VERIFICATION.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add the canonical referral contract section to the data-model docs</name>
  <files>docs/data-model.md</files>
  <action>Add a dedicated `links[].referral` section to the canonical data-model doc. Include the approved field vocabulary, a small curated example set (minimal marker, richer two-sided example with `termsUrl`, and a supported-family `non_profile` example), the short manual-over-generated rule, and a concise deeper explanation of precedence and generated-field visibility.</action>
  <verify>Review the documented fields against `schema/links.schema.json` and the runtime referral helpers, then run `bun run biome:check`.</verify>
  <done>The repo has one clear canonical referral contract/reference layer instead of scattered implicit guidance.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Make the repo-native AI CRUD docs referral-aware</name>
  <files>docs/openclaw-update-crud.md, docs/ai-guided-customization.md, docs/data-model.md</files>
  <action>Update the OpenClaw day-2 CRUD contract and the AI-guided customization wizard so referral authoring is explicitly supported through the recommended repo-native flow. Keep the docs path-hierarchical: point to `docs/data-model.md` for canonical field meaning, clarify that bespoke extractors are not the default answer, and keep direct JSON as the lower-level fallback rather than the first recommendation.</action>
  <verify>Review cross-links and maintainer-path wording for consistency, then run `bun run biome:check`.</verify>
  <done>Maintainers can follow the existing AI CRUD workflows to add or update referral links without guessing where referral guidance lives.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Align the customization inventory and Studio docs with the current referral authoring reality</name>
  <files>docs/customization-catalog.md, docs/studio-self-serve.md, docs/data-model.md</files>
  <action>Add the referral surface to the customization inventory and make Studio’s current referral status explicit: browser-based CRUD is valid when it fits, but referral editing currently relies on Advanced JSON instead of first-class referral controls. Keep this honest and avoid implying shipped Studio capabilities that do not exist.</action>
  <verify>Run `bun run biome:check` and review the final doc set for one consistent path hierarchy: AI CRUD first, Studio second, direct JSON fallback last.</verify>
  <done>The maintainer workflow docs and inventory references all describe the same current referral authoring reality.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docs/data-model.md` contains the canonical referral contract and curated examples.
- [ ] Authoring docs use the same path hierarchy: AI CRUD first, Studio Advanced JSON fallback, manual JSON fallback last.
- [ ] Precedence is documented in both short and deeper forms without conflicting wording.
- [ ] The docs do not imply bespoke extractor work or first-class Studio referral controls are required.
</verification>

<success_criteria>
- All tasks completed.
- MAINT-01 is materially advanced through explicit referral authoring guidance on the repo’s preferred CRUD paths.
- Phase 21 can move into discoverability, verification, and downstream notes on top of one canonical referral contract story.
</success_criteria>

<output>
After completion, create .planning/phases/21-maintainer-crud-guidance-docs-verification/21-01-SUMMARY.md
</output>
