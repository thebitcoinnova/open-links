---
phase: 21-maintainer-crud-guidance-docs-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - README.md
  - docs/quickstart.md
  - docs/social-card-verification.md
  - docs/downstream-open-links-sites.md
files-modified:
  - README.md
  - docs/quickstart.md
  - docs/social-card-verification.md
  - docs/downstream-open-links-sites.md
autonomous: true
requirements:
  - MAINT-02
user_setup: []
objective: Make referral support discoverable from top-level maintainer docs, document script-backed verification and warning interpretation, and add explicit downstream breadcrumbs for the additive referral contract
must_haves:
  truths:
    - README and quickstart surface referral support briefly and route maintainers to the right deeper docs instead of burying the feature.
    - Verification guidance gives maintainers a script-backed referral checklist and clearly states that extracted referral data is assistive, not authoritative.
    - The docs call out the main referral warning classes maintainers may encounter.
    - Downstream notes explicitly distinguish UI-only referral changes from shared contract/schema changes.
    - `open-links-sites` guidance frames `links[].referral` as additive but still review-worthy when syncing upstream changes.
  artifacts:
    - path: README.md
      provides: top-level referral discoverability, maintainer-path routing, and downstream breadcrumbs
      min_lines: 520
    - path: docs/quickstart.md
      provides: referral-aware maintainer routing from the main setup/update entrypoint
      min_lines: 560
    - path: docs/social-card-verification.md
      provides: referral-specific script-backed verification checklist plus warning/limits messaging
      min_lines: 220
    - path: docs/downstream-open-links-sites.md
      provides: referral-specific additive schema guidance and UI-vs-contract distinction for downstream maintainers
      min_lines: 80
  key_links:
    - from: README.md
      to: docs/data-model.md
      via: referral feature discovery routing into the canonical contract docs
      pattern: "referral"
    - from: docs/quickstart.md
      to: docs/social-card-verification.md
      via: referral-aware maintainer flow handing off into script-backed verification
      pattern: "verification"
    - from: docs/data-model.md
      to: docs/downstream-open-links-sites.md
      via: additive contract notes plus downstream review breadcrumbs
      pattern: "open-links-sites"
---

<objective>
Close the milestone by making referral support discoverable, verifiable, and downstream-aware.

Purpose: make maintainers see the new referral capability from the top-level docs, understand how to verify it, and know when downstream consumers need to care.
Output: README/quickstart discoverability updates, referral verification guidance, and explicit downstream compatibility notes.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-maintainer-crud-guidance-docs-verification/21-CONTEXT.md
@.planning/phases/21-maintainer-crud-guidance-docs-verification/21-RESEARCH.md
@.planning/phases/21-maintainer-crud-guidance-docs-verification/21-01-PLAN.md
@.planning/phases/20-referral-card-presentation-transparency-ux/20-VERIFICATION.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Surface referral support in README and quickstart without turning them into schema docs</name>
  <files>README.md, docs/quickstart.md</files>
  <action>Add short, high-signal referral-aware maintainer guidance to README and quickstart. Make the new feature discoverable from the main CRUD entrypoints, preserve the same path hierarchy approved in context, and route deeper field details to `docs/data-model.md` rather than duplicating the full referral contract inline.</action>
  <verify>Review the updated entry points for fast scan-ability and link correctness, then run `bun run biome:check`.</verify>
  <done>Maintainers can discover referral support quickly from the first docs they read and land on the right deeper guidance.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add referral-specific verification and warning interpretation guidance</name>
  <files>docs/social-card-verification.md, README.md, docs/quickstart.md</files>
  <action>Extend the shared verification guide with a referral-focused checklist and script-backed command path. Explain that extracted referral data is assistive rather than authoritative, and call out the main warning classes maintainers may see so `validate:data`, enrichment, and build output are interpretable instead of surprising.</action>
  <verify>Run `bun run biome:check` and review the commands/warning terminology against the current validation and enrichment outputs.</verify>
  <done>Maintainers know what to run and how to read the important referral warnings after adding or editing a referral link.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add explicit downstream referral breadcrumbs and finish with a docs-drift pass</name>
  <files>docs/downstream-open-links-sites.md, README.md, docs/quickstart.md, docs/social-card-verification.md</files>
  <action>Update the downstream synopsis so referral changes are discussed explicitly as additive schema work with lighter-risk UI-only changes distinguished clearly. Add or refine breadcrumbs in the maintainer-facing docs so downstream review on sync is visible where maintainers are likely to read, then finish with a docs-drift pass across the updated files.</action>
  <verify>Run `bun run biome:check` and review the final diff for one consistent downstream message: additive contract, review-worthy on sync, UI-only changes lower risk than schema/policy/script changes.</verify>
  <done>Phase 21 closes with maintainer docs that are discoverable, script-backed, and downstream-aware rather than leaving referral support implicit.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] README and quickstart surface referral support and route to the deeper contract docs.
- [ ] Verification guidance is script-backed and clearly says extraction is assistive, not authoritative.
- [ ] Main referral warning classes are called out in maintainer docs.
- [ ] Downstream notes distinguish UI-only changes from shared contract/schema changes and treat `links[].referral` as additive but review-worthy.
</verification>

<success_criteria>
- All tasks completed.
- MAINT-02 is materially advanced through discoverable maintainer docs, explicit verification guidance, and downstream compatibility notes.
- The milestone closes with documentation that makes referral support maintainable day 2 instead of only implemented at runtime.
</success_criteria>

<output>
After completion, create .planning/phases/21-maintainer-crud-guidance-docs-verification/21-02-SUMMARY.md
</output>
