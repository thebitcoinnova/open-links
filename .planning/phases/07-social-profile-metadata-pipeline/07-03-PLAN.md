---
phase: 07-social-profile-metadata-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - scripts/sync-content-images.ts
  - src/lib/content/load-content.ts
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/social-profile-metadata.ts
files-modified:
  - scripts/sync-content-images.ts
  - src/lib/content/load-content.ts
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/social-profile-metadata.ts
autonomous: true
requirements:
  - DATA-07
  - DATA-09
user_setup: []
objective: Materialize and expose the new social profile metadata at runtime without performing the Phase 8 card redesign yet
must_haves:
  truths:
    - Manual and generated profile-avatar URLs are materialized/localized independently from preview images.
    - Runtime content loading preserves manual override semantics while safely omitting unresolved profile-avatar assets.
    - Card-view-model helpers expose handle, avatar, preview image, and audience counts as separate concepts for later Phase 8 rendering work.
    - Existing cards continue to render without regressions when new profile metadata is absent.
    - The image-sync pipeline remains deterministic for both preview imagery and profile avatars.
  artifacts:
    - path: scripts/sync-content-images.ts
      provides: content-image materialization support for separate profile-avatar candidates
      min_lines: 260
    - path: src/lib/content/load-content.ts
      provides: runtime merge + localization for profile-avatar and audience-count metadata
      min_lines: 220
    - path: src/lib/ui/social-profile-metadata.ts
      provides: normalized profile metadata helper for card-facing consumption
      min_lines: 60
    - path: src/lib/ui/rich-card-policy.ts
      provides: rich-card view model access to the new profile metadata without final UI redesign
      min_lines: 120
  key_links:
    - from: scripts/sync-content-images.ts
      to: src/lib/content/load-content.ts
      via: baked local asset paths for profile-avatar candidates
      pattern: "profileImage"
    - from: src/lib/content/load-content.ts
      to: src/lib/ui/social-profile-metadata.ts
      via: merged runtime metadata consumed by card-facing helpers
      pattern: "metadata"
    - from: src/lib/ui/social-profile-metadata.ts
      to: src/lib/ui/rich-card-policy.ts
      via: card-view-model access to social profile metadata
      pattern: "resolve"
---

<objective>
Connect Phase 7 metadata to runtime loading and card-facing helpers.

Purpose: Ensure the new profile-avatar and audience-count fields survive asset sync and are available to Phase 8 UI work without forcing visual changes in this phase.
Output: Runtime localization, normalized social-profile helper(s), and card-view-model plumbing.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-social-profile-metadata-pipeline/07-CONTEXT.md
@.planning/phases/07-social-profile-metadata-pipeline/07-RESEARCH.md
@.planning/phases/07-social-profile-metadata-pipeline/07-01-PLAN.md
@.planning/phases/07-social-profile-metadata-pipeline/07-02-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend image materialization and runtime localization for profile avatars</name>
  <files>scripts/sync-content-images.ts, src/lib/content/load-content.ts</files>
  <action>Teach the content-image sync and runtime localization flow to track a dedicated profile-avatar field in addition to preview imagery. Keep the behavior deterministic: bake manual remote avatars when possible, preserve local authenticated-cache assets, and omit unresolved avatar paths safely instead of rendering raw remotes unexpectedly.</action>
  <verify>Run `bun run images:sync`, `bun run validate:data`, and `bun run build` to confirm profile-avatar assets localize correctly and missing avatars degrade safely.</verify>
  <done>Profile-avatar fields participate in the same deterministic asset workflow as preview imagery without breaking existing cards.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add a normalized social-profile metadata helper for card-facing consumers</name>
  <files>src/lib/ui/social-profile-metadata.ts, src/lib/content/load-content.ts</files>
  <action>Create a shared helper that normalizes the new profile metadata into card-facing concepts such as handle, avatar, preview image, and audience metrics. Keep the helper presentation-neutral so Phase 8 can use it for both simple and rich cards.</action>
  <verify>Add focused automated coverage for the helper or equivalent runtime assertions, then run `bun test` and `bun run typecheck` to confirm the normalized shape is stable across manual-only, generated-only, and mixed metadata inputs.</verify>
  <done>Phase 8 has a stable presentation-neutral metadata layer for social profile cards.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Surface the new metadata through existing rich-card policy without visual redesign</name>
  <files>src/lib/ui/rich-card-policy.ts, src/lib/ui/social-profile-metadata.ts</files>
  <action>Expose the normalized social-profile data through the existing rich-card policy/view-model layer so later UI work can consume it immediately. Do not redesign markup in this phase; limit changes to non-visual plumbing and backward-compatible view-model expansion.</action>
  <verify>Run `bun run typecheck` and `bun run build`, then confirm current card rendering still succeeds when the new metadata is absent and that the richer view model is available for Phase 8 consumers.</verify>
  <done>Runtime card plumbing knows about Phase 7 metadata without prematurely changing the visual card presentation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `images:sync` and runtime loading both understand a separate profile-avatar field.
- [ ] Manual override and omission behavior remain deterministic after runtime merge/localization.
- [ ] Card-facing helpers expose the new metadata without forcing Phase 8 UI changes early.
- [ ] Build and validation still pass when links do not use any of the new Phase 7 fields.
</verification>

<success_criteria>
- All tasks completed.
- DATA-07 and DATA-09 are fully wired from stored metadata into runtime consumption paths.
- Phase 8 can focus on presentation because the metadata pipeline is already runtime-ready.
</success_criteria>

<output>
After completion, create .planning/phases/07-social-profile-metadata-pipeline/07-03-SUMMARY.md
</output>
