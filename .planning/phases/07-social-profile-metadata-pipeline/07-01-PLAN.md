---
phase: 07-social-profile-metadata-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/links.schema.json
  - schema/rich-authenticated-cache.schema.json
  - src/lib/content/load-content.ts
  - scripts/enrichment/types.ts
  - scripts/authenticated-extractors/types.ts
  - scripts/enrich-rich-links.ts
  - scripts/validate-data.ts
files-modified:
  - schema/links.schema.json
  - schema/rich-authenticated-cache.schema.json
  - src/lib/content/load-content.ts
  - scripts/enrichment/types.ts
  - scripts/authenticated-extractors/types.ts
  - scripts/enrich-rich-links.ts
  - scripts/validate-data.ts
autonomous: true
requirements:
  - DATA-07
  - DATA-09
user_setup: []
objective: Define the Phase 7 metadata contract, field-level manual override semantics, and warning policy for profile avatars and audience counts
must_haves:
  truths:
    - Link metadata supports a distinct profile-avatar field alongside the existing preview image field.
    - First-pass profile metrics can store explicit platform-native counts with numeric and original-text companion values where needed.
    - Manual metadata remains the source of truth for overlapping profile-specific fields when generated or authenticated metadata exists.
    - Supported-profile metric gaps produce strong warnings without turning Phase 7 into a blocking failure path.
    - Existing non-profile and non-social links remain valid without any new required metadata.
  artifacts:
    - path: schema/links.schema.json
      provides: manual link metadata contract for profile avatars and audience-count fields
      min_lines: 190
    - path: schema/rich-authenticated-cache.schema.json
      provides: authenticated cache schema for avatar plus audience-stat payloads
      min_lines: 90
    - path: scripts/enrichment/types.ts
      provides: generated-metadata and report typing for profile-specific fields
      min_lines: 45
    - path: scripts/authenticated-extractors/types.ts
      provides: shared extractor/cache TypeScript contract for profile-specific metadata
      min_lines: 120
    - path: scripts/validate-data.ts
      provides: warning-level validation surface for expected missing stats and partial profile metadata
      min_lines: 180
  key_links:
    - from: schema/links.schema.json
      to: src/lib/content/load-content.ts
      via: RichLinkMetadata structural parity for manual + generated profile fields
      pattern: "metadata"
    - from: scripts/enrichment/types.ts
      to: scripts/enrich-rich-links.ts
      via: generated metadata merge contract for profile-specific fields
      pattern: "EnrichmentMetadata"
    - from: scripts/authenticated-extractors/types.ts
      to: schema/rich-authenticated-cache.schema.json
      via: authenticated cache metadata shape alignment
      pattern: "AuthenticatedCacheMetadata"
    - from: scripts/validate-data.ts
      to: schema/links.schema.json
      via: warning policy and applicability checks for supported profile links
      pattern: "links.schema"
---

<objective>
Establish the Phase 7 metadata and validation foundation.

Purpose: Make profile avatars and audience counts first-class metadata without breaking current rich-link behavior or manual authoring workflows.
Output: Schema/type updates, field-level merge precedence, and explicit warning rules for partial expected stats.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-social-profile-metadata-pipeline/07-CONTEXT.md
@.planning/phases/07-social-profile-metadata-pipeline/07-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend manual and generated metadata contracts for social profile fields</name>
  <files>schema/links.schema.json, schema/rich-authenticated-cache.schema.json, src/lib/content/load-content.ts, scripts/enrichment/types.ts, scripts/authenticated-extractors/types.ts</files>
  <action>Add the Phase 7 profile-specific contract: a dedicated profile-avatar field, explicit first-pass audience-count fields, and clearly paired parsed/original-text metadata values where extractor parsing may be uncertain. Keep all new fields optional and compatible with existing non-profile links.</action>
  <verify>Run `bun run typecheck` and `bun run validate:data` to confirm starter/manual data remains valid while the new fields are accepted by schema and runtime types.</verify>
  <done>The repo has one coherent contract for manual links, generated rich metadata, and authenticated cache entries that can express Phase 7 profile data.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement field-level manual override precedence for profile-specific metadata</name>
  <files>scripts/enrich-rich-links.ts, src/lib/content/load-content.ts, scripts/enrichment/types.ts</files>
  <action>Replace whole-object merge behavior with explicit precedence rules so manual profile fields win over generated/authenticated values while still allowing generated metadata to fill missing fields. Keep the merge policy deterministic and narrow to the profile-specific contract established in this phase.</action>
  <verify>Add focused automated coverage for merge precedence or equivalent helper behavior, then run `bun test` and `bun run typecheck` to confirm the override policy is enforced and typed.</verify>
  <done>Manual counts and manual profile images remain stable after enrichment, while generated metadata still fills gaps predictably.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add supported-profile warning rules for partial expected stats</name>
  <files>scripts/validate-data.ts, scripts/enrich-rich-links.ts, schema/links.schema.json</files>
  <action>Teach validation and enrichment policy to recognize supported Instagram/YouTube profile URLs, warn strongly when expected audience stats are missing, and keep unsupported or non-profile URLs silent/omitted. Do not turn these new warnings into blocking errors.</action>
  <verify>Run `bun run validate:data` against representative supported-profile fixtures and confirm warnings are explicit, non-blocking, and absent for non-profile URLs.</verify>
  <done>Phase 7 warning semantics are established before extractor-specific implementation begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Manual and authenticated metadata contracts support profile avatar plus audience-count fields.
- [ ] Manual profile-specific values override generated/authenticated values deterministically.
- [ ] Supported-profile missing-stat conditions emit strong warnings without failing validation/build.
- [ ] Existing links without new fields remain valid and loadable.
</verification>

<success_criteria>
- All tasks completed.
- DATA-07 and DATA-09 are established as schema/runtime contracts before extractor work begins.
- Phase 7 has a stable metadata foundation for Instagram and YouTube capture work.
</success_criteria>

<output>
After completion, create .planning/phases/07-social-profile-metadata-pipeline/07-01-SUMMARY.md
</output>
