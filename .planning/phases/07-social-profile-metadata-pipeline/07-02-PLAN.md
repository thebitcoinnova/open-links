---
phase: 07-social-profile-metadata-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - scripts/enrich-rich-links.ts
  - scripts/enrichment/report.ts
  - scripts/authenticated-extractors/cache.ts
  - scripts/authenticated-extractors/plugins/instagram-auth-browser.ts
  - scripts/authenticated-extractors/plugins/youtube-auth-browser.ts
  - data/cache/rich-authenticated-cache.json
  - public/cache/rich-authenticated/*
files-modified:
  - scripts/enrich-rich-links.ts
  - scripts/enrichment/report.ts
  - scripts/authenticated-extractors/cache.ts
  - scripts/authenticated-extractors/plugins/instagram-auth-browser.ts
  - scripts/authenticated-extractors/plugins/youtube-auth-browser.ts
  - data/cache/rich-authenticated-cache.json
  - public/cache/rich-authenticated/*
autonomous: true
requirements:
  - DATA-08
  - DATA-09
user_setup: []
objective: Capture and persist Instagram and YouTube profile avatars plus audience stats through the authenticated and generated metadata pipelines
must_haves:
  truths:
    - Only clear Instagram and YouTube profile/account URLs attempt audience-stat capture.
    - Instagram extraction captures follower and following counts plus a true profile avatar when available.
    - YouTube extraction captures subscriber count plus a true channel avatar when available.
    - When parsing is uncertain, numeric values and original raw text are both preserved.
    - If only generic preview imagery is available, the profile-avatar field stays empty and the run emits a strong warning.
    - Enrichment/authenticated-cache reporting explicitly surfaces expected missing stats without failing the build.
  artifacts:
    - path: scripts/authenticated-extractors/plugins/instagram-auth-browser.ts
      provides: Instagram-specific stat and avatar extraction for profile URLs
      min_lines: 220
    - path: scripts/authenticated-extractors/plugins/youtube-auth-browser.ts
      provides: YouTube-specific subscriber and avatar extraction for channel/profile URLs
      min_lines: 260
    - path: scripts/authenticated-extractors/cache.ts
      provides: authenticated cache validation + enrichment conversion for profile-specific fields
      min_lines: 220
    - path: scripts/enrich-rich-links.ts
      provides: generated metadata persistence and warning propagation for Phase 7 fields
      min_lines: 220
    - path: scripts/enrichment/report.ts
      provides: report payload support for expected missing-stat warning detail
      min_lines: 120
  key_links:
    - from: scripts/authenticated-extractors/plugins/instagram-auth-browser.ts
      to: scripts/authenticated-extractors/cache.ts
      via: persisted authenticated cache metadata for Instagram profile stats
      pattern: "metadata"
    - from: scripts/authenticated-extractors/plugins/youtube-auth-browser.ts
      to: scripts/authenticated-extractors/cache.ts
      via: persisted authenticated cache metadata for YouTube profile stats
      pattern: "metadata"
    - from: scripts/authenticated-extractors/cache.ts
      to: scripts/enrich-rich-links.ts
      via: authenticated cache conversion into generated enrichment metadata
      pattern: "validateAuthenticatedCacheEntry"
    - from: scripts/enrich-rich-links.ts
      to: scripts/enrichment/report.ts
      via: explicit missing-stat warning details in enrichment reports
      pattern: "writeEnrichmentReport"
---

<objective>
Implement first-pass Instagram and YouTube profile-data capture.

Purpose: Persist avatar and audience-count metadata for the two supported first-pass platforms while keeping warnings explicit and non-blocking.
Output: Extractor updates, authenticated-cache persistence, and enrichment reporting for profile-specific fields.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-social-profile-metadata-pipeline/07-CONTEXT.md
@.planning/phases/07-social-profile-metadata-pipeline/07-RESEARCH.md
@.planning/phases/07-social-profile-metadata-pipeline/07-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend Instagram extractor for structured audience stats and avatar capture</name>
  <files>scripts/authenticated-extractors/plugins/instagram-auth-browser.ts, scripts/enrich-rich-links.ts</files>
  <action>Build Instagram-specific parsing for follower/following counts and a true profile avatar, using only clear profile URLs. Preserve parsed numeric values plus original raw text when needed, and emit warnings when expected Instagram profile stats or avatar data are unavailable.</action>
  <verify>Add focused extractor/parsing coverage or equivalent fixture checks, then run the relevant tests plus `bun run enrich:rich:strict` to confirm Instagram metadata persists with the Phase 7 contract.</verify>
  <done>Instagram profile links can persist structured audience metadata and a canonical avatar instead of only a generic description string.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Extend YouTube extractor for subscriber stats and channel-avatar capture</name>
  <files>scripts/authenticated-extractors/plugins/youtube-auth-browser.ts, scripts/enrich-rich-links.ts</files>
  <action>Implement YouTube-specific profile/channel parsing that captures subscriber count and a true channel avatar for supported profile URLs only. Keep non-profile URLs out of scope, preserve raw text when numeric parsing is uncertain, and warn strongly when expected channel stats are missing.</action>
  <verify>Add focused parser/extractor coverage or equivalent fixture checks, then run the relevant tests plus `bun run enrich:rich:strict` to confirm YouTube metadata persists with the Phase 7 contract.</verify>
  <done>YouTube profile links save structured subscriber metadata and channel-avatar data rather than generic page-level fields only.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Propagate profile-specific fields through cache validation and enrichment reporting</name>
  <files>scripts/authenticated-extractors/cache.ts, scripts/enrichment/report.ts, scripts/enrich-rich-links.ts, data/cache/rich-authenticated-cache.json, public/cache/rich-authenticated/*</files>
  <action>Update authenticated cache conversion, validation, and enrichment reporting so profile-specific fields are persisted, warning details are explicit, and representative Instagram/YouTube cache fixtures/assets remain coherent with the new schema.</action>
  <verify>Run `bun run validate:data`, `bun run enrich:rich:strict`, and any focused tests to confirm cache entries validate, warnings surface clearly, and the generated metadata/report files include the new profile fields.</verify>
  <done>Phase 7 extractor output is fully connected from plugin capture through cache validation to generated metadata and warning reports.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Instagram and YouTube profile URLs persist the intended first-pass audience metrics.
- [ ] Profile-avatar capture is separate from preview image handling, with warning fallback when avatar capture is unavailable.
- [ ] Authenticated cache validation and enrichment report payloads understand the new fields.
- [ ] Missing expected stats surface as strong warnings rather than blocking failures.
</verification>

<success_criteria>
- All tasks completed.
- DATA-08 is satisfied for the first-pass platforms and DATA-09 remains intact through reporting/merge behavior.
- Phase 7 has real persisted Instagram and YouTube profile metadata ready for runtime consumption.
</success_criteria>

<output>
After completion, create .planning/phases/07-social-profile-metadata-pipeline/07-02-SUMMARY.md
</output>
