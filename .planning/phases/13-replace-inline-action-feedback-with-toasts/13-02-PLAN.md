---
phase: 13-replace-inline-action-feedback-with-toasts
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/lib/share/copy-to-clipboard.ts
  - src/lib/share/copy-to-clipboard.test.ts
  - src/lib/share/share-link.ts
  - src/lib/share/share-link.test.ts
  - src/components/cards/PaymentLinkCard.tsx
  - src/components/cards/PaymentLinkCard.test.tsx
  - src/styles/base.css
  - src/styles/responsive.css
files-modified:
  - src/lib/share/copy-to-clipboard.ts
  - src/lib/share/copy-to-clipboard.test.ts
  - src/lib/share/share-link.ts
  - src/lib/share/share-link.test.ts
  - src/components/cards/PaymentLinkCard.tsx
  - src/components/cards/PaymentLinkCard.test.tsx
  - src/styles/base.css
  - src/styles/responsive.css
autonomous: true
requirements: []
user_setup: []
objective: Normalize the remaining public-site copy feedback onto the same toast and clipboard path, then finish the toast styling and cleanup work
must_haves:
  truths:
    - Share fallback and payment-rail copy use one shared clipboard helper instead of maintaining different browser fallback behavior.
    - Payment-rail copy feedback becomes toast-based rather than toggling the button label between `Copy` and `Copied`.
    - Obsolete inline-status CSS is removed once no migrated surface depends on it, while persistent utility-pill live regions remain untouched.
    - Toast styling fits the current public-site chrome and mobile layout without reopening the broader dialog-library question.
  artifacts:
    - path: src/lib/share/copy-to-clipboard.ts
      provides: reusable clipboard write logic with the textarea fallback path needed by both share and payment copy
      min_lines: 70
    - path: src/lib/share/copy-to-clipboard.test.ts
      provides: focused coverage for clipboard success, navigator failure fallback, and hard failure cases
      min_lines: 110
    - path: src/lib/share/share-link.ts
      provides: share fallback updated to reuse the new shared clipboard helper
      min_lines: 90
    - path: src/components/cards/PaymentLinkCard.tsx
      provides: payment-rail copy feedback routed through shared copy + toast helpers without local copied-label timer drift
      min_lines: 360
    - path: src/components/cards/PaymentLinkCard.test.tsx
      provides: focused regression coverage for payment copy semantics and toast-triggering behavior
      min_lines: 140
    - path: src/styles/base.css
      provides: OpenLinks-specific toast theming plus removal of obsolete inline-status selectors
      min_lines: 980
    - path: src/styles/responsive.css
      provides: mobile-safe toast placement/spacing adjustments where needed
      min_lines: 110
  key_links:
    - from: src/lib/share/copy-to-clipboard.ts
      to: src/lib/share/share-link.ts
      via: shared clipboard fallback used by URL-copy share fallback
      pattern: "copy"
    - from: src/lib/share/copy-to-clipboard.ts
      to: src/components/cards/PaymentLinkCard.tsx
      via: shared clipboard helper for payment-rail copy actions
      pattern: "copy"
    - from: src/components/cards/PaymentLinkCard.tsx
      to: src/lib/ui/action-toast.ts
      via: shared toast feedback for payment copy outcomes
      pattern: "toast"
---

<objective>
Finish Phase 13 by normalizing the last copy surface and cleaning up the temporary inline-feedback leftovers.

Purpose: make share and copy feedback consistent across the public site instead of leaving payment rails on their own button-label timer path.
Output: one shared clipboard helper, payment-copy toasts, toast theming, and focused regression coverage for the remaining copy flow.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-replace-inline-action-feedback-with-toasts/13-RESEARCH.md
@.planning/phases/13-replace-inline-action-feedback-with-toasts/13-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extract one shared clipboard helper and reuse it in `share-link.ts`</name>
  <files>src/lib/share/copy-to-clipboard.ts, src/lib/share/copy-to-clipboard.test.ts, src/lib/share/share-link.ts, src/lib/share/share-link.test.ts</files>
  <action>Move the current clipboard write and textarea fallback logic out of `share-link.ts` into a reusable helper under `src/lib/share/`. Update `share-link.ts` to use that helper so share fallback and payment copy can rely on one browser-compatibility path.</action>
  <verify>Run `bun test src/lib/share/copy-to-clipboard.test.ts src/lib/share/share-link.test.ts` and `bun run typecheck`.</verify>
  <done>The repo has one clipboard write path for both share fallback and direct copy actions.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Migrate payment-rail copy feedback from button-label timers to shared toasts</name>
  <files>src/components/cards/PaymentLinkCard.tsx, src/components/cards/PaymentLinkCard.test.tsx</files>
  <action>Update `PaymentLinkCard` so payment-rail copy actions use the shared clipboard helper and the toast helper instead of the current `Copied` label timer. Keep stable button text and accessible labels, emit success/failure toasts, and preserve the existing QR/fullscreen behavior and action layout.</action>
  <verify>Run `bun test src/components/cards/PaymentLinkCard.test.tsx` and `bun run typecheck`.</verify>
  <done>Payment-rail copy joins the same transient-feedback model as the share surfaces without changing the rest of the payment-card interaction model.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Finish toast theming, remove obsolete inline-status styling, and verify the public-site result</name>
  <files>src/styles/base.css, src/styles/responsive.css, src/components/cards/PaymentLinkCard.test.tsx, src/lib/share/share-link.test.ts</files>
  <action>Add a minimal OpenLinks-specific toast class layer so the new notifications fit the current visual system and mobile spacing. Remove now-unused `.profile-share-status` and `.non-payment-card-action-status` selectors, leave utility pills untouched, and add the final regression checks needed for profile share, card share, and payment copy outcomes.</action>
  <verify>Run `bun test src/lib/share/copy-to-clipboard.test.ts src/lib/share/share-link.test.ts src/components/cards/PaymentLinkCard.test.tsx src/components/profile/ProfileHeader.test.tsx src/components/cards/non-payment-card-accessibility.test.tsx`, `bun run biome:check`, `bun run typecheck`, and `bun run build`. Then inspect a built preview on desktop and mobile to confirm the toast placement does not collide with the top utility bar or card chrome.</verify>
  <done>The public site has consistent toast feedback across share and copy surfaces, and the old inline-status styling is fully retired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Share fallback and payment copy now reuse the same clipboard helper.
- [ ] Payment-rail copy no longer swaps visible button text to `Copied`.
- [ ] Obsolete inline-status CSS is removed from the migrated surfaces.
- [ ] Toast placement and styling hold up on desktop and mobile built previews.
</verification>

<success_criteria>
- All tasks completed.
- The public site uses one consistent share/copy feedback model across profile, card, and payment-copy surfaces.
- Phase 13 closes without expanding into a modal-library migration or broader UI-system rewrite.
</success_criteria>

<output>
After completion, create .planning/phases/13-replace-inline-action-feedback-with-toasts/13-02-SUMMARY.md
</output>
