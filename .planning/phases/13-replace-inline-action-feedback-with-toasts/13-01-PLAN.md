---
phase: 13-replace-inline-action-feedback-with-toasts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/routes/index.tsx
  - src/lib/ui/action-toast.ts
  - src/lib/ui/action-toast.test.ts
  - src/components/profile/ProfileHeader.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
files-modified:
  - package.json
  - bun.lock
  - src/routes/index.tsx
  - src/lib/ui/action-toast.ts
  - src/lib/ui/action-toast.test.ts
  - src/components/profile/ProfileHeader.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
autonomous: true
requirements: []
user_setup: []
objective: Add one public-site toast host with `solid-sonner` and migrate profile/card share feedback away from component-local timers and inline status outputs
must_haves:
  truths:
    - The public site mounts exactly one global toast host and does not introduce a broader modal or component-library dependency.
    - `ProfileHeader` and `NonPaymentLinkCardShell` stop rendering transient inline share/action status outputs and instead route share results through a shared toast helper.
    - Native-share dismissals remain silent, while success and failure states produce consistent toast feedback.
    - Profile-header button ordering and card action-row semantics remain unchanged.
  artifacts:
    - path: package.json
      provides: the new `solid-sonner` dependency added to the root public-site app
      min_lines: 100
    - path: src/routes/index.tsx
      provides: a single public-site `Toaster` host aligned with the existing route shell and mode state
      min_lines: 500
    - path: src/lib/ui/action-toast.ts
      provides: reusable helpers that map share/copy result contracts into OpenLinks toast notifications
      min_lines: 70
    - path: src/lib/ui/action-toast.test.ts
      provides: focused coverage for dismissed/no-op, success, and failure toast mapping
      min_lines: 100
    - path: src/components/profile/ProfileHeader.tsx
      provides: profile share behavior without local timer state or inline status output
      min_lines: 120
    - path: src/components/cards/NonPaymentLinkCardShell.tsx
      provides: card action handling without local timer state or inline action-status output
      min_lines: 220
  key_links:
    - from: src/lib/ui/action-toast.ts
      to: src/components/profile/ProfileHeader.tsx
      via: shared share-result toasting for profile-level share actions
      pattern: "share"
    - from: src/lib/ui/action-toast.ts
      to: src/components/cards/NonPaymentLinkCardShell.tsx
      via: shared action-result toasting for card-level share actions
      pattern: "share"
    - from: src/routes/index.tsx
      to: src/lib/ui/action-toast.ts
      via: a single shell-level `Toaster` host for all public-site toast calls
      pattern: "Toaster"
---

<objective>
Lay the toast foundation for the public site before touching the remaining payment-copy surface.

Purpose: replace the current share/card inline feedback pattern with one reusable toast delivery path.
Output: one global toast host, one shared toast helper, and profile/card share surfaces migrated off local timer-driven status markup.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-replace-inline-action-feedback-with-toasts/13-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add `solid-sonner` and mount a single toast host in the public-site shell</name>
  <files>package.json, bun.lock, src/routes/index.tsx</files>
  <action>Add `solid-sonner` at the root package and mount one `Toaster` inside `RouteIndex` so the public site has a single toast host aligned with the current page shell and mode state. Keep the host placement and defaults minimal; do not introduce a broader primitive library or touch Studio packages.</action>
  <verify>Run `bun install` if needed, then `bun run typecheck` and `bun run build` to prove the new dependency and toast host compile cleanly.</verify>
  <done>The public site can emit toast notifications from any action surface without component-local providers.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Create a shared toast-mapping helper and migrate profile/card share feedback onto it</name>
  <files>src/lib/ui/action-toast.ts, src/lib/ui/action-toast.test.ts, src/components/profile/ProfileHeader.tsx, src/components/cards/NonPaymentLinkCardShell.tsx</files>
  <action>Create a small helper that translates `ShareLinkResult`-style outcomes into OpenLinks toast notifications, ignoring `dismissed` and preserving the current message semantics. Update `ProfileHeader` and `NonPaymentLinkCardShell` to call that helper instead of managing local status signals, timers, and inline `<output>` nodes.</action>
  <verify>Run `bun test src/lib/ui/action-toast.test.ts src/components/profile/ProfileHeader.test.tsx src/components/cards/non-payment-card-accessibility.test.tsx` and `bun run typecheck`.</verify>
  <done>Profile-level and card-level share feedback is toast-based and no longer depends on per-component timer state or inline status markup.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Lock down the toast foundation before the payment-copy follow-up</name>
  <files>src/lib/ui/action-toast.test.ts, src/components/profile/ProfileHeader.tsx, src/components/cards/NonPaymentLinkCardShell.tsx</files>
  <action>Add or update focused assertions that the migrated surfaces no longer render the old inline status nodes, while keeping button order, sibling action semantics, and silent dismiss behavior intact. Keep the verification centered on structural regressions and helper-level tests rather than brittle DOM-timer assertions.</action>
  <verify>Run `bun test src/lib/ui/action-toast.test.ts src/components/profile/ProfileHeader.test.tsx src/components/cards/non-payment-card-accessibility.test.tsx`, `bun run biome:check`, and `bun run build`.</verify>
  <done>The toast foundation is safe to build on, and Wave 2 can focus on the remaining copy surface instead of re-checking the initial share migration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The public site mounts exactly one global toast host.
- [ ] `ProfileHeader` no longer manages local share-status timer state or inline status markup.
- [ ] `NonPaymentLinkCardShell` no longer manages local action-status timer state or inline status markup.
- [ ] Dismissed native-share flows remain silent while success/failure states toast consistently.
</verification>

<success_criteria>
- All tasks completed.
- The public site has a stable toast foundation via `solid-sonner`.
- Profile and card share surfaces now use shared toast-based feedback without regressing Phase 12 action semantics.
</success_criteria>

<output>
After completion, create .planning/phases/13-replace-inline-action-feedback-with-toasts/13-01-SUMMARY.md
</output>
