---
phase: 22-source-authored-referral-flow-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/links.json
  - data/generated/rich-metadata.json
  - data/generated/rich-enrichment-report.json
  - src/lib/content/load-content.referral.test.ts
  - src/lib/ui/rich-card-description-sourcing.test.ts
files-modified:
  - data/links.json
  - data/generated/rich-metadata.json
  - data/generated/rich-enrichment-report.json
  - src/lib/content/load-content.referral.test.ts
  - src/lib/ui/rich-card-description-sourcing.test.ts
autonomous: true
requirements:
  - MAINT-01
  - MAINT-02
user_setup: []
objective: Close the milestone audit gap by proving the manual-first referral flow on real project data with one source-authored `links[].referral` entry, refreshed generated artifacts, and focused load/merge regression coverage
must_haves:
  truths:
    - At least one real link in `data/links.json` now carries a source-authored `links[].referral` object.
    - The chosen source-authored referral shape complements generated referral output instead of creating avoidable manual/generated drift.
    - `bun run enrich:rich:strict`, `bun run validate:data`, and `bun run build` prove the source-authored referral path end to end on real repo data.
    - A focused content-level regression proves runtime loading merges the source-authored referral object with generated referral output as intended.
    - The fix closes the milestone audit gap without reopening broader extraction, schema, or UI design scope.
  artifacts:
    - path: data/links.json
      provides: a real source-authored referral object on a live project link
      min_lines: 120
    - path: src/lib/content/load-content.referral.test.ts
      provides: direct runtime/load-content proof for manual-plus-generated referral merge on real dataset inputs
      min_lines: 120
    - path: data/generated/rich-metadata.json
      provides: refreshed generated artifact evidence after the source-authored referral addition
      min_lines: 120
    - path: data/generated/rich-enrichment-report.json
      provides: refreshed report evidence for the same live referral entry
      min_lines: 120
  key_links:
    - from: data/links.json
      to: data/generated/rich-metadata.json
      via: source-authored referral fields merging with generated referral output on the same live link
      pattern: "cluborange-referral"
    - from: src/lib/content/load-content.referral.test.ts
      to: src/lib/content/load-content.ts
      via: runtime merge/load proof for the real source-authored referral entry
      pattern: "loadContent"
    - from: data/generated/rich-enrichment-report.json
      to: .planning/v1.3-MILESTONE-AUDIT.md
      via: refreshed audit evidence that the manual-first source flow is now exercised
      pattern: "cluborange-referral"
---

<objective>
Prove the missing manual-first referral flow on real data and close the audit gap.

Purpose: move the referral milestone from “docs/tests imply this should work” to “the real repo data exercises this path end to end.”
Output: one source-authored referral entry, refreshed generated artifacts, and focused load/merge regression coverage.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/22-source-authored-referral-flow-proof/22-RESEARCH.md
@.planning/phases/21-maintainer-crud-guidance-docs-verification/21-VERIFICATION.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add a minimal source-authored referral object to the live Club Orange referral link</name>
  <files>data/links.json</files>
  <action>Update the real `cluborange-referral` entry in `data/links.json` so it carries a small but meaningful `links[].referral` object. Prefer stable manual fields that complement generated output, such as `kind`, a manual owner-side disclosure, and a canonical `termsUrl`, instead of copying generated promo copy into source data unnecessarily.</action>
  <verify>Run `bun run validate:data` and confirm the updated source-authored referral shape does not introduce manual/generated drift warnings.</verify>
  <done>The repo’s real source data now includes a maintainer-authored referral object instead of relying only on generated artifacts.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Refresh enrichment/build artifacts so the real source-authored referral path is exercised end to end</name>
  <files>data/generated/rich-metadata.json, data/generated/rich-enrichment-report.json, data/links.json</files>
  <action>Run the normal referral verification flow on the updated live dataset: `bun run enrich:rich:strict`, `bun run validate:data`, and `bun run build`. If the generated artifacts change, commit the refreshed `rich-metadata` and `rich-enrichment-report` outputs in the same batch so the source-authored proof is visible on disk.</action>
  <verify>Run `bun run enrich:rich:strict`, `bun run validate:data`, and `bun run build`.</verify>
  <done>The live repo proves source-authored referral data, generated blank-fill behavior, and final render/build success together.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add a focused runtime/load-content regression for the source-authored referral merge path</name>
  <files>src/lib/content/load-content.referral.test.ts, src/lib/ui/rich-card-description-sourcing.test.ts</files>
  <action>Add one targeted content-level regression that loads the real dataset and proves the source-authored `cluborange-referral.referral` fields survive while generated `offerSummary` / `termsSummary` still fill blanks as intended. If needed, add one small supporting assertion in an existing card/model dataset test, but keep the regression centered on real data loading rather than new fixture-only coverage.</action>
  <verify>Run `bun test src/lib/content/load-content.referral.test.ts src/lib/ui/rich-card-description-sourcing.test.ts` and `bun run typecheck`.</verify>
  <done>The audit gap cannot reopen without breaking a direct regression on the real source-authored referral flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] A real source-authored `links[].referral` object exists in `data/links.json`.
- [ ] The chosen manual fields avoid avoidable drift while still proving manual-first authoring.
- [ ] Enrichment, validation, and build all pass on the updated real dataset.
- [ ] A focused runtime/load-content regression proves manual-plus-generated merge on the live referral entry.
- [ ] The phase closes only the audit gap and does not sprawl into broader referral redesign work.
</verification>

<success_criteria>
- All tasks completed.
- The milestone audit’s only critical gap is materially closed.
- Phase 22 leaves the repo ready for `$gsd-audit-milestone` to re-run and pass.
</success_criteria>

<output>
After completion, create .planning/phases/22-source-authored-referral-flow-proof/22-01-SUMMARY.md
</output>
