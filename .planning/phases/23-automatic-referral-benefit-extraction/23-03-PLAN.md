---
phase: 23-automatic-referral-benefit-extraction
plan: 03
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified:
  - scripts/enrichment/report.ts
  - scripts/enrichment/report.test.ts
  - scripts/enrichment/generated-metadata.test.ts
  - data/generated/rich-metadata.json
  - data/generated/rich-enrichment-report.json
  - src/lib/content/load-content.referral.test.ts
files-modified:
  - scripts/enrichment/report.ts
  - scripts/enrichment/report.test.ts
  - scripts/enrichment/generated-metadata.test.ts
  - data/generated/rich-metadata.json
  - data/generated/rich-enrichment-report.json
  - src/lib/content/load-content.referral.test.ts
autonomous: true
requirements:
  - ENR-02
  - ENR-03
user_setup: []
objective: Harden report, artifact, and regression coverage for automatic referral benefit extraction so the repo can explain browser/static outcomes, preserve manual truth, and prove the feature on real generated output
must_haves:
  truths:
    - Enrichment reports and generated metadata surfaces can show extracted benefit fields and keep completeness/provenance coherent.
    - Manual benefit fields stay authoritative even when generated extraction finds benefit text.
    - Auth-gated or ambiguous pages still omit generated benefit fields instead of appearing complete.
    - Real generated artifacts can change when public benefit extraction finds something useful for current referral entries.
    - The regression surface protects both the new benefit extraction behavior and the existing manual-first merge posture.
  artifacts:
    - path: scripts/enrichment/report.test.ts
      provides: report coverage for extracted benefit fields plus completeness/provenance behavior
      min_lines: 130
    - path: scripts/enrichment/generated-metadata.test.ts
      provides: manifest stabilization coverage when referral benefit fields change or stay unchanged
      min_lines: 130
    - path: src/lib/content/load-content.referral.test.ts
      provides: real-data proof that manual fields still override generated benefit extraction after Phase 23
      min_lines: 60
    - path: data/generated/rich-metadata.json
      provides: refreshed real generated referral output if current links gain new generated benefits
      min_lines: 120
    - path: data/generated/rich-enrichment-report.json
      provides: refreshed report output with the same referral benefit extraction surface
      min_lines: 120
  key_links:
    - from: scripts/enrichment/report.ts
      to: scripts/enrichment/report.test.ts
      via: extracted benefit fields staying visible in structured report output
      pattern: "visitorBenefit"
    - from: data/generated/rich-metadata.json
      to: src/lib/content/load-content.referral.test.ts
      via: real generated referral output continuing to merge with manual fields without drift
      pattern: "cluborange-referral"
---

<objective>
Finish Phase 23 by making the new generated behavior visible and durable.

Purpose: prove the repo can explain benefit extraction outcomes, keep manual truth authoritative, and refresh the real generated referral artifact surface.
Output: report coverage, generated artifact refresh, and durable regression tests.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-automatic-referral-benefit-extraction/23-CONTEXT.md
@.planning/phases/23-automatic-referral-benefit-extraction/23-RESEARCH.md
@.planning/phases/23-automatic-referral-benefit-extraction/23-01-PLAN.md
@.planning/phases/23-automatic-referral-benefit-extraction/23-02-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend report and manifest regression coverage for extracted benefit fields</name>
  <files>scripts/enrichment/report.ts, scripts/enrichment/report.test.ts, scripts/enrichment/generated-metadata.test.ts</files>
  <action>Update the report/manifest regression surface so extracted `visitorBenefit` / `ownerBenefit` values remain visible in generated referral payloads, completeness stays coherent, and unchanged manifests still stabilize correctly. Keep the output structured and avoid inventing new confidence metadata.</action>
  <verify>Run `bun test scripts/enrichment/report.test.ts scripts/enrichment/generated-metadata.test.ts` and `bun run typecheck`.</verify>
  <done>The report and manifest surfaces explain Phase 23 outcomes instead of hiding them inside opaque generated blobs.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Refresh real generated referral artifacts and lock manual-precedence regressions</name>
  <files>data/generated/rich-metadata.json, data/generated/rich-enrichment-report.json, src/lib/content/load-content.referral.test.ts</files>
  <action>Re-run enrichment on the live dataset so any newly extracted benefit fields appear in the tracked generated artifacts, then update the real-data referral regression so manual benefit fields still win over generated ones while generated benefit blanks can fill when appropriate.</action>
  <verify>Run `bun run enrich:rich:strict`, `bun run validate:data`, `bun test src/lib/content/load-content.referral.test.ts`, and `bun run build`.</verify>
  <done>Phase 23 ends with real generated artifact proof and a regression that protects manual-over-generated benefit precedence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Report and manifest tests cover extracted benefit fields explicitly.
- [ ] Manual benefit fields remain authoritative over generated ones.
- [ ] Auth-gated or ambiguous pages still omit generated benefit fields.
- [ ] Real generated referral artifacts are refreshed if the phase changes current live output.
</verification>

<success_criteria>
- All tasks completed.
- ENR-03 is materially advanced through visible report/artifact coverage.
- The repo can explain and regress-test Phase 23 behavior on both fixture and real generated referral outputs.
</success_criteria>

<output>
After completion, create .planning/phases/23-automatic-referral-benefit-extraction/23-03-SUMMARY.md
</output>
