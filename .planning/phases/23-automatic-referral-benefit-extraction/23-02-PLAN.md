---
phase: 23-automatic-referral-benefit-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - scripts/enrichment/public-browser.ts
  - scripts/enrich-rich-links.ts
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/public-rich-sync.test.ts
files-modified:
  - scripts/enrichment/public-browser.ts
  - scripts/enrich-rich-links.ts
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/public-rich-sync.test.ts
autonomous: true
requirements:
  - ENR-01
  - ENR-02
user_setup: []
objective: Add a generic public browser fallback for JS-heavy public referral pages so benefit extraction can recover explicit visitor/owner text when static fetch is insufficient, without drifting into authenticated or per-program extractor behavior
must_haves:
  truths:
    - Browser fallback only runs for public referral candidates after static extraction leaves benefit fields unresolved.
    - Browser fallback reuses the same explicit-only benefit extraction logic as the static path.
    - Auth-gated or login-redirected targets are skipped rather than browser-automated.
    - The fallback stays generic and does not require a bespoke domain-specific extractor by default.
    - HTML-like public targets are eligible; XML/oEmbed-only strategies remain outside the browser fallback path.
  artifacts:
    - path: scripts/enrich-rich-links.ts
      provides: conditional browser fallback orchestration for unresolved public referral benefit extraction
      min_lines: 1750
    - path: scripts/enrichment/public-browser.ts
      provides: reusable public browser capture support for referral benefit extraction
      min_lines: 110
    - path: scripts/enrichment/public-augmentation.test.ts
      provides: browser fallback regression coverage for JS-heavy public referral pages and auth-gated skip behavior
      min_lines: 360
  key_links:
    - from: scripts/enrich-rich-links.ts
      to: scripts/enrichment/public-browser.ts
      via: conditional browser capture for unresolved public referral benefit fields
      pattern: "public browser"
    - from: scripts/enrich-rich-links.ts
      to: scripts/enrichment/public-augmentation.ts
      via: browser-captured rendered text being fed back through the same explicit-only extraction helper
      pattern: "visitorBenefit"
---

<objective>
Add JS-heavy public-page coverage without making browser automation the default.

Purpose: recover explicit visitor/owner benefit text from public rendered pages when plain fetch is insufficient, while keeping the flow public-first and conservative.
Output: a generic public browser fallback wired into referral enrichment only when static extraction leaves benefit fields blank.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-automatic-referral-benefit-extraction/23-CONTEXT.md
@.planning/phases/23-automatic-referral-benefit-extraction/23-RESEARCH.md
@.planning/phases/23-automatic-referral-benefit-extraction/23-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add a generic public browser capture path for referral benefit extraction</name>
  <files>scripts/enrichment/public-browser.ts, scripts/enrich-rich-links.ts</files>
  <action>Reuse the existing public browser utility layer to capture rendered public referral page text for HTML-like strategies when static extraction leaves `visitorBenefit` / `ownerBenefit` unresolved. Keep the capture generic and public-only, and avoid using it for XML/oEmbed strategies or auth-gated targets.</action>
  <verify>Run the focused browser-fallback tests plus `bun run typecheck`.</verify>
  <done>The repo has one conditional browser fallback path for referral benefits without expanding into authenticated extractor behavior.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Feed browser-captured public text back through the same explicit-only extractor</name>
  <files>scripts/enrich-rich-links.ts, scripts/enrichment/public-augmentation.ts, scripts/enrichment/public-augmentation.test.ts</files>
  <action>After browser capture, pass the rendered public text back through the same extraction helper introduced in 23-01 so static and browser paths share one ruleset. Add regression coverage for JS-heavy public pages that recover a benefit field and for auth-gated pages that stay omitted.</action>
  <verify>Run `bun test scripts/enrichment/public-augmentation.test.ts scripts/public-rich-sync.test.ts` and `bun run typecheck`.</verify>
  <done>Browser fallback improves coverage for JS-heavy public referral pages without creating a second extraction logic path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Browser fallback only runs after static benefit extraction leaves fields unresolved.
- [ ] Browser fallback remains public-only and skips auth-gated pages.
- [ ] Static and browser extraction share one explicit-only benefit ruleset.
- [ ] No bespoke per-domain extractor is required by default.
</verification>

<success_criteria>
- All tasks completed.
- ENR-01 and ENR-02 are materially advanced through a generic public browser fallback path.
- JS-heavy public referral pages are supported without weakening the public-first design.
</success_criteria>

<output>
After completion, create .planning/phases/23-automatic-referral-benefit-extraction/23-02-SUMMARY.md
</output>
