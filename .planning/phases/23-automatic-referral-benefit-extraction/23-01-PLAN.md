---
phase: 23-automatic-referral-benefit-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/enrich-rich-links.ts
  - scripts/enrichment/strategy-types.ts
  - scripts/enrichment/types.ts
files-modified:
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/enrich-rich-links.ts
  - scripts/enrichment/strategy-types.ts
  - scripts/enrichment/types.ts
autonomous: true
requirements:
  - ENR-02
user_setup: []
objective: Extend the public referral augmentation path so static public content can fill explicit `visitorBenefit` and `ownerBenefit` fields conservatively, without changing schema or weakening manual precedence
must_haves:
  truths:
    - Generated referral output can populate `visitorBenefit` and `ownerBenefit` from explicit public evidence.
    - Static parsing remains the default benefit-extraction path.
    - Ambiguous marketing copy is omitted rather than guessed into owner/visitor benefit fields.
    - Manual referral fields remain authoritative because merge behavior stays field-by-field and blank-fill-only.
    - No new referral schema fields or confidence scores are introduced.
  artifacts:
    - path: scripts/enrichment/public-augmentation.ts
      provides: static explicit-only benefit extraction logic integrated into public referral augmentation
      min_lines: 1250
    - path: scripts/enrichment/public-augmentation.test.ts
      provides: static-path regression coverage for explicit visitor-only, owner-only, both-side, and omitted cases
      min_lines: 320
    - path: scripts/enrich-rich-links.ts
      provides: wiring for generated benefit fields entering per-link output during enrichment
      min_lines: 1700
  key_links:
    - from: scripts/enrichment/public-augmentation.ts
      to: scripts/enrich-rich-links.ts
      via: generated `visitorBenefit` / `ownerBenefit` payload flowing into the tracked generated metadata output
      pattern: "visitorBenefit"
    - from: scripts/enrichment/public-augmentation.ts
      to: scripts/enrichment/public-augmentation.test.ts
      via: explicit-only extraction rules proven through focused public referral fixtures
      pattern: "ownerBenefit"
---

<objective>
Add static, explicit-only benefit extraction to the public referral pipeline.

Purpose: let public referral augmentation capture clearly stated visitor/owner benefits without changing the schema or defaulting to guesswork.
Output: generated `visitorBenefit` / `ownerBenefit` fields from static public content.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-automatic-referral-benefit-extraction/23-CONTEXT.md
@.planning/phases/23-automatic-referral-benefit-extraction/23-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add a pure explicit-only referral benefit extraction helper</name>
  <files>scripts/enrichment/public-augmentation.ts, scripts/enrichment/public-augmentation.test.ts</files>
  <action>Add a pure helper that inspects normalized public copy and only returns `visitorBenefit` / `ownerBenefit` when the text directly states what the visitor or owner gets. Keep the heuristics high-signal, omit vague marketing copy, and do not introduce brand-specific branches as the default path.</action>
  <verify>Run `bun test scripts/enrichment/public-augmentation.test.ts` and confirm coverage for visitor-only, owner-only, both-side, and omitted/ambiguous inputs.</verify>
  <done>Benefit extraction rules are centralized, conservative, and testable before any browser fallback is introduced.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Wire static benefit extraction into generated public referral output</name>
  <files>scripts/enrichment/public-augmentation.ts, scripts/enrich-rich-links.ts, scripts/enrichment/types.ts, scripts/enrichment/strategy-types.ts</files>
  <action>Update the public referral augmentation output so static public content can populate generated `visitorBenefit` / `ownerBenefit` fields alongside the existing `offerSummary` / `termsSummary` behavior. Keep manual-over-generated semantics intact by relying on the existing field-by-field merge path.</action>
  <verify>Run `bun run typecheck` and `bun run enrich:rich:strict` if the output-wiring path changes materially.</verify>
  <done>The main enrichment command can emit generated benefit fields without any schema or merge-policy changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Static public content can populate `visitorBenefit` and `ownerBenefit`.
- [ ] Benefit extraction is explicit-only and omits ambiguous marketing copy.
- [ ] Manual referral fields remain authoritative through the existing merge behavior.
- [ ] No schema change or confidence scoring is introduced.
</verification>

<success_criteria>
- All tasks completed.
- ENR-02 is materially advanced through static public benefit extraction.
- Phase 23 starts from the existing public augmentation seam instead of branching into a new referral subsystem.
</success_criteria>

<output>
After completion, create .planning/phases/23-automatic-referral-benefit-extraction/23-01-SUMMARY.md
</output>
