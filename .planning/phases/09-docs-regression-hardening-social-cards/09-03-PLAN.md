---
phase: 09-docs-regression-hardening-social-cards
plan: 03
type: execute
wave: 3
gap_closure: true
depends_on: []
files_modified:
  - README.md
  - docs/data-model.md
  - docs/customization-catalog.md
  - docs/social-card-verification.md
  - docs/ai-guided-customization.md
  - docs/studio-self-serve.md
files-modified:
  - README.md
  - docs/data-model.md
  - docs/customization-catalog.md
  - docs/social-card-verification.md
  - docs/ai-guided-customization.md
  - docs/studio-self-serve.md
autonomous: true
requirements:
  - DOC-05
  - DOC-06
user_setup: []
objective: Clarify across the core maintainer docs that the recommended CRUD path for social-card and profile data is via the repo AI workflows/skills or the Studio webapp, with direct JSON editing framed as the lower-level fallback
must_haves:
  truths:
    - The canonical docs explicitly recommend AI-assisted CRUD and the Studio webapp before raw JSON editing.
    - Manual file editing remains documented, but it is clearly described as the lower-level/manual fallback path.
    - README and the Phase 9 doc set route readers consistently into the recommended workflow docs instead of leaving the guidance implicit.
    - The new workflow guidance does not collapse the canonical deep-dive/reference split established in Phase 9.
  artifacts:
    - path: README.md
      provides: top-level routing that makes AI workflows and Studio the preferred CRUD entry points
      min_lines: 380
    - path: docs/data-model.md
      provides: canonical field reference with explicit recommended-workflow callouts
      min_lines: 900
    - path: docs/customization-catalog.md
      provides: quick-reference guidance that distinguishes recommended CRUD flow from manual fallback
      min_lines: 310
    - path: docs/social-card-verification.md
      provides: verification guidance that points maintainers back to AI/Studio workflows for routine updates
      min_lines: 190
  key_links:
    - from: README.md
      to: docs/ai-guided-customization.md
      via: preferred AI-assisted CRUD path
      pattern: "AI"
    - from: README.md
      to: docs/studio-self-serve.md
      via: preferred Studio CRUD path
      pattern: "Studio"
    - from: docs/data-model.md
      to: docs/customization-catalog.md
      via: canonical deep dive plus quick-reference workflow guidance
      pattern: "manual fallback"
    - from: docs/social-card-verification.md
      to: docs/data-model.md
      via: verification guidance that still points back to the canonical contract
      pattern: "CRUD"
---

<objective>
Close the Phase 9 UAT docs-clarity gap around preferred maintainer workflows.

Purpose: Make it obvious that most maintainers should CRUD this data via the repo AI workflows/skills or the Studio webapp, not by starting with raw JSON edits.
Output: Consistent workflow callouts across the top-level and social-card docs, with manual editing kept as the lower-level fallback.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-docs-regression-hardening-social-cards/09-UAT.md
@.planning/phases/09-docs-regression-hardening-social-cards/09-VERIFICATION.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add explicit preferred-workflow callouts to the top-level maintainer entrypoints</name>
  <files>README.md, docs/ai-guided-customization.md, docs/studio-self-serve.md</files>
  <action>Update the repo entrypoint docs so they clearly recommend the AI-assisted repo workflows/skills and the Studio webapp as the primary CRUD paths for most maintainers. Keep direct file editing documented, but reposition it as the lower-level/manual path for advanced or fallback use.</action>
  <verify>Review the updated routing copy for consistency, then run `bun run biome:check`.</verify>
  <done>The top-level docs no longer leave the preferred CRUD path implicit.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Align the Phase 9 social-card docs with the recommended CRUD posture</name>
  <files>docs/data-model.md, docs/customization-catalog.md, docs/social-card-verification.md</files>
  <action>Add concise callouts near the top of the canonical deep dive, quick-reference catalog, and verification guide that tell maintainers to prefer the AI workflows/skills or Studio for routine CRUD changes. Keep the existing field-reference and verification content intact while making manual JSON editing read as the fallback path.</action>
  <verify>Review the updated docs together to confirm they no longer imply raw JSON editing is the default maintainer experience, then run `bun run biome:check`.</verify>
  <done>The core Phase 9 docs consistently point readers toward the recommended CRUD workflows.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Close with a docs-drift pass across the workflow messaging</name>
  <files>README.md, docs/data-model.md, docs/customization-catalog.md, docs/social-card-verification.md, docs/ai-guided-customization.md, docs/studio-self-serve.md</files>
  <action>Do a final docs-drift pass to ensure the recommended-workflow language is consistent across the touched docs, avoids overpromising unavailable Studio features, and still preserves the reference-doc split established in Phase 9.</action>
  <verify>Run `bun run build` if any touched docs are surfaced in the site or maintainer flow, and review the final diff for contradictory workflow guidance.</verify>
  <done>The docs set has one clear recommended CRUD story instead of mixed signals.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] README makes AI workflows/skills and Studio the preferred CRUD entry points.
- [ ] The canonical/social-card docs describe manual JSON editing as the lower-level fallback path.
- [ ] Workflow guidance is consistent across the touched docs.
- [ ] The new guidance does not imply unsupported Studio capabilities.
</verification>

<success_criteria>
- All tasks completed.
- The Phase 9 UAT docs-clarity issue is fully addressed by one executable gap-closure plan.
- The resulting fix is ready for `$gsd-execute-phase 9 --gaps-only`.
</success_criteria>

<output>
After completion, create .planning/phases/09-docs-regression-hardening-social-cards/09-03-SUMMARY.md
</output>
