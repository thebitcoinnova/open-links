---
phase: 12-add-share-button-in-each-card-next-to-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/share/share-link.ts
  - src/lib/share/share-link.test.ts
  - src/components/profile/ProfileHeader.tsx
  - src/components/profile/ProfileHeader.test.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/RichLinkCard.tsx
  - src/components/cards/SimpleLinkCard.tsx
files-modified:
  - src/lib/share/share-link.ts
  - src/lib/share/share-link.test.ts
  - src/components/profile/ProfileHeader.tsx
  - src/components/profile/ProfileHeader.test.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/RichLinkCard.tsx
  - src/components/cards/SimpleLinkCard.tsx
autonomous: true
requirements: []
user_setup: []
objective: Extract and unify the native-share plus clipboard-fallback behavior into a shared utility, then extend the card action contract so cards can reuse that behavior without duplicating the profile-header implementation
must_haves:
  truths:
    - Profile-level and card-level share actions use one shared native-share utility instead of duplicating `navigator.share` and clipboard fallback logic.
    - The shared utility respects Web Share constraints: user-triggered invocation, secure-context assumptions, and graceful fallback when native sharing is unavailable.
    - Card share actions remain sibling buttons outside the anchor element, matching the current analytics action semantics.
    - Existing profile-header share behavior remains functionally equivalent after the extraction.
  artifacts:
    - path: src/lib/share/share-link.ts
      provides: reusable share helper for native share, `canShare` checks where helpful, and clipboard fallback result handling
      min_lines: 160
    - path: src/lib/share/share-link.test.ts
      provides: focused coverage for native-share success, clipboard fallback, and failure handling
      min_lines: 140
    - path: src/components/profile/ProfileHeader.tsx
      provides: profile-level share behavior migrated onto the shared utility
      min_lines: 150
    - path: src/components/cards/NonPaymentLinkCardShell.tsx
      provides: a generalized sibling card-action seam that can host analytics plus share
      min_lines: 220
  key_links:
    - from: src/lib/share/share-link.ts
      to: src/components/profile/ProfileHeader.tsx
      via: shared native/fallback share logic reused by the profile header
      pattern: "share"
    - from: src/lib/share/share-link.ts
      to: src/components/cards/NonPaymentLinkCardShell.tsx
      via: shared native/fallback share logic reused by card actions
      pattern: "share"
    - from: src/components/cards/NonPaymentLinkCardShell.tsx
      to: src/components/cards/RichLinkCard.tsx
      via: generalized sibling action contract for rich and simple cards
      pattern: "resolve"
---

<objective>
Create the shared share-action foundation before wiring the new card button into the UI.

Purpose: Keep profile-level and card-level sharing behavior aligned and avoid duplicating native-share and clipboard fallback code.
Output: One share utility, profile-header reuse, and a card action contract ready for the new button.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-add-share-button-in-each-card-next-to-analytics/12-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extract the current share logic into a shared utility with native and fallback paths</name>
  <files>src/lib/share/share-link.ts, src/lib/share/share-link.test.ts, src/components/profile/ProfileHeader.tsx</files>
  <action>Move the share-specific helpers out of `ProfileHeader.tsx` into a shared utility that tries `navigator.share()` for supported payloads, falls back to clipboard copy when native share is unavailable, and returns a small result/status contract. Update the profile header to call that utility so existing profile-level share behavior stays intact.</action>
  <verify>Run focused share utility tests plus `bun test src/components/profile/ProfileHeader.test.tsx`, then `bun run typecheck`.</verify>
  <done>The repo has one reusable share implementation instead of surface-specific copies.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Extend the sibling card-action seam so cards can host analytics plus share</name>
  <files>src/components/cards/NonPaymentLinkCardShell.tsx, src/components/cards/RichLinkCard.tsx, src/components/cards/SimpleLinkCard.tsx</files>
  <action>Update the shared card shell and its props so cards can render both analytics and share actions as sibling buttons outside the anchor. Keep the seam flexible enough for two actions without reintroducing the Phase 11 “side column” regression or nested interactive content.</action>
  <verify>Run `bun test src/components/cards/non-payment-card-accessibility.test.tsx` and `bun run typecheck`.</verify>
  <done>The card shell can support the extra share action cleanly before the actual button behavior and layout land.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Lock down profile-share parity and card-action semantics before UI rollout</name>
  <files>src/lib/share/share-link.test.ts, src/components/profile/ProfileHeader.test.tsx, src/components/cards/non-payment-card-accessibility.test.tsx</files>
  <action>Add or update focused tests proving the extracted share helper preserves the current profile-header semantics and that the card action contract still keeps auxiliary actions outside the anchor.</action>
  <verify>Run `bun test src/lib/share/share-link.test.ts src/components/profile/ProfileHeader.test.tsx src/components/cards/non-payment-card-accessibility.test.tsx` and `bun run biome:check`.</verify>
  <done>The share foundation is regression-safe before the visible card button lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] One shared share helper now owns native-share and clipboard fallback behavior.
- [ ] Profile-header sharing still behaves the same after moving to the shared helper.
- [ ] Card action seams can host both analytics and share buttons outside the anchor.
- [ ] Focused tests cover both share helper behavior and preserved card semantics.
</verification>

<success_criteria>
- All tasks completed.
- The repo has a single reusable share implementation ready for both profile and card surfaces.
- Phase 12 can add the visible card share button without duplicating or drifting share behavior.
</success_criteria>

<output>
After completion, create .planning/phases/12-add-share-button-in-each-card-next-to-analytics/12-01-SUMMARY.md
</output>
