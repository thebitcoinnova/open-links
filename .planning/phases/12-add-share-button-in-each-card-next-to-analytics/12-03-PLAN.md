---
phase: 12-add-share-button-in-each-card-next-to-analytics
plan: 03
type: execute
wave: 3
gap_closure: true
depends_on: []
files_modified:
  - src/lib/share/share-link.ts
  - src/lib/share/share-link.test.ts
  - src/components/profile/ProfileHeader.tsx
  - src/routes/index.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/non-payment-card-accessibility.test.tsx
files-modified:
  - src/lib/share/share-link.ts
  - src/lib/share/share-link.test.ts
  - src/components/profile/ProfileHeader.tsx
  - src/routes/index.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/non-payment-card-accessibility.test.tsx
autonomous: true
requirements: []
user_setup: []
objective: Fix the Phase 12 share-copy and visibility gaps so share actions copy clean URLs and appear on cards even when follower-history analytics are unavailable
must_haves:
  truths:
    - Profile share produces a clean URL-oriented copy result instead of a copied string polluted with headline or descriptive text.
    - Card share produces a clean card URL-oriented copy result instead of appending title or description text to the copied string.
    - Cards without follower-history data still expose the share action even though they do not expose analytics.
    - History-aware cards continue to show analytics first and share second, while non-history cards show share only.
    - Regression coverage proves both the new share payload behavior and the widened share visibility rules.
  artifacts:
    - path: src/lib/share/share-link.ts
      provides: a configurable share-payload policy that can preserve clean URL copy behavior
      min_lines: 170
    - path: src/lib/share/share-link.test.ts
      provides: focused coverage for URL-only/copy-safe share payload behavior
      min_lines: 150
    - path: src/routes/index.tsx
      provides: card action resolution that renders share for all shareable cards and analytics only for history-aware cards
      min_lines: 320
    - path: src/components/cards/non-payment-card-accessibility.test.tsx
      provides: regression coverage for share-only cards and analytics-plus-share cards
      min_lines: 240
  key_links:
    - from: src/lib/share/share-link.ts
      to: src/components/profile/ProfileHeader.tsx
      via: clean URL-oriented share payloads for profile share
      pattern: "share"
    - from: src/lib/share/share-link.ts
      to: src/routes/index.tsx
      via: clean URL-oriented share payloads for card share
      pattern: "share"
    - from: src/routes/index.tsx
      to: src/components/cards/NonPaymentLinkCardShell.tsx
      via: share-only vs analytics-plus-share card action resolution
      pattern: "resolveCardActions"
---

<objective>
Close the Phase 12 UAT gaps around share payloads and share visibility.

Purpose: Make share copy results pasteable as clean URLs and ensure share is available on cards even when analytics is not.
Output: Copy-safe share payload behavior, widened card share visibility, and regression coverage for both cases.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-add-share-button-in-each-card-next-to-analytics/12-UAT.md
@.planning/phases/12-add-share-button-in-each-card-next-to-analytics/12-VERIFICATION.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Make shared share payloads copy-safe for URL-oriented copy behavior</name>
  <files>src/lib/share/share-link.ts, src/lib/share/share-link.test.ts, src/components/profile/ProfileHeader.tsx</files>
  <action>Update the shared share helper so surfaces that need copy-safe URL behavior can omit extra descriptive fields from the native share payload. Apply that to the profile-level share flow and add focused tests proving the resulting payload keeps the URL clean for copy-oriented share targets.</action>
  <verify>Run `bun test src/lib/share/share-link.test.ts src/components/profile/ProfileHeader.test.tsx` and `bun run typecheck`.</verify>
  <done>Profile share no longer produces copy strings polluted by extra text fields.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Decouple card share visibility from analytics availability</name>
  <files>src/routes/index.tsx, src/components/cards/NonPaymentLinkCardShell.tsx</files>
  <action>Change the card action resolution so all eligible cards render share, while only history-aware cards render analytics. Keep the existing order for history-aware cards (analytics then share), and render share-only on cards without history data.</action>
  <verify>Inspect the main links page on first load and confirm share-only cards appear where analytics is absent.</verify>
  <done>The card share action is available beyond the history-aware subset, while analytics stays correctly scoped.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add regression coverage for clean copy payloads and share-only cards</name>
  <files>src/lib/share/share-link.test.ts, src/components/cards/non-payment-card-accessibility.test.tsx, src/routes/index.tsx</files>
  <action>Add tests that prove share payloads can be URL-only when required, cards without history render a share action but no analytics action, and history-aware cards still render analytics then share in order.</action>
  <verify>Run `bun test src/lib/share/share-link.test.ts src/components/cards/non-payment-card-accessibility.test.tsx`, `bun run biome:check`, and `bun run build`.</verify>
  <done>The Phase 12 share gaps are protected by focused regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Profile share copy is URL-clean.
- [ ] Card share copy is URL-clean.
- [ ] Cards without history data still show share.
- [ ] History-aware cards still show analytics then share in order.
- [ ] Focused tests and repo verification commands pass after the fix.
</verification>

<success_criteria>
- All tasks completed.
- The Phase 12 UAT issues are fully addressed by one executable gap-closure plan.
- The resulting fix is ready for `$gsd-execute-phase 12 --gaps-only`.
</success_criteria>

<output>
After completion, create .planning/phases/12-add-share-button-in-each-card-next-to-analytics/12-03-SUMMARY.md
</output>
