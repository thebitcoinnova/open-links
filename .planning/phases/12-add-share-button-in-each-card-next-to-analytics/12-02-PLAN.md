---
phase: 12-add-share-button-in-each-card-next-to-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/routes/index.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/cards/non-payment-card-accessibility.test.tsx
  - src/components/profile/ProfileHeader.test.tsx
files-modified:
  - src/routes/index.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/cards/non-payment-card-accessibility.test.tsx
  - src/components/profile/ProfileHeader.test.tsx
autonomous: true
requirements: []
user_setup: []
objective: Render the card-level native share button to the right of analytics, wire it to the shared share utility, and verify layout and accessibility across desktop and mobile
must_haves:
  truths:
    - History-aware cards expose both analytics and share actions on the main links page.
    - The share button sits immediately to the right of analytics within the card header action area instead of drifting into a separate side column.
    - The share action targets the specific card destination URL and invokes native share when available, falling back cleanly otherwise.
    - Cards without history data do not gain a broken or empty action row.
    - Regression and accessibility coverage prove the two-button action row preserves anchor semantics and layout stability.
  artifacts:
    - path: src/routes/index.tsx
      provides: share-action resolution for history-aware cards using the shared share helper
      min_lines: 320
    - path: src/components/cards/NonPaymentLinkCardShell.tsx
      provides: rendered analytics + share action row in the card header area
      min_lines: 230
    - path: src/styles/base.css
      provides: aligned two-button card action row styling
      min_lines: 1060
    - path: src/styles/responsive.css
      provides: mobile-safe wrapping and spacing for the card action pair
      min_lines: 150
    - path: src/components/cards/non-payment-card-accessibility.test.tsx
      provides: focused regression coverage for the two-button action row and preserved anchor semantics
      min_lines: 230
  key_links:
    - from: src/routes/index.tsx
      to: src/components/cards/NonPaymentLinkCardShell.tsx
      via: resolved card analytics and share actions for history-aware cards
      pattern: "resolve"
    - from: src/components/cards/NonPaymentLinkCardShell.tsx
      to: src/styles/base.css
      via: two-button card-header action layout
      pattern: "card-analytics-button"
    - from: src/components/cards/non-payment-card-accessibility.test.tsx
      to: src/routes/index.tsx
      via: action-row regression coverage for first render behavior
      pattern: "share"
---

<objective>
Ship the visible Phase 12 behavior after the shared share helper exists.

Purpose: Add a card-level native share affordance that feels like a natural sibling to analytics instead of a bolted-on extra control.
Output: Two-button card action row, wired share behavior, and desktop/mobile regression coverage.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-add-share-button-in-each-card-next-to-analytics/12-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Wire card-level share resolution beside analytics for history-aware cards</name>
  <files>src/routes/index.tsx, src/components/cards/NonPaymentLinkCardShell.tsx</files>
  <action>Use the new shared share helper to add a card-level share action for every history-aware card. Keep the action scoped to the card URL/title, place it immediately to the right of analytics, and leave cards without history data unchanged.</action>
  <verify>Run focused card rendering/accessibility coverage and manually inspect the main links page on first load.</verify>
  <done>History-aware cards now expose both analytics and share actions from the same header action area.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Align the two-button card action row across desktop and mobile</name>
  <files>src/components/cards/NonPaymentLinkCardShell.tsx, src/styles/base.css, src/styles/responsive.css</files>
  <action>Update the card action layout so analytics remains first, share sits immediately to its right, and both stay visually integrated with the card header row across breakpoints. Avoid whole-card spacing hacks that would make the action pair read like a second column again.</action>
  <verify>Inspect desktop and mobile built previews to confirm the action pair stays aligned and does not overlap title/meta content.</verify>
  <done>The two-button action row feels intentional and stable across the existing card variants and breakpoints.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add focused regression and accessibility coverage for the card share action</name>
  <files>src/components/cards/non-payment-card-accessibility.test.tsx, src/components/profile/ProfileHeader.test.tsx</files>
  <action>Add tests that prove history-aware cards render analytics then share in order, both actions remain outside the anchor, and the profile-level share tests still pass after the shared share helper extraction. Cover the case where cards without history do not render the new action row.</action>
  <verify>Run `bun test src/components/cards/non-payment-card-accessibility.test.tsx src/components/profile/ProfileHeader.test.tsx`, `bun run typecheck`, `bun run biome:check`, and `bun run build`.</verify>
  <done>The visible Phase 12 behavior is guarded against regressions in semantics, order, and layout.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] History-aware cards show analytics then share in that order.
- [ ] The share action uses native share when available and falls back cleanly otherwise.
- [ ] The card action pair stays aligned with the card header row on desktop and mobile.
- [ ] Card anchors keep their existing action-oriented naming and do not wrap either auxiliary action.
</verification>

<success_criteria>
- All tasks completed.
- Phase 12 delivers the requested card-level share affordance without regressing Phase 11 card actions.
- The repo has regression coverage for the two-button card action row and shared share behavior.
</success_criteria>

<output>
After completion, create .planning/phases/12-add-share-button-in-each-card-next-to-analytics/12-02-SUMMARY.md
</output>
