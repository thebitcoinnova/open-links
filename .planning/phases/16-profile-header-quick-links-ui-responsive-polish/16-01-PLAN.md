---
phase: 16-profile-header-quick-links-ui-responsive-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profile/ProfileQuickLinks.tsx
  - src/components/profile/ProfileQuickLinks.test.tsx
  - src/components/profile/ProfileHeader.tsx
files-modified:
  - src/components/profile/ProfileQuickLinks.tsx
  - src/components/profile/ProfileQuickLinks.test.tsx
  - src/components/profile/ProfileHeader.tsx
autonomous: true
requirements:
  - QLINK-01
  - QLINK-05
user_setup: []
objective: Build the visible Quick Links strip component and integrate it above the existing profile action bar without re-deriving data or introducing a second action bar pattern
must_haves:
  truths:
    - The profile header renders a visible Quick Links strip when `quickLinks.hasAny` is true and hides it completely when empty.
    - The strip is icon-first, has no visible heading, and feels like outbound social shortcuts rather than app actions.
    - The new UI consumes the Phase 15 quick-link state seam instead of recomputing eligibility or ordering rules.
    - The initial visible strip uses one dedicated component rather than stuffing all markup into `ProfileHeader`.
  artifacts:
    - path: src/components/profile/ProfileQuickLinks.tsx
      provides: the dedicated visible strip component for icon-only quick links above the action bar
      min_lines: 140
    - path: src/components/profile/ProfileQuickLinks.test.tsx
      provides: focused coverage for visible render vs hidden-empty behavior and icon-link structure
      min_lines: 160
    - path: src/components/profile/ProfileHeader.tsx
      provides: integration of the new strip above the existing action bar without re-deriving quick-link rules
      min_lines: 170
  key_links:
    - from: src/components/profile/ProfileQuickLinks.tsx
      to: src/components/profile/ProfileHeader.tsx
      via: visible strip rendered from the existing header quick-link state seam
      pattern: "quickLinks"
    - from: src/components/profile/ProfileQuickLinks.test.tsx
      to: src/components/profile/ProfileQuickLinks.tsx
      via: visible-strip render/hide assertions
      pattern: "quick"
    - from: src/components/profile/ProfileHeader.tsx
      to: src/components/profile/ProfileQuickLinks.tsx
      via: strip positioned above the existing action bar
      pattern: "profile-action-bar"
---

<objective>
Make the Quick Links strip real before polishing how it flexes across breakpoints.

Purpose: give Phase 16 a concrete, visible strip component that uses the Phase 15 foundation seam and establishes the basic non-empty/empty rendering contract.
Output: one dedicated `ProfileQuickLinks` component, header integration above the action bar, and focused tests proving the strip renders only when eligible quick links exist.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-profile-header-quick-links-ui-responsive-polish/16-CONTEXT.md
@.planning/phases/16-profile-header-quick-links-ui-responsive-polish/16-RESEARCH.md
@.planning/phases/15-quick-links-foundation/15-02-SUMMARY.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Create the dedicated visible Quick Links strip component</name>
  <files>src/components/profile/ProfileQuickLinks.tsx, src/components/profile/ProfileQuickLinks.test.tsx</files>
  <action>Create a new profile-level Quick Links component that renders the Phase 15 quick-link state as centered, icon-only outbound links with no visible heading. Keep the component visually lighter than the action bar and do not re-derive the data rules inside it.</action>
  <verify>Run `bun test src/components/profile/ProfileQuickLinks.test.tsx` and `bun run typecheck`.</verify>
  <done>The repo has one dedicated component for the visible Quick Links strip rather than scattering strip markup inside `ProfileHeader`.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Integrate the strip above the profile action bar using the Phase 15 seam</name>
  <files>src/components/profile/ProfileHeader.tsx, src/components/profile/ProfileQuickLinks.tsx</files>
  <action>Insert the new strip above the existing profile action bar, keep the strip hidden when `quickLinks.hasAny` is false, and preserve the current action-bar ordering and semantics. Treat the strip as a distinct small block above the actions rather than another action row.</action>
  <verify>Run `bun test src/components/profile/ProfileQuickLinks.test.tsx src/components/profile/ProfileHeader.test.tsx` and `bun run typecheck`.</verify>
  <done>The header visibly renders Quick Links only when foundation data exists and keeps the action bar intact beneath it.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Lock the initial visible-strip contract before responsive polish</name>
  <files>src/components/profile/ProfileQuickLinks.test.tsx, src/components/profile/ProfileHeader.tsx</files>
  <action>Add or refine assertions that the visible strip appears above the action bar, stays icon-only with no heading, and disappears entirely when no eligible quick links exist. Keep the checks structural and phase-scoped so Wave 2 can focus on layout polish rather than basic visibility rules.</action>
  <verify>Run `bun test src/components/profile/ProfileQuickLinks.test.tsx src/components/profile/ProfileHeader.test.tsx`, `bun run biome:check`, and `bun run build`.</verify>
  <done>The visible-strip contract is real and stable before responsive/layout work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The profile header renders a visible Quick Links strip only when `quickLinks.hasAny` is true.
- [ ] The strip has no visible heading and reads as icon-first outbound social shortcuts.
- [ ] The strip sits above the existing action bar without replacing or duplicating it.
- [ ] The component structure is dedicated and reusable rather than being embedded as ad-hoc markup inside the header.
</verification>

<success_criteria>
- All tasks completed.
- Phase 16 has a concrete visible Quick Links strip above the profile action bar.
- Later responsive and accessibility polish can refine a stable visible component instead of inventing it.
</success_criteria>

<output>
After completion, create .planning/phases/16-profile-header-quick-links-ui-responsive-polish/16-01-SUMMARY.md
</output>
