---
phase: 16-profile-header-quick-links-ui-responsive-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/components/profile/ProfileQuickLinks.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/profile/ProfileQuickLinks.test.tsx
files-modified:
  - src/components/profile/ProfileQuickLinks.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/profile/ProfileQuickLinks.test.tsx
autonomous: true
requirements:
  - HEAD-02
user_setup: []
objective: Add the approved responsive layout, spacing, wrapping, and overflow treatment so the Quick Links strip stays polished and subordinate to the action bar on mobile and desktop
must_haves:
  truths:
    - The visible Quick Links strip is centered under the profile copy and reads as a small distinct block above the action bar.
    - The default layout wraps naturally to multiple lines instead of locking a fixed visible count.
    - Desktop follows the same overall behavior while fitting more links before overflow becomes necessary.
    - Overflow uses a subtle horizontal-scroll affordance with edge fade/shadow hint rather than a new menu-first pattern.
    - The strip remains visually lighter than the share/copy/QR action row.
  artifacts:
    - path: src/components/profile/ProfileQuickLinks.tsx
      provides: overflow container and class hooks for centered, multi-line, icon-only layout behavior
      min_lines: 170
    - path: src/styles/base.css
      provides: the default centered strip styling, spacing, subtle motion baseline, and edge-fade treatment hooks
      min_lines: 500
    - path: src/styles/responsive.css
      provides: mobile/desktop adjustments for wrap behavior and overflow treatment
      min_lines: 200
    - path: src/components/profile/ProfileQuickLinks.test.tsx
      provides: structural checks for centered/wrapping/overflow-related markup hooks where practical
      min_lines: 190
  key_links:
    - from: src/components/profile/ProfileQuickLinks.tsx
      to: src/styles/base.css
      via: strip-specific classes for centered layout, overflow affordance, and interaction styling
      pattern: "profile-quick-links"
    - from: src/components/profile/ProfileQuickLinks.tsx
      to: src/styles/responsive.css
      via: responsive wrapping and overflow behavior
      pattern: "quick-links"
    - from: src/components/profile/ProfileQuickLinks.test.tsx
      to: src/components/profile/ProfileQuickLinks.tsx
      via: structural coverage for layout/overflow hooks without freezing the exact CSS implementation
      pattern: "quick"
---

<objective>
Make the strip feel intentional and resilient across breakpoints.

Purpose: turn the visible strip into a polished part of the header by implementing the approved spacing, wrapping, and overflow behavior while keeping it visually subordinate to the action bar.
Output: centered multi-line default layout, desktop/mobile responsive behavior, and subtle overflow affordance for crowded cases.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-profile-header-quick-links-ui-responsive-polish/16-CONTEXT.md
@.planning/phases/16-profile-header-quick-links-ui-responsive-polish/16-RESEARCH.md
@.planning/phases/16-profile-header-quick-links-ui-responsive-polish/16-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add centered default multi-line layout and spacing for the strip</name>
  <files>src/components/profile/ProfileQuickLinks.tsx, src/styles/base.css</files>
  <action>Implement the default centered strip layout with tight spacing above the action bar, keeping the strip visually distinct but connected. Use multi-line wrapping by default and keep the strip lighter than the action bar chrome.</action>
  <verify>Run `bun run build` and focused strip/header tests if they exist.</verify>
  <done>The strip looks like a deliberate, centered icon row rather than an accidental pile of shortcuts or a second action bar.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add responsive behavior for mobile/desktop fit and overflow hinting</name>
  <files>src/components/profile/ProfileQuickLinks.tsx, src/styles/base.css, src/styles/responsive.css</files>
  <action>Implement the approved responsive behavior: multi-line by default, desktop naturally fitting more first, and horizontal-scroll affordance with subtle edge fade/shadow hint when overflow occurs. Do not add configurable modes or menu-first overflow in this phase.</action>
  <verify>Run `bun run build` and inspect the relevant CSS/markup hooks through tests or a focused preview check if needed.</verify>
  <done>The strip behaves gracefully when link counts grow without introducing a new configuration surface.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Lock the responsive strip structure with focused non-fragile tests</name>
  <files>src/components/profile/ProfileQuickLinks.test.tsx, src/components/profile/ProfileQuickLinks.tsx</files>
  <action>Add structural tests for the strip's class hooks and overflow-ready container behavior without freezing pixel-level layout details. Keep the tests about phase-level responsive intent, not about brittle CSS snapshots.</action>
  <verify>Run `bun test src/components/profile/ProfileQuickLinks.test.tsx`, `bun run biome:check`, and `bun run typecheck`.</verify>
  <done>The responsive strip behavior is explicit enough that Wave 3 can focus on accessibility and interaction polish.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The strip is centered and visually distinct above the action bar.
- [ ] The default behavior is multi-line wrapping with no fixed visible count.
- [ ] Desktop follows the same general behavior while naturally fitting more before overflow.
- [ ] Overflow uses subtle horizontal-scroll hinting rather than a new menu-first pattern.
- [ ] The strip stays visually lighter than the action bar.
</verification>

<success_criteria>
- All tasks completed.
- The Quick Links strip is responsive and polished on mobile and desktop.
- Overflow behavior exists without introducing new configuration or Phase 17 documentation scope.
</success_criteria>

<output>
After completion, create .planning/phases/16-profile-header-quick-links-ui-responsive-polish/16-02-SUMMARY.md
</output>
