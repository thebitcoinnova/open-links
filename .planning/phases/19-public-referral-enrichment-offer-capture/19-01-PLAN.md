---
phase: 19-public-referral-enrichment-offer-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/enrichment/referral-targets.ts
  - scripts/enrichment/referral-targets.test.ts
  - scripts/enrichment/fetch-metadata.ts
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/enrichment/strategy-types.ts
  - scripts/enrichment/strategy-registry.test.ts
files-modified:
  - scripts/enrichment/referral-targets.ts
  - scripts/enrichment/referral-targets.test.ts
  - scripts/enrichment/fetch-metadata.ts
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/enrichment/strategy-types.ts
  - scripts/enrichment/strategy-registry.test.ts
autonomous: true
requirements:
  - ENR-01
user_setup: []
objective: Build a reusable public referral-target normalization layer with bounded redirect handling so common referral URLs can resolve to canonical public landing pages without moving brand-specific rules into user-authored data
must_haves:
  truths:
    - Public referral target resolution preserves both the original saved URL and the resolved extraction target.
    - Known-family canonical rewrites remain in the enrichment strategy layer, not the content schema.
    - Offer-affecting referral identifiers are preserved while pure analytics params are stripped during normalization.
    - Shorteners and bounded multi-hop redirects are supported only while the chain stays public.
    - Existing public augmentation strategies, including Club Orange, remain aligned with the new target-resolution seam.
  artifacts:
    - path: scripts/enrichment/referral-targets.ts
      provides: reusable referral URL normalization, param filtering, and redirect-policy helpers
      min_lines: 120
    - path: scripts/enrichment/referral-targets.test.ts
      provides: direct, query-param, path-based, shortener, and canonical-rewrite regression coverage
      min_lines: 160
    - path: scripts/enrichment/fetch-metadata.ts
      provides: fetch result support for resolved/final public target tracking
      min_lines: 90
    - path: scripts/enrichment/public-augmentation.ts
      provides: integration with the reusable referral-target normalization seam
      min_lines: 950
    - path: scripts/enrichment/strategy-types.ts
      provides: public strategy/source contracts that can carry original/resolved target context
      min_lines: 60
  key_links:
    - from: scripts/enrichment/referral-targets.ts
      to: scripts/enrichment/public-augmentation.ts
      via: generic target resolution reused by public augmentation strategies
      pattern: "referral"
    - from: scripts/enrichment/fetch-metadata.ts
      to: scripts/enrichment/referral-targets.ts
      via: bounded redirect/final-url capture
      pattern: "final"
    - from: scripts/enrichment/public-augmentation.test.ts
      to: scripts/enrichment/referral-targets.test.ts
      via: Club Orange and generic referral-target resolution staying aligned
      pattern: "referral"
---

<objective>
Generalize how public referral URLs resolve before any offer-term extraction happens.

Purpose: create one reusable, public-only target-resolution seam so Phase 19 can handle query-param, path-based, and shortener-backed referral links without turning every provider into a bespoke strategy.
Output: reusable referral target helpers plus the fetch/strategy plumbing needed to preserve original and resolved target URLs.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-public-referral-enrichment-offer-capture/19-CONTEXT.md
@.planning/phases/19-public-referral-enrichment-offer-capture/19-RESEARCH.md
@.planning/phases/18-referral-contract-link-plumbing/18-VERIFICATION.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add a reusable referral target normalization helper</name>
  <files>scripts/enrichment/referral-targets.ts, scripts/enrichment/referral-targets.test.ts, scripts/enrichment/public-augmentation.ts</files>
  <action>Create a dedicated helper module that normalizes public referral URLs by shape rather than by brittle brand list. It should understand direct public landings, query-param referral URLs, path-based invite/referral URLs, and known-family canonical rewrites while preserving the original saved URL and the resolved extraction target.</action>
  <verify>Run `bun test scripts/enrichment/referral-targets.test.ts scripts/enrichment/public-augmentation.test.ts` and `bun run typecheck`.</verify>
  <done>The repo has one reusable referral-target seam instead of only isolated brand-specific rewrites.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Extend fetch and strategy contracts for bounded redirect provenance</name>
  <files>scripts/enrichment/fetch-metadata.ts, scripts/enrichment/strategy-types.ts, scripts/enrichment/public-augmentation.ts</files>
  <action>Extend the public fetch/strategy contracts so referral normalization can record bounded redirect/final-target information without leaking into authenticated flows. Keep redirect handling public-only, preserve offer-affecting params, and strip pure analytics params when they do not change the offer.</action>
  <verify>Run `bun test scripts/enrichment/fetch-metadata.test.ts scripts/enrichment/referral-targets.test.ts` and `bun run typecheck`.</verify>
  <done>The public enrichment pipeline can represent original vs resolved public targets cleanly before extracting any referral terms.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Lock the target-resolution regression surface before term extraction begins</name>
  <files>scripts/enrichment/public-augmentation.test.ts, scripts/enrichment/strategy-registry.test.ts, scripts/enrichment/referral-targets.test.ts</files>
  <action>Add focused regression cases covering direct landing pages, query-param referrals, path-based referrals, bounded shortener resolution, and the existing Club Orange canonical rewrite. Keep the assertions centered on target resolution and public-only bounds, not later term extraction behavior.</action>
  <verify>Run `bun test scripts/enrichment/public-augmentation.test.ts scripts/enrichment/strategy-registry.test.ts scripts/enrichment/referral-targets.test.ts` and `bun run typecheck`.</verify>
  <done>Phase 19 has a stable target-resolution contract that later offer extraction can build on confidently.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Original saved URL and resolved extraction target are both representable.
- [ ] Known-family rewrites remain in strategy code, not user-authored JSON.
- [ ] Offer-affecting referral params are preserved while pure analytics params are stripped.
- [ ] Shorteners are supported only within a bounded public-only redirect policy.
- [ ] Existing Club Orange behavior stays covered by the new regression surface.
</verification>

<success_criteria>
- All tasks completed.
- ENR-01 is materially advanced through generalized public target resolution.
- Phase 19 can proceed to term extraction without reopening target-resolution design.
</success_criteria>

<output>
After completion, create .planning/phases/19-public-referral-enrichment-offer-capture/19-01-SUMMARY.md
</output>
