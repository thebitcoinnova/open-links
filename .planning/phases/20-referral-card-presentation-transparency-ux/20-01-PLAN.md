---
phase: 20-referral-card-presentation-transparency-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ui/referral-card-presentation.ts
  - src/lib/ui/referral-card-presentation.test.ts
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/rich-card-description-sourcing.test.ts
  - src/components/cards/social-profile-card-rendering.test.tsx
files-modified:
  - src/lib/ui/referral-card-presentation.ts
  - src/lib/ui/referral-card-presentation.test.ts
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/rich-card-description-sourcing.test.ts
  - src/components/cards/social-profile-card-rendering.test.tsx
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-04
user_setup: []
objective: Add a referral-presentation seam to the shared non-payment card view model so both simple and rich URL cards can expose visible disclosure, benefit, and terms state without becoming a separate card family
must_haves:
  truths:
    - Referral presentation stays additive to the existing shared non-payment card view model rather than introducing a new referral card type.
    - The visible disclosure label is formatted from `referral.kind`, with a generic fallback such as `Referral` for soft markers that omit `kind`.
    - Referral cards can prefer `offerSummary` in the shared description slot when it exists, while non-referral description sourcing remains unchanged.
    - Benefit rows are ordered visitor-first, only include populated sides, and standardize labels without rewriting the underlying benefit text.
    - Supported social profile cards and ordinary non-referral cards keep their existing view-model semantics.
  artifacts:
    - path: src/lib/ui/referral-card-presentation.ts
      provides: pure helper logic for disclosure labels, benefit rows, offer-summary precedence, and terms presentation decisions
      min_lines: 120
    - path: src/lib/ui/referral-card-presentation.test.ts
      provides: focused unit coverage for referral-label fallback, benefit ordering, soft markers, and summary/terms decisions
      min_lines: 150
    - path: src/lib/ui/rich-card-policy.ts
      provides: shared simple/rich non-payment card view-model integration for referral presentation
      min_lines: 540
    - path: src/lib/ui/rich-card-description-sourcing.test.ts
      provides: regression coverage proving referral offer summaries do not disturb ordinary description sourcing
      min_lines: 420
    - path: src/components/cards/social-profile-card-rendering.test.tsx
      provides: view-model assertions for referral cards and non-profile supported-family referral paths
      min_lines: 920
  key_links:
    - from: src/lib/ui/referral-card-presentation.ts
      to: src/lib/ui/rich-card-policy.ts
      via: pure referral presentation decisions feeding the shared non-payment card view model
      pattern: "referral"
    - from: src/lib/ui/rich-card-policy.ts
      to: src/lib/ui/rich-card-description-sourcing.test.ts
      via: offer-summary precedence remaining referral-only
      pattern: "offerSummary"
    - from: src/lib/ui/rich-card-policy.ts
      to: src/components/cards/social-profile-card-rendering.test.tsx
      via: rich/simple referral view-model behavior staying aligned with shared card semantics
      pattern: "referral"
---

<objective>
Prepare the shared card model for referral presentation before any JSX or CSS changes happen.

Purpose: create one testable referral-presentation seam that both simple and rich non-payment cards can consume, while keeping non-referral and supported social profile behavior stable.
Output: a shared referral-presentation helper plus view-model integration for disclosure labels, benefit rows, offer-summary precedence, and terms-link state.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-referral-card-presentation-transparency-ux/20-CONTEXT.md
@.planning/phases/20-referral-card-presentation-transparency-ux/20-RESEARCH.md
@.planning/phases/19-public-referral-enrichment-offer-capture/19-VERIFICATION.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add a pure referral-presentation helper for the shared card model</name>
  <files>src/lib/ui/referral-card-presentation.ts, src/lib/ui/referral-card-presentation.test.ts, src/lib/ui/rich-card-policy.ts</files>
  <action>Create a small pure helper that converts merged `link.referral` data into card-friendly presentation state. It should format the visible disclosure label, fall back sensibly for soft markers, build visitor-first benefit rows, and decide whether `offerSummary`, `termsSummary`, and `termsUrl` should appear in the shared card model.</action>
  <verify>Run `bun test src/lib/ui/referral-card-presentation.test.ts` and `bun run typecheck`.</verify>
  <done>The repo has one explicit referral-presentation seam instead of scattering disclosure formatting logic through JSX and CSS.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Extend the shared non-payment card view model without creating a new card type</name>
  <files>src/lib/ui/rich-card-policy.ts, src/lib/ui/rich-card-description-sourcing.test.ts, src/components/cards/social-profile-card-rendering.test.tsx</files>
  <action>Wire the referral-presentation helper into the shared non-payment card view model used by both `SimpleLinkCard` and `RichLinkCard`. Keep referral treatment additive, let referral `offerSummary` take over the primary summary slot when present, and preserve existing view-model behavior for non-referral links and supported social profile cards.</action>
  <verify>Run `bun test src/lib/ui/rich-card-description-sourcing.test.ts src/components/cards/social-profile-card-rendering.test.tsx` and `bun run typecheck`.</verify>
  <done>Simple and rich cards can both describe referral state through the same view model before render work begins.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Lock view-model regressions for soft markers, one-sided benefits, and non-profile referral URLs</name>
  <files>src/lib/ui/referral-card-presentation.test.ts, src/components/cards/social-profile-card-rendering.test.tsx, src/lib/ui/rich-card-description-sourcing.test.ts</files>
  <action>Add focused tests for soft referral markers, one-sided benefits, `offerSummary` precedence, and supported profile-family referral links that intentionally stay in non-profile mode. Keep assertions centered on card model output, not final CSS treatment.</action>
  <verify>Run `bun test src/lib/ui/referral-card-presentation.test.ts src/components/cards/social-profile-card-rendering.test.tsx src/lib/ui/rich-card-description-sourcing.test.ts` and `bun run typecheck`.</verify>
  <done>Phase 20 has a stable view-model contract before badge/layout rendering starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Referral presentation is additive to the shared non-payment card view model.
- [ ] Disclosure labels format from `referral.kind` with a sensible soft-marker fallback.
- [ ] Visitor-first benefit-row ordering is centralized and only populated rows render.
- [ ] `offerSummary` precedence is limited to referral cards and does not regress ordinary description sourcing.
- [ ] Supported social profile cards and non-referral cards keep their existing view-model behavior.
</verification>

<success_criteria>
- All tasks completed.
- CARD-01 and CARD-02 are materially advanced through a stable referral-aware view-model seam.
- CARD-04 is protected early by keeping non-referral and supported-profile behavior unchanged at the model layer.
</success_criteria>

<output>
After completion, create .planning/phases/20-referral-card-presentation-transparency-ux/20-01-SUMMARY.md
</output>
