---
phase: 20-referral-card-presentation-transparency-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/referral-card-rendering.test.tsx
  - src/styles/base.css
  - src/styles/responsive.css
files-modified:
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/referral-card-rendering.test.tsx
  - src/styles/base.css
  - src/styles/responsive.css
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03
user_setup: []
objective: Render the approved referral disclosure, benefit, terms, and source/media treatment inside the shared non-payment card shell while preserving valid interaction structure and existing rich-card media behavior
must_haves:
  truths:
    - Referral cards show an explicit visible disclosure badge near the title/top of the card.
    - Benefit rows render in the text area, visitor-first, and omit empty sides instead of reserving blank slots.
    - Short `termsSummary` can render inline, while longer terms use an excerpt plus a quiet `Terms` or `Details` affordance.
    - The quiet `termsUrl` affordance avoids nested interactive markup inside the primary card anchor.
    - Promo imagery, brand icons, and source labels continue to use the current shared rich-card pipelines rather than a referral-only media system.
  artifacts:
    - path: src/components/cards/NonPaymentLinkCardShell.tsx
      provides: shared JSX structure for disclosure badges, benefit rows, inline terms copy, and quiet terms affordance
      min_lines: 390
    - path: src/components/cards/referral-card-rendering.test.tsx
      provides: focused rendering coverage for simple and rich referral cards, including terms-link placement
      min_lines: 220
    - path: src/styles/base.css
      provides: referral card sub-variant styling, badge treatment, benefit-row layout, and terms-link presentation
      min_lines: 1380
    - path: src/styles/responsive.css
      provides: mobile-safe referral badge, benefit-row, and quiet-terms-link behavior
      min_lines: 330
  key_links:
    - from: src/components/cards/NonPaymentLinkCardShell.tsx
      to: src/styles/base.css
      via: shared markup and referral sub-variant selectors staying aligned
      pattern: "referral"
    - from: src/components/cards/NonPaymentLinkCardShell.tsx
      to: src/components/cards/referral-card-rendering.test.tsx
      via: badge, benefit, and terms affordance rendering coverage
      pattern: "Terms"
    - from: src/styles/responsive.css
      to: src/components/cards/referral-card-rendering.test.tsx
      via: referral content staying scannable on mobile without overlaying into promo art
      pattern: "benefit"
---

<objective>
Turn the referral-aware card model into real UI in the shared shell.

Purpose: make referral cards visibly distinct and more transparent without forking simple/rich card markup or breaking the existing media/icon/source behavior.
Output: shared shell rendering plus CSS for disclosure badges, benefit rows, terms context, and quiet terms links.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-referral-card-presentation-transparency-ux/20-CONTEXT.md
@.planning/phases/20-referral-card-presentation-transparency-ux/20-RESEARCH.md
@.planning/phases/20-referral-card-presentation-transparency-ux/20-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Render visible referral disclosure and benefit rows in the shared shell</name>
  <files>src/components/cards/NonPaymentLinkCardShell.tsx, src/components/cards/referral-card-rendering.test.tsx, src/styles/base.css</files>
  <action>Extend the shared shell so referral cards render a noticeable but restrained disclosure badge near the title and separate benefit rows in the text content area. Keep the visitor-side benefit first, omit empty rows, and preserve the existing simple/rich lead-media behavior.</action>
  <verify>Run `bun test src/components/cards/referral-card-rendering.test.tsx` and `bun run typecheck`.</verify>
  <done>Referral cards visibly disclose themselves and surface benefit text without branching into a separate card family.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add inline terms behavior and a quiet terms affordance without nested links</name>
  <files>src/components/cards/NonPaymentLinkCardShell.tsx, src/components/cards/referral-card-rendering.test.tsx, src/styles/base.css</files>
  <action>Implement the approved terms behavior: short `termsSummary` inline by default, longer terms as an excerpt, and a quiet `Terms` or `Details` affordance whenever `termsUrl` exists. Keep the affordance outside the primary card anchor or otherwise valid so the shell does not create nested interactive content.</action>
  <verify>Run `bun test src/components/cards/referral-card-rendering.test.tsx src/components/cards/non-payment-card-accessibility.test.tsx` and `bun run typecheck`.</verify>
  <done>Referral cards can expose terms context clearly without breaking valid interaction structure.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Style referral cards as a distinct shared sub-variant across desktop and mobile</name>
  <files>src/styles/base.css, src/styles/responsive.css, src/components/cards/referral-card-rendering.test.tsx</files>
  <action>Introduce a referral-specific sub-variant styling layer that keeps promo imagery leading, brand icons supporting, and source labels readable. Keep disclosure and benefits in the text area, tune spacing/typography for desktop and mobile, and avoid ad-like or overlay-heavy treatments.</action>
  <verify>Run `bun test src/components/cards/referral-card-rendering.test.tsx` and `bun run build`.</verify>
  <done>Referral cards look intentionally different at a glance while remaining visually coherent with the existing card system.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Disclosure badge is explicit, near the title/top of the card, and visually restrained.
- [ ] Benefit rows render visitor-first in the text area and omit missing sides.
- [ ] Short terms render inline; longer terms use excerpt + quiet affordance.
- [ ] `termsUrl` does not create nested links or invalid focus order inside the main card anchor.
- [ ] Promo imagery, brand icons, and source labels still flow through the existing shared card/media system.
</verification>

<success_criteria>
- All tasks completed.
- CARD-01, CARD-02, and CARD-03 are materially advanced through shared-shell rendering and styling.
- Referral cards are transparently different without regressing the current rich/simple card family.
</success_criteria>

<output>
After completion, create .planning/phases/20-referral-card-presentation-transparency-ux/20-02-SUMMARY.md
</output>
