---
phase: 08.1-custom-profile-descriptions
plan: 03
type: execute
wave: 3
depends_on:
  - 08.1-01
  - 08.1-02
files_modified:
  - src/lib/ui/social-profile-metadata.ts
  - src/lib/ui/social-profile-metadata.test.ts
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/rich-card-description-sourcing.test.ts
  - src/components/cards/social-profile-card-rendering.test.tsx
  - packages/studio-web/src/pages/EditorPage.tsx
  - docs/studio-phase-checklist.md
  - packages/studio-web/src/lib/phase-checklist.ts
files-modified:
  - src/lib/ui/social-profile-metadata.ts
  - src/lib/ui/social-profile-metadata.test.ts
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/rich-card-description-sourcing.test.ts
  - src/components/cards/social-profile-card-rendering.test.tsx
  - packages/studio-web/src/pages/EditorPage.tsx
  - docs/studio-phase-checklist.md
  - packages/studio-web/src/lib/phase-checklist.ts
autonomous: true
requirements:
  - UI-10
  - DOC-07
user_setup: []
objective: Surface `profileDescription` intentionally in card rendering and Studio while preserving the current generic description fallback rules for non-profile links
must_haves:
  truths:
    - Social profile cards can render `profileDescription` without changing the generic non-profile description path.
    - Rich and simple profile card logic stay aligned through shared metadata helpers instead of duplicating description precedence in components.
    - Guided Studio editing exposes the new field directly and updates the Studio delivery checklist in the same batch.
    - Regression tests cover both the new profile-description path and the existing fallback path.
  artifacts:
    - path: src/lib/ui/social-profile-metadata.ts
      provides: display-ready social profile metadata including profile-authored description selection
      min_lines: 120
    - path: src/lib/ui/rich-card-policy.ts
      provides: card view-model description precedence that respects `profileDescription` only for supported profile links
      min_lines: 220
    - path: src/components/cards/social-profile-card-rendering.test.tsx
      provides: card-level regression coverage for profile-description rendering
      min_lines: 280
    - path: packages/studio-web/src/pages/EditorPage.tsx
      provides: guided Studio field editing for `profileDescription`
      min_lines: 240
    - path: docs/studio-phase-checklist.md
      provides: delivery tracking updates for the new Studio editor scope
      min_lines: 80
  key_links:
    - from: src/lib/ui/social-profile-metadata.ts
      to: src/lib/ui/rich-card-policy.ts
      via: shared profile-description selection for card view models
      pattern: "profileDescription"
    - from: src/lib/ui/rich-card-policy.ts
      to: src/components/cards/social-profile-card-rendering.test.tsx
      via: profile-card description precedence and fallback assertions
      pattern: "description"
    - from: packages/studio-web/src/pages/EditorPage.tsx
      to: docs/studio-phase-checklist.md
      via: required Studio delivery-tracking updates
      pattern: "profileDescription"
    - from: docs/studio-phase-checklist.md
      to: packages/studio-web/src/lib/phase-checklist.ts
      via: synchronized in-app roadmap data for Studio work
      pattern: "Phase 4"
---

<objective>
Finish Phase 08.1 by making the new field visible to maintainers and users.

Purpose: Ensure `profileDescription` changes card behavior only where it should and can be edited without dropping into raw JSON.
Output: Runtime description-selection updates, card regression coverage, and guided Studio support for the new field.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.1-custom-profile-descriptions/08.1-CONTEXT.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend shared social-profile resolution to carry display-ready `profileDescription`</name>
  <files>src/lib/ui/social-profile-metadata.ts, src/lib/ui/social-profile-metadata.test.ts, src/lib/ui/rich-card-policy.ts</files>
  <action>Add `profileDescription` to the shared social-profile view model and make card policy choose it only for supported profile links. Preserve the current `resolveLinkCardDescription` behavior for non-profile content and keep description precedence centralized instead of scattering it across JSX.</action>
  <verify>Run `bun test src/lib/ui/social-profile-metadata.test.ts src/lib/ui/rich-card-description-sourcing.test.ts` and `bun run typecheck`.</verify>
  <done>The runtime can intentionally prefer the profile-authored description for social profile cards without mutating the generic description contract.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add card-level regression coverage for profile-description rendering and fallback behavior</name>
  <files>src/components/cards/social-profile-card-rendering.test.tsx, src/lib/ui/rich-card-description-sourcing.test.ts</files>
  <action>Add tests that prove X and at least one additional supported profile render `profileDescription` when present, while a non-profile rich card and a profile card without `profileDescription` both keep the current fallback behavior.</action>
  <verify>Run `bun test src/components/cards/social-profile-card-rendering.test.tsx src/lib/ui/rich-card-description-sourcing.test.ts` and `bun run build`.</verify>
  <done>Phase 08.1 has a regression net for both the new behavior and the existing fallback path.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Expose `profileDescription` in Studio and update delivery tracking</name>
  <files>packages/studio-web/src/pages/EditorPage.tsx, docs/studio-phase-checklist.md, packages/studio-web/src/lib/phase-checklist.ts</files>
  <action>Add a guided editor field for `links[].metadata.profileDescription` on rich profile links, keep the UX consistent with the current lightweight editor approach, and update both Studio tracking artifacts in the same change batch as required by repo policy.</action>
  <verify>Run `bun run studio:lint`, `bun run studio:typecheck`, `bun run studio:test:integration`, and `bun run build`.</verify>
  <done>Maintainers can edit the new field directly in Studio, and Studio delivery tracking stays synchronized with the implementation scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Supported profile cards can render `profileDescription` when present.
- [ ] Non-profile links and profile links without `profileDescription` keep the existing description fallback path.
- [ ] Shared helpers, not components, own the new precedence logic.
- [ ] Studio exposes the new field and its tracking artifacts are updated together.
</verification>

<success_criteria>
- All tasks completed.
- UI-10 and DOC-07 are satisfied for the runtime and Studio surfaces.
- The repo is ready for the broader docs and regression-hardening pass in Phase 9.
</success_criteria>

<output>
After completion, create .planning/phases/08.1-custom-profile-descriptions/08.1-03-SUMMARY.md
</output>
