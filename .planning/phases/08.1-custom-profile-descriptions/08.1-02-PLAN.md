---
phase: 08.1-custom-profile-descriptions
plan: 02
type: execute
wave: 2
depends_on:
  - 08.1-01
files_modified:
  - scripts/enrich-rich-links.ts
  - scripts/public-rich-sync.ts
  - scripts/public-rich-sync.test.ts
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/enrichment/supported-social-profile-metadata.ts
  - scripts/enrichment/supported-social-profile-metadata.test.ts
  - skills/create-new-rich-content-extractor/SKILL.md
  - data/cache/rich-public-cache.json
  - data/cache/rich-authenticated-cache.json
files-modified:
  - scripts/enrich-rich-links.ts
  - scripts/public-rich-sync.ts
  - scripts/public-rich-sync.test.ts
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/enrichment/supported-social-profile-metadata.ts
  - scripts/enrichment/supported-social-profile-metadata.test.ts
  - skills/create-new-rich-content-extractor/SKILL.md
  - data/cache/rich-public-cache.json
  - data/cache/rich-authenticated-cache.json
autonomous: true
requirements:
  - DATA-11
  - DOC-07
user_setup: []
objective: Audit supported platform capture paths so `profileDescription` is persisted when a distinct profile-authored bio exists, and update repo-local extractor guidance accordingly
must_haves:
  truths:
    - Supported public and authenticated profile sources are audited instead of assuming every platform exposes a distinct profile description.
    - X is validated first because its current public metadata description is generic rather than profile-authored.
    - Public augmentation remains preferred whenever a stable public source exists; authenticated paths are only updated where the repo already depends on them.
    - Repo-local extractor guidance explicitly asks contributors to capture both page description and profile description when applicable.
  artifacts:
    - path: scripts/public-rich-sync.ts
      provides: public-cache refresh path that can persist `profileDescription`
      min_lines: 240
    - path: scripts/enrich-rich-links.ts
      provides: generated metadata pipeline that preserves `profileDescription`
      min_lines: 420
    - path: scripts/enrichment/public-augmentation.ts
      provides: platform-specific public parsing rules for profile-authored descriptions
      min_lines: 300
    - path: scripts/enrichment/supported-social-profile-metadata.ts
      provides: platform normalization helpers for supported profile metadata
      min_lines: 80
    - path: skills/create-new-rich-content-extractor/SKILL.md
      provides: extractor authoring instructions that distinguish page metadata from profile-authored descriptions
      min_lines: 120
  key_links:
    - from: scripts/enrichment/public-augmentation.ts
      to: scripts/public-rich-sync.ts
      via: public-source parsing and committed cache refresh for supported profiles
      pattern: "profileDescription"
    - from: scripts/enrich-rich-links.ts
      to: data/cache/rich-public-cache.json
      via: generated metadata fed from committed public cache entries
      pattern: "profileDescription"
    - from: scripts/enrich-rich-links.ts
      to: data/cache/rich-authenticated-cache.json
      via: generated metadata fed from authenticated cache entries
      pattern: "profileDescription"
    - from: skills/create-new-rich-content-extractor/SKILL.md
      to: scripts/enrichment/public-augmentation.ts
      via: contributor workflow for choosing and capturing the correct description surface
      pattern: "description"
---

<objective>
Implement the platform audit and metadata-capture half of Phase 08.1.

Purpose: Make current supported profile links persist a distinct `profileDescription` when the platform actually exposes one.
Output: Updated enrichment/public-sync logic, refreshed cache data where needed, and extractor guidance that encodes the distinction for future work.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.1-custom-profile-descriptions/08.1-CONTEXT.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Audit the current supported profile platforms and choose the right description surface per platform</name>
  <files>scripts/enrichment/public-augmentation.ts, scripts/enrichment/supported-social-profile-metadata.ts, data/cache/rich-public-cache.json, data/cache/rich-authenticated-cache.json</files>
  <action>Review the current supported profile set (Primal, GitHub, X, LinkedIn, Facebook, Instagram, YouTube, Medium, Substack) and determine whether each exposes a distinct profile-authored description. Add or update parsing only when the platform truly provides a second description surface, and leave single-surface platforms unchanged.</action>
  <verify>Capture the audit outcome in code comments or normalization logic where necessary, then run `bun run enrich:rich:strict` and inspect the generated metadata for X plus at least two additional supported platforms.</verify>
  <done>The repo knows which current platforms can populate `profileDescription` and which should continue using only the existing `description` field.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Update public and authenticated cache ingestion to persist `profileDescription`</name>
  <files>scripts/public-rich-sync.ts, scripts/public-rich-sync.test.ts, scripts/enrich-rich-links.ts, data/cache/rich-public-cache.json, data/cache/rich-authenticated-cache.json</files>
  <action>Wire the new field through public sync and enrichment ingestion so committed cache entries and generated metadata preserve `profileDescription`. Refresh affected cache fixtures or checked-in data for the currently seeded links when the source actually exposes a distinct profile-authored description.</action>
  <verify>Run `bun run public:rich:sync -- --only-link x`, `bun run enrich:rich:strict`, and `bun run validate:data`. If authenticated cache updates are required for existing supported extractors, follow the repo's explicit auth workflow rules and stop for user choice before any blocker or policy mutation.</verify>
  <done>Generated rich metadata can carry both `description` and `profileDescription` from the repo's supported public or authenticated paths.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Update extractor guidance and tests so the distinction is durable</name>
  <files>skills/create-new-rich-content-extractor/SKILL.md, scripts/enrichment/public-augmentation.test.ts, scripts/enrichment/supported-social-profile-metadata.test.ts</files>
  <action>Teach the repo-local extractor workflow to ask for both page description and profile description when applicable, and add regression coverage proving the new capture behavior for X and at least one additional supported platform.</action>
  <verify>Run `bun test scripts/public-rich-sync.test.ts scripts/enrichment/public-augmentation.test.ts scripts/enrichment/supported-social-profile-metadata.test.ts` and `bun run build`.</verify>
  <done>Future platform work will not collapse profile-authored descriptions back into the generic description slot.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] X persists a distinct `profileDescription` if the public profile exposes one.
- [ ] At least one additional currently supported platform is proven to either persist `profileDescription` or intentionally remain single-surface.
- [ ] Generated metadata preserves both description fields when cache data provides them.
- [ ] Repo-local extractor guidance explicitly distinguishes page description from profile-authored description.
</verification>

<success_criteria>
- All tasks completed.
- DATA-11 is satisfied for the currently supported profile platforms.
- The repo's future extractor workflow will not regress this distinction.
</success_criteria>

<output>
After completion, create .planning/phases/08.1-custom-profile-descriptions/08.1-02-SUMMARY.md
</output>
