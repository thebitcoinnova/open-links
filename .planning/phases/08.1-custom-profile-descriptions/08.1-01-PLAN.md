---
phase: 08.1-custom-profile-descriptions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/links.schema.json
  - schema/rich-public-cache.schema.json
  - schema/rich-authenticated-cache.schema.json
  - src/lib/content/social-profile-fields.ts
  - src/lib/content/social-profile-fields.test.ts
  - src/lib/content/load-content.ts
  - scripts/enrichment/types.ts
  - scripts/enrichment/public-cache.ts
  - scripts/authenticated-extractors/types.ts
files-modified:
  - schema/links.schema.json
  - schema/rich-public-cache.schema.json
  - schema/rich-authenticated-cache.schema.json
  - src/lib/content/social-profile-fields.ts
  - src/lib/content/social-profile-fields.test.ts
  - src/lib/content/load-content.ts
  - scripts/enrichment/types.ts
  - scripts/enrichment/public-cache.ts
  - scripts/authenticated-extractors/types.ts
autonomous: true
requirements:
  - DATA-10
  - DATA-11
user_setup: []
objective: Define a first-class `profileDescription` contract and field-level merge semantics across manual, public, generated, and authenticated metadata
must_haves:
  truths:
    - `profileDescription` exists as a distinct optional field alongside the current `description` field.
    - Manual `metadata.profileDescription` overrides generated or cached values without replacing unrelated metadata fields.
    - Existing `description` and `descriptionSource` behavior for non-profile links remains unchanged.
    - Current links and caches without `profileDescription` stay valid and loadable.
  artifacts:
    - path: schema/links.schema.json
      provides: manual link metadata contract for `profileDescription`
      min_lines: 190
    - path: schema/rich-public-cache.schema.json
      provides: committed public cache contract for `profileDescription`
      min_lines: 95
    - path: schema/rich-authenticated-cache.schema.json
      provides: authenticated cache contract for `profileDescription`
      min_lines: 150
    - path: src/lib/content/social-profile-fields.ts
      provides: field lists and merge semantics covering `profileDescription`
      min_lines: 110
    - path: src/lib/content/load-content.ts
      provides: runtime link metadata types carrying `profileDescription`
      min_lines: 120
  key_links:
    - from: schema/links.schema.json
      to: src/lib/content/load-content.ts
      via: RichLinkMetadata contract parity for manual link metadata
      pattern: "profileDescription"
    - from: schema/rich-public-cache.schema.json
      to: scripts/enrichment/public-cache.ts
      via: public cache registry type and normalization support
      pattern: "profileDescription"
    - from: schema/rich-authenticated-cache.schema.json
      to: scripts/authenticated-extractors/types.ts
      via: authenticated cache metadata surface for profile-authored descriptions
      pattern: "profileDescription"
    - from: src/lib/content/social-profile-fields.ts
      to: scripts/enrichment/types.ts
      via: shared social profile field list reused by generated metadata typing
      pattern: "profileDescription"
---

<objective>
Establish the Phase 08.1 metadata contract before any platform-specific audit or UI work.

Purpose: Make profile-authored descriptions storable without overloading the existing generic `description` path.
Output: Schema/type updates, override semantics, and regression tests proving the new field is additive.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.1-custom-profile-descriptions/08.1-CONTEXT.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend manual, public, generated, and authenticated metadata shapes</name>
  <files>schema/links.schema.json, schema/rich-public-cache.schema.json, schema/rich-authenticated-cache.schema.json, src/lib/content/load-content.ts, scripts/enrichment/types.ts, scripts/authenticated-extractors/types.ts, scripts/enrichment/public-cache.ts</files>
  <action>Add `profileDescription` everywhere the repo currently models `description` for rich profile metadata. Keep it optional, preserve existing required-field behavior for generic `description`, and avoid forcing the new field onto non-profile or non-rich links.</action>
  <verify>Run `bun run typecheck` and `bun run validate:data` to confirm the new field is accepted without breaking existing content.</verify>
  <done>Manual links, generated metadata, public cache, and authenticated cache can all carry `profileDescription` without schema drift.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Extend merge and social-profile helper semantics for `profileDescription`</name>
  <files>src/lib/content/social-profile-fields.ts, src/lib/content/load-content.ts, scripts/enrichment/types.ts</files>
  <action>Update the shared social-profile field list and merge helpers so manual `metadata.profileDescription` wins field-by-field over generated or cached values, matching the repo's existing profile-field override posture.</action>
  <verify>Add focused automated coverage in `src/lib/content/social-profile-fields.test.ts`, then run `bun test src/lib/content/social-profile-fields.test.ts` and `bun run typecheck`.</verify>
  <done>The new field behaves predictably with the existing override model instead of introducing special-case merge rules later.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Prove the new field is additive rather than disruptive</name>
  <files>src/lib/content/social-profile-fields.test.ts, schema/links.schema.json, scripts/enrichment/public-cache.ts</files>
  <action>Add regression tests that demonstrate links and caches without `profileDescription` still validate and that the new field does not change generic `description` or `descriptionSource` behavior for non-profile content.</action>
  <verify>Run `bun test src/lib/content/social-profile-fields.test.ts`, `bun run validate:data`, and `bun run build`.</verify>
  <done>Phase 08.1 has a stable contract foundation before platform audits and UI changes begin.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `profileDescription` is accepted in manual link metadata, public cache, authenticated cache, and generated metadata.
- [ ] Manual `metadata.profileDescription` overrides generated or cached values without replacing unrelated fields.
- [ ] Existing `description` and `descriptionSource` semantics remain unchanged for non-profile links.
- [ ] Existing links and cache manifests without `profileDescription` remain valid.
</verification>

<success_criteria>
- All tasks completed.
- DATA-10 and the contract half of DATA-11 are established before capture or UI work begins.
- The repo has one coherent field name and merge model for profile-authored descriptions.
</success_criteria>

<output>
After completion, create .planning/phases/08.1-custom-profile-descriptions/08.1-01-SUMMARY.md
</output>
