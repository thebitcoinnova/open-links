---
phase: 10-configurable-rich-card-description-image-row
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/site.schema.json
  - src/lib/content/load-content.ts
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/rich-card-description-sourcing.test.ts
  - src/components/cards/social-profile-card-rendering.test.tsx
files-modified:
  - schema/site.schema.json
  - src/lib/content/load-content.ts
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/rich-card-description-sourcing.test.ts
  - src/components/cards/social-profile-card-rendering.test.tsx
autonomous: true
requirements:
  - UI-11
user_setup: []
objective: Define the global and per-site rich-card description-image-row policy plus shared view-model fields before touching parser or JSX layout code
must_haves:
  truths:
    - The extra description-image row resolves from one shared policy layer, not component-local conditionals.
    - Maintainers can disable the row globally and re-enable or disable it per site/platform without editing individual links.
    - The default behavior is additive: only rich profile cards with a distinct preview image become eligible for the row.
    - Legacy `mobile.imageLayout` remains a separate backward-compatibility surface and is not reused for this new behavior.
  artifacts:
    - path: schema/site.schema.json
      provides: `site.ui.richCards.descriptionImageRow` contract with global and per-site policy fields
      min_lines: 300
    - path: src/lib/content/load-content.ts
      provides: runtime TypeScript types for the new policy surface and rich-card row mode
      min_lines: 240
    - path: src/lib/ui/rich-card-policy.ts
      provides: resolved row visibility/image selection and override precedence
      min_lines: 260
    - path: src/lib/ui/rich-card-description-sourcing.test.ts
      provides: policy-precedence coverage for global defaults and per-site overrides
      min_lines: 170
    - path: src/components/cards/social-profile-card-rendering.test.tsx
      provides: view-model level assertions for description-image eligibility before markup changes land
      min_lines: 420
  key_links:
    - from: schema/site.schema.json
      to: src/lib/content/load-content.ts
      via: rich-card policy contract parity
      pattern: "descriptionImageRow"
    - from: src/lib/ui/rich-card-policy.ts
      to: src/lib/icons/known-sites-data.ts
      via: per-site override resolution using known-site ids and domains
      pattern: "resolveKnownSite"
    - from: src/lib/ui/rich-card-policy.ts
      to: src/components/cards/social-profile-card-rendering.test.tsx
      via: resolved view-model fields for the new row
      pattern: "descriptionImage"
    - from: src/lib/ui/rich-card-policy.ts
      to: src/lib/ui/rich-card-description-sourcing.test.ts
      via: shared precedence logic for global and per-site policy
      pattern: "descriptionImageRow"
---

<objective>
Establish the Phase 10 policy contract before parser changes or UI markup changes begin.

Purpose: Make the new row configurable in a durable way instead of smuggling behavior through one-off component flags.
Output: Schema/type updates, centralized policy resolution, and tests that prove global-plus-per-site precedence.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-configurable-rich-card-description-image-row/10-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add a dedicated rich-card description-image-row policy surface to the site contract</name>
  <files>schema/site.schema.json, src/lib/content/load-content.ts</files>
  <action>Add a new `site.ui.richCards.descriptionImageRow` object that supports a global default plus per-site overrides. Keep the values simple (`auto` vs `off` unless the implementation proves a stronger mode is necessary), and model them in TypeScript so the rest of the runtime can treat the policy as first-class config.</action>
  <verify>Run `bun run typecheck` and `bun run validate:data` to confirm the new config surface is accepted without disturbing existing site files.</verify>
  <done>The repo has one explicit place to configure the extra row globally and per site.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Resolve global and per-site row policy in the shared rich-card policy layer</name>
  <files>src/lib/ui/rich-card-policy.ts, src/lib/content/load-content.ts</files>
  <action>Implement a shared resolver that uses known-site ids first and normalized host fallbacks second to determine whether a rich profile card should render the extra description-image row. Expose the result on the rich-card view model as dedicated fields (for example a boolean plus the resolved row image URL) instead of overloading `leadKind`.</action>
  <verify>Run `bun test src/lib/ui/rich-card-description-sourcing.test.ts src/components/cards/social-profile-card-rendering.test.tsx` and `bun run typecheck`.</verify>
  <done>Components can consume one view-model decision instead of duplicating policy matching logic.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Lock in precedence and duplicate-image suppression before the renderer changes</name>
  <files>src/lib/ui/rich-card-description-sourcing.test.ts, src/components/cards/social-profile-card-rendering.test.tsx</files>
  <action>Add tests that prove: the row defaults to `auto`, a global `off` policy suppresses it, a per-site override can re-enable or disable it for Substack-style cards, and cards without a distinct preview image never become row-eligible even when the policy is `auto`.</action>
  <verify>Run `bun test src/lib/ui/rich-card-description-sourcing.test.ts src/components/cards/social-profile-card-rendering.test.tsx` and `bun run build`.</verify>
  <done>Phase 10 starts with stable policy semantics instead of letting markup decisions define behavior by accident.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `site.ui.richCards.descriptionImageRow` supports a global default and per-site overrides.
- [ ] Rich-card policy resolves overrides from stable site/platform identifiers rather than ad hoc raw strings.
- [ ] Cards without a distinct preview image never expose the extra row through the view model.
- [ ] Existing simple cards and non-profile rich cards keep their current policy defaults.
</verification>

<success_criteria>
- All tasks completed.
- The repo has a centralized, testable policy contract for the extra description-image row.
- UI-11 groundwork is in place before parser, validation, or shell changes begin.
</success_criteria>

<output>
After completion, create .planning/phases/10-configurable-rich-card-description-image-row/10-01-SUMMARY.md
</output>
