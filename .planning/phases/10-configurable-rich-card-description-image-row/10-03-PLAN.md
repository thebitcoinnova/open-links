---
phase: 10-configurable-rich-card-description-image-row
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/RichLinkCard.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/cards/social-profile-card-rendering.test.tsx
  - src/components/cards/non-payment-card-accessibility.test.tsx
  - docs/data-model.md
  - docs/customization-catalog.md
files-modified:
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/RichLinkCard.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/cards/social-profile-card-rendering.test.tsx
  - src/components/cards/non-payment-card-accessibility.test.tsx
  - docs/data-model.md
  - docs/customization-catalog.md
autonomous: true
requirements:
  - UI-11
  - DOC-06
  - QUAL-06
user_setup: []
objective: Render the new full-width description-image row in rich profile cards, document the new policy surface, and lock down accessibility/regression coverage
must_haves:
  truths:
    - Rich profile cards keep the avatar/header row first, the description row second, and place the new media row after the description instead of replacing the lead slot.
    - The row is rich-only and only appears when the shared policy layer resolves it on.
    - Footer/source context remains legible after the new row is inserted and accessibility semantics stay intact.
    - Maintainer docs explain both the global default and the per-site override path with concrete examples.
  artifacts:
    - path: src/components/cards/NonPaymentLinkCardShell.tsx
      provides: shared markup support for the new full-width description-image row
      min_lines: 140
    - path: src/styles/base.css
      provides: grid areas and media styling for the new row
      min_lines: 560
    - path: src/styles/responsive.css
      provides: breakpoint behavior that preserves the requested row order on mobile and desktop
      min_lines: 150
    - path: src/components/cards/social-profile-card-rendering.test.tsx
      provides: rich-card regression coverage for row ordering and per-site overrides
      min_lines: 470
    - path: docs/data-model.md
      provides: maintainer-facing contract docs for the new policy surface
      min_lines: 500
    - path: docs/customization-catalog.md
      provides: concise customization guidance and copy-paste examples
      min_lines: 210
  key_links:
    - from: src/lib/ui/rich-card-policy.ts
      to: src/components/cards/NonPaymentLinkCardShell.tsx
      via: row visibility/image selection consumed by the shared shell
      pattern: "descriptionImage"
    - from: src/components/cards/NonPaymentLinkCardShell.tsx
      to: src/styles/base.css
      via: new grid area and media-row class names
      pattern: "description-image"
    - from: src/components/cards/social-profile-card-rendering.test.tsx
      to: src/components/cards/non-payment-card-accessibility.test.tsx
      via: regression and accessibility coverage for the inserted row
      pattern: "rich"
    - from: docs/data-model.md
      to: docs/customization-catalog.md
      via: contract reference plus practical override examples
      pattern: "descriptionImageRow"
---

<objective>
Ship the visible Phase 10 behavior after the policy and data groundwork is in place.

Purpose: Give rich profile cards the requested three-step reading order without regressing accessibility, fallback states, or maintainer clarity.
Output: Shared shell/CSS updates, accessibility/regression coverage, and docs for global/per-site configuration.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-configurable-rich-card-description-image-row/10-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add the rich-only full-width description-image row to the shared non-payment shell</name>
  <files>src/components/cards/NonPaymentLinkCardShell.tsx, src/components/cards/RichLinkCard.tsx, src/styles/base.css, src/styles/responsive.css</files>
  <action>Update the shared shell so rich profile cards can render a dedicated full-width media row after the description and before the footer/source row when the view model resolves one. Preserve the existing avatar-first header and keep non-profile/simple cards on their current layouts.</action>
  <verify>Run `bun test src/components/cards/social-profile-card-rendering.test.tsx src/components/cards/non-payment-card-accessibility.test.tsx` and `bun run build`.</verify>
  <done>The runtime matches the requested row order without forking the card component tree.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add regression and accessibility coverage for global defaults, per-site overrides, and fallback states</name>
  <files>src/components/cards/social-profile-card-rendering.test.tsx, src/components/cards/non-payment-card-accessibility.test.tsx</files>
  <action>Add tests that prove: a Substack-style card with distinct images renders the new row, a global `off` policy suppresses it, a per-site override can suppress it for one site while leaving others on, and non-profile rich cards plus simple cards keep their current behavior. Include accessibility assertions for the extra row’s reading order and decorative-media treatment.</action>
  <verify>Run `bun test src/components/cards/social-profile-card-rendering.test.tsx src/components/cards/non-payment-card-accessibility.test.tsx` and `bun run typecheck`.</verify>
  <done>The new row has a regression net for both behavior and semantics.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Document the new rich-card media-row policy for maintainers</name>
  <files>docs/data-model.md, docs/customization-catalog.md</files>
  <action>Document the new `site.ui.richCards.descriptionImageRow` policy, explain the global default plus per-site override precedence, and show at least one concrete example for disabling the row globally and another for disabling or enabling it per site.</action>
  <verify>Run `bun run build` and review the rendered docs copy for consistency with the implemented config names and precedence.</verify>
  <done>Maintainers can use the new behavior without reading code or guessing the override key format.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Rich profile cards with distinct preview/profile images render the new full-width row after the description.
- [ ] Global and per-site `off` policies suppress the row without disturbing the avatar/header treatment.
- [ ] Non-profile rich cards and simple cards keep their current rendered structure.
- [ ] Accessibility semantics remain intact and maintainer docs explain the new config clearly.
</verification>

<success_criteria>
- All tasks completed.
- UI-11 is materially advanced through a reusable rich-card policy + shared-shell extension.
- DOC-06 and QUAL-06 are extended to cover the new media-row customization surface.
</success_criteria>

<output>
After completion, create .planning/phases/10-configurable-rich-card-description-image-row/10-03-SUMMARY.md
</output>
