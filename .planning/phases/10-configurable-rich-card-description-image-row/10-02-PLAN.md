---
phase: 10-configurable-rich-card-description-image-row
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/public-rich-sync.test.ts
  - scripts/validate-data.ts
  - src/lib/ui/social-profile-metadata.test.ts
  - data/cache/rich-public-cache.json
files-modified:
  - scripts/enrichment/public-augmentation.ts
  - scripts/enrichment/public-augmentation.test.ts
  - scripts/public-rich-sync.test.ts
  - scripts/validate-data.ts
  - src/lib/ui/social-profile-metadata.test.ts
  - data/cache/rich-public-cache.json
autonomous: true
requirements:
  - UI-11
  - QUAL-06
user_setup: []
objective: Preserve meaningful preview-vs-profile image separation for supported profile sources and align validation with the new optional rich-card image row
must_haves:
  truths:
    - Supported profile sources keep `profileImage` for identity and only preserve a separate `image` when that second image is actually useful for the new row.
    - Substack is audited first because the current parser/cache path collapses the two image roles and would otherwise leave the feature dormant.
    - Rich-card validation only requires preview media when the runtime genuinely needs it, instead of forcing unused images for avatar-first profile cards.
    - Existing non-profile rich cards keep their current preview-image quality guarantees.
  artifacts:
    - path: scripts/enrichment/public-augmentation.ts
      provides: source-specific parsing rules that can preserve distinct preview and profile images
      min_lines: 700
    - path: scripts/enrichment/public-augmentation.test.ts
      provides: parser coverage for Substack and other supported profile media-role decisions
      min_lines: 260
    - path: scripts/validate-data.ts
      provides: preview-image validation aligned to rendered rich-card surfaces
      min_lines: 420
    - path: src/lib/ui/social-profile-metadata.test.ts
      provides: runtime proof that distinct images become visible to the policy layer
      min_lines: 320
    - path: data/cache/rich-public-cache.json
      provides: seeded public-cache examples with corrected media-role separation where applicable
      min_lines: 1
  key_links:
    - from: scripts/enrichment/public-augmentation.ts
      to: data/cache/rich-public-cache.json
      via: committed public metadata preserving distinct preview/profile images
      pattern: "\"profileImage\""
    - from: data/cache/rich-public-cache.json
      to: src/lib/ui/social-profile-metadata.test.ts
      via: runtime `hasDistinctPreviewImage` behavior for seeded profile links
      pattern: "substack"
    - from: scripts/validate-data.ts
      to: src/lib/ui/rich-card-policy.ts
      via: preview-image requirements matching the resolved runtime layout policy
      pattern: "rich"
---

<objective>
Make the Phase 10 renderer meaningful by preserving the right image roles and by removing obsolete preview-image requirements.

Purpose: Ensure the extra row can actually render for supported profile sources without forcing unused preview media on cards that no longer depend on it.
Output: Parser/cache updates, targeted fixture refresh, and validation rules aligned to avatar-first profile cards.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-configurable-rich-card-description-image-row/10-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Audit supported profile sources and preserve a second image only where it improves the new row</name>
  <files>scripts/enrichment/public-augmentation.ts, scripts/enrichment/public-augmentation.test.ts, src/lib/ui/social-profile-metadata.test.ts</files>
  <action>Review the currently supported avatar-first profile sources, starting with Substack, and decide which ones should preserve a distinct preview/description image alongside `profileImage`. Update parser logic only where the second image is meaningful for the new row; keep intentionally single-image platforms unchanged.</action>
  <verify>Run `bun test scripts/enrichment/public-augmentation.test.ts src/lib/ui/social-profile-metadata.test.ts` and inspect Substack plus at least one other supported profile source for distinct-image behavior.</verify>
  <done>The policy layer receives the right raw material instead of a blanket image collapse.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Refresh seeded public metadata and sync-path coverage for distinct image roles</name>
  <files>data/cache/rich-public-cache.json, scripts/public-rich-sync.test.ts, scripts/enrichment/public-augmentation.ts</files>
  <action>Refresh the committed public cache or sync fixtures so supported profile links that truly have a second image preserve both `image` and `profileImage`. Add or update sync-path coverage so future refreshes do not regress back to one image when the parser knows better.</action>
  <verify>Run `bun test scripts/public-rich-sync.test.ts`, `bun run enrich:rich:strict`, and `bun run validate:data`. If a blocker or authenticated-path incident appears, stop and follow the repo’s required blocker workflow instead of mutating policy ad hoc.</verify>
  <done>The repo’s checked-in data and sync path both preserve meaningful media-role separation.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Align preview-image validation with the actual rendered rich-card surfaces</name>
  <files>scripts/validate-data.ts, src/lib/ui/social-profile-metadata.test.ts</files>
  <action>Update validation so non-profile rich cards still require preview media, while avatar-first profile cards only require the fields their configured runtime actually uses. Do not silently weaken unrelated rich-card quality checks.</action>
  <verify>Run `bun run validate:data`, `bun run enrich:rich:strict`, and `bun run build`.</verify>
  <done>Maintainers can turn the extra row off without being forced to keep unused preview images just to satisfy stale validation logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Substack or another supported profile source can preserve both `profileImage` and `image` when they serve different roles.
- [ ] The seeded public cache demonstrates at least one distinct-image profile example or an explicit intentional single-image decision.
- [ ] Non-profile rich cards still fail validation when preview media is missing.
- [ ] Avatar-first profile cards no longer fail validation solely because an optional description-image row is disabled or unavailable.
</verification>

<success_criteria>
- All tasks completed.
- The Phase 10 UI work has real distinct-media inputs to consume.
- QUAL-06-aligned validation remains strong for the surfaces that still depend on preview media.
</success_criteria>

<output>
After completion, create .planning/phases/10-configurable-rich-card-description-image-row/10-02-SUMMARY.md
</output>
