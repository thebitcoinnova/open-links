---
phase: 11-historical-follower-tracking-growth-charts
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - package.json
  - bun.lock
  - src/lib/analytics/follower-history.ts
  - src/lib/icons/custom-icons.tsx
  - src/components/analytics/FollowerHistoryChart.tsx
  - src/components/analytics/FollowerHistoryModal.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/profile/ProfileHeader.tsx
  - src/routes/index.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/cards/social-profile-card-rendering.test.tsx
  - src/components/cards/non-payment-card-accessibility.test.tsx
files-modified:
  - package.json
  - bun.lock
  - src/lib/analytics/follower-history.ts
  - src/lib/icons/custom-icons.tsx
  - src/components/analytics/FollowerHistoryChart.tsx
  - src/components/analytics/FollowerHistoryModal.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/profile/ProfileHeader.tsx
  - src/routes/index.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/cards/social-profile-card-rendering.test.tsx
  - src/components/cards/non-payment-card-accessibility.test.tsx
autonomous: true
requirements: []
user_setup: []
objective: Build the minimal follower-growth analytics surface with a dedicated all-platform view, card-level modal entry points, and a Solid-friendly chart integration that respects the locked comparison rules
must_haves:
  truths:
    - The all-platform analytics surface is reachable from a new stats or analytics button immediately left of `Share` in the profile header.
    - Cards with available history data expose a clear analytics icon that opens a platform-specific chart view without disrupting the current card reading order.
    - The first shipped analytics experience stays minimal, defaults to the last 30 days, and offers switchable time-range controls plus a raw-count versus growth-rate toggle for single-platform views.
    - Cross-platform charts remain mostly separate unless the chosen chart implementation can clearly support independent y-axes.
    - The analytics surface uses query/hash state and existing modal patterns rather than forcing a broader router migration into the public app.
  artifacts:
    - path: package.json
      provides: `echarts` as the selected charting dependency for the public analytics surface
      min_lines: 80
    - path: src/components/analytics/FollowerHistoryChart.tsx
      provides: a Solid wrapper around the selected charting library with resize and cleanup handling
      min_lines: 180
    - path: src/components/analytics/FollowerHistoryModal.tsx
      provides: the card-level platform analytics modal using the repo's established dialog/backdrop posture
      min_lines: 160
    - path: src/components/profile/ProfileHeader.tsx
      provides: a dedicated all-platform analytics button positioned immediately left of `Share`
      min_lines: 160
    - path: src/components/cards/NonPaymentLinkCardShell.tsx
      provides: analytics icon affordance for cards whose links have history data
      min_lines: 220
    - path: src/routes/index.tsx
      provides: analytics state routing, all-platform page rendering, and history-data fetch orchestration
      min_lines: 320
    - path: src/styles/base.css
      provides: minimal analytics page, modal, icon-button, and chart-container styling
      min_lines: 1050
  key_links:
    - from: src/lib/analytics/follower-history.ts
      to: src/components/analytics/FollowerHistoryChart.tsx
      via: shared raw-count and growth-rate series shaping for the chart layer
      pattern: "growth"
    - from: src/components/profile/ProfileHeader.tsx
      to: src/routes/index.tsx
      via: query/hash-driven entry into the dedicated all-platform analytics surface
      pattern: "analytics"
    - from: src/components/cards/NonPaymentLinkCardShell.tsx
      to: src/components/analytics/FollowerHistoryModal.tsx
      via: platform-specific card-level analytics entry
      pattern: "analytics"
    - from: src/components/analytics/FollowerHistoryChart.tsx
      to: package.json
      via: the selected ECharts dependency and Solid wrapper lifecycle
      pattern: "echarts"
---

<objective>
Ship the user-visible Phase 11 analytics experience after the data contract and publication path exist.

Purpose: Let visitors and the site owner inspect follower growth from both the profile header and individual platform cards without introducing a heavy dashboard or a public-app router rewrite.
Output: A selected chart library, a dedicated all-platform analytics surface, a platform modal, and regression coverage for the new entry points and accessibility semantics.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-historical-follower-tracking-growth-charts/11-CONTEXT.md
@.planning/phases/11-historical-follower-tracking-growth-charts/11-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add the selected charting stack and build one reusable follower-history chart component</name>
  <files>package.json, bun.lock, src/lib/analytics/follower-history.ts, src/components/analytics/FollowerHistoryChart.tsx</files>
  <action>Install `echarts` and create one Solid wrapper component that renders follower-history time series, handles mount/update/dispose correctly, and can show both raw-count and growth-rate views from the shared analytics data layer. Keep the configuration minimal and legible, and treat multi-axis combined charts as optional enhancement rather than the default rendering path.</action>
  <verify>Run `bun install`, `bun run typecheck`, and focused analytics-data tests such as `bun test src/lib/analytics/follower-history.test.ts`.</verify>
  <done>The repo has one chart primitive that both the full analytics page and platform modal can reuse.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add the all-platform analytics page entry from the profile header</name>
  <files>src/components/profile/ProfileHeader.tsx, src/routes/index.tsx, src/styles/base.css, src/styles/responsive.css, src/lib/icons/custom-icons.tsx</files>
  <action>Add a stats or analytics button immediately left of the existing `Share` button in the profile header. Use query-string or hash state to switch the public app into a dedicated analytics view that defaults to the last 30 days, uses sleek range controls, and renders separate per-platform charts as the primary comparison mode. Only add a combined chart if the final ECharts implementation can show independent y-axes clearly.</action>
  <verify>Run `bun run build` and manually inspect desktop/mobile layout for the profile-header actions and the full analytics surface.</verify>
  <done>The app has a dedicated all-platform growth view without forcing a router migration.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add card-level analytics entry and modal behavior with focused regression coverage</name>
  <files>src/components/cards/NonPaymentLinkCardShell.tsx, src/components/analytics/FollowerHistoryModal.tsx, src/styles/base.css, src/styles/responsive.css, src/components/cards/social-profile-card-rendering.test.tsx, src/components/cards/non-payment-card-accessibility.test.tsx</files>
  <action>Add a clear analytics icon to cards whose links have follower-history data, and make it open a platform-specific modal that reuses the shared chart component plus the existing accessible modal posture used elsewhere in the app. Add regression and accessibility coverage proving that history-aware cards expose the new entry point, cards without history do not, the modal supports the raw-count versus growth-rate toggle, and the new controls do not break the current card shell semantics.</action>
  <verify>Run `bun test src/components/cards/social-profile-card-rendering.test.tsx src/components/cards/non-payment-card-accessibility.test.tsx`, `bun run typecheck`, and `bun run build`.</verify>
  <done>The analytics experience is reachable from both the profile header and individual cards without regressing current interaction or accessibility behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The profile header has a dedicated analytics button to the left of `Share`.
- [ ] The full analytics surface defaults to a 30-day minimal view with switchable time ranges.
- [ ] Cards with history data expose a platform analytics icon and modal, while cards without history remain unchanged.
- [ ] Single-platform views can toggle between raw counts and growth rate.
- [ ] Cross-platform charts stay mostly separate unless independent multi-axis combined charts prove clear and correct.
</verification>

<success_criteria>
- All tasks completed.
- The app exposes a minimal but complete follower-growth analytics surface aligned with the locked Phase 11 decisions.
- The chart integration and entry points are reusable enough to support future analytics refinements without another navigation rewrite.
</success_criteria>

<output>
After completion, create .planning/phases/11-historical-follower-tracking-growth-charts/11-03-SUMMARY.md
</output>
