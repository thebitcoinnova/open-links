---
phase: 11-historical-follower-tracking-growth-charts
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on: []
files_modified:
  - src/routes/index.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/RichLinkCard.tsx
  - src/components/cards/SimpleLinkCard.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/cards/non-payment-card-accessibility.test.tsx
files-modified:
  - src/routes/index.tsx
  - src/components/cards/NonPaymentLinkCardShell.tsx
  - src/components/cards/RichLinkCard.tsx
  - src/components/cards/SimpleLinkCard.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/cards/non-payment-card-accessibility.test.tsx
autonomous: true
requirements: []
user_setup: []
objective: Fix the Phase 11 card analytics regressions so history-aware cards expose their analytics action on first page load and place that control within the card header row instead of a floating side column
must_haves:
  truths:
    - History-aware cards show their analytics action immediately on the first render of the main links page, without requiring a prior visit to the page-level analytics view.
    - The card analytics control is visually aligned inside the card header/summary row rather than floating as a separate side column.
    - The analytics action stays outside the main link anchor semantics so the card does not contain nested interactive content.
    - Regression and accessibility coverage lock in both the first-load visibility and the new header-row placement.
  artifacts:
    - path: src/routes/index.tsx
      provides: a reliably reactive path from the follower-history index into per-card analytics action availability
      min_lines: 320
    - path: src/components/cards/NonPaymentLinkCardShell.tsx
      provides: card-header-row analytics action placement without nested-interactive markup
      min_lines: 220
    - path: src/styles/base.css
      provides: header-row placement styles for the card analytics action
      min_lines: 1050
    - path: src/components/cards/non-payment-card-accessibility.test.tsx
      provides: regression coverage for first-load action visibility and card-header action semantics
      min_lines: 220
  key_links:
    - from: src/routes/index.tsx
      to: src/components/cards/NonPaymentLinkCardShell.tsx
      via: reactive history availability feeding per-card analytics buttons on initial page load
      pattern: "analyticsButton"
    - from: src/components/cards/NonPaymentLinkCardShell.tsx
      to: src/styles/base.css
      via: card-header action layout and non-overlay positioning
      pattern: "card-analytics-button"
    - from: src/components/cards/non-payment-card-accessibility.test.tsx
      to: src/routes/index.tsx
      via: regression coverage for first-load analytics button behavior
      pattern: "analytics"
---

<objective>
Close the Phase 11 UAT gaps around card-level analytics actions.

Purpose: Make the per-card analytics affordance behave correctly on first load and align visually with the card header instead of reading like a separate side column.
Output: Reactive first-load analytics-button availability, card-header placement, and regression coverage that proves both fixes.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-historical-follower-tracking-growth-charts/11-UAT.md
@.planning/phases/11-historical-follower-tracking-growth-charts/11-VERIFICATION.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Make card analytics availability reactive on the first main-page render</name>
  <files>src/routes/index.tsx, src/components/cards/RichLinkCard.tsx, src/components/cards/SimpleLinkCard.tsx</files>
  <action>Move the history-availability lookup out of the current route render helper pattern and into a reliably reactive per-card seam so the analytics action appears as soon as the follower-history index finishes loading. Preserve the current page-level analytics behavior and do not require a route remount or page-mode toggle to reveal card actions.</action>
  <verify>Run focused rendering coverage for the updated card path, then manually confirm the analytics buttons appear on first load before visiting the full analytics page.</verify>
  <done>History-aware cards expose their analytics action immediately on the first page load.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Reposition the card analytics action into the card header row</name>
  <files>src/components/cards/NonPaymentLinkCardShell.tsx, src/styles/base.css, src/styles/responsive.css</files>
  <action>Move the analytics control into the card summary/header row while keeping it outside the anchor element so the card remains semantically valid. Replace the current floating overlay treatment with a header-row action style that works in both simple and rich cards.</action>
  <verify>Inspect the main links page on desktop and mobile to confirm the button reads as a header action instead of a separate side column.</verify>
  <done>The analytics action is visually integrated into the card header row without becoming nested interactive content.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add regression and accessibility coverage for the fixed card analytics path</name>
  <files>src/components/cards/non-payment-card-accessibility.test.tsx, src/routes/index.tsx</files>
  <action>Add or update tests to prove history-aware cards expose analytics controls on first load and that the card action remains a separate button with intact card anchor semantics after the layout change.</action>
  <verify>Run `bun test src/components/cards/non-payment-card-accessibility.test.tsx`, `bun run typecheck`, `bun run biome:check`, and `bun run build`.</verify>
  <done>The fixed analytics action path has regression coverage and stays aligned with the repo’s accessibility posture.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Card analytics buttons appear on first page load without toggling the page-level analytics view.
- [ ] The card analytics control sits within the card header/summary row rather than a floating side column.
- [ ] Card anchors keep their existing action-oriented accessible naming and do not wrap the analytics button.
- [ ] Focused tests and the repo verification commands pass after the fix.
</verification>

<success_criteria>
- All tasks completed.
- The two Phase 11 UAT issues are fully addressed by one executable gap-closure plan.
- The resulting fix is ready for `$gsd-execute-phase 11 --gaps-only`.
</success_criteria>

<output>
After completion, create .planning/phases/11-historical-follower-tracking-growth-charts/11-04-SUMMARY.md
</output>
