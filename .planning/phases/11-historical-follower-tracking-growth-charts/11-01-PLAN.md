---
phase: 11-historical-follower-tracking-growth-charts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/follower-history/append-history.ts
  - scripts/follower-history/append-history.test.ts
  - scripts/sync-follower-history.ts
  - src/lib/analytics/follower-history.ts
  - src/lib/analytics/follower-history.test.ts
  - scripts/validate-data.ts
  - scripts/validate-data.test.ts
files-modified:
  - scripts/follower-history/append-history.ts
  - scripts/follower-history/append-history.test.ts
  - scripts/sync-follower-history.ts
  - src/lib/analytics/follower-history.ts
  - src/lib/analytics/follower-history.test.ts
  - scripts/validate-data.ts
  - scripts/validate-data.test.ts
autonomous: true
requirements: []
user_setup: []
objective: Define the public follower-history artifact contract, append-only snapshot rules, and shared runtime parsing utilities before workflow automation or chart UI work begins
must_haves:
  truths:
    - Follower history lives in public static assets, not hidden generated JSON, so CSVs remain directly accessible after deploy.
    - History is split into one append-only CSV per platform and never rewrites old rows as part of the normal nightly path.
    - Each history row carries enough audit context to trace a count back to the originating link and source.
    - Runtime analytics code and nightly snapshot code share one row contract instead of drifting into separate parsers.
    - Validation fails visibly on malformed history files or index drift before workflow automation starts publishing them.
  artifacts:
    - path: scripts/follower-history/append-history.ts
      provides: append-only CSV contract, header definitions, and row-writing logic for per-platform history files
      min_lines: 220
    - path: scripts/sync-follower-history.ts
      provides: a deterministic CLI entrypoint that reads current audience counts and writes public history artifacts
      min_lines: 260
    - path: src/lib/analytics/follower-history.ts
      provides: shared parsing, time-window filtering, and raw-count versus growth-rate transforms for the analytics UI
      min_lines: 220
    - path: scripts/validate-data.ts
      provides: history artifact validation for CSV headers, required columns, and index/file parity
      min_lines: 1250
    - path: scripts/validate-data.test.ts
      provides: regression coverage for malformed or drifting history artifacts
      min_lines: 160
  key_links:
    - from: scripts/follower-history/append-history.ts
      to: src/lib/analytics/follower-history.ts
      via: one shared row schema for snapshot writing and runtime parsing
      pattern: "audienceCount"
    - from: scripts/sync-follower-history.ts
      to: scripts/follower-history/append-history.ts
      via: nightly snapshot command delegating to append-only history writers
      pattern: "observedAt"
    - from: scripts/validate-data.ts
      to: scripts/sync-follower-history.ts
      via: contract enforcement for the public history artifacts generated by the sync command
      pattern: "history/followers"
    - from: src/lib/analytics/follower-history.ts
      to: src/routes/index.tsx
      via: reusable analytics data loading for the eventual page and modal surfaces
      pattern: "followers"
---

<objective>
Lock the history contract and shared utilities before automation or charts land.

Purpose: Avoid having the nightly workflow, public CSV artifacts, and analytics UI each invent their own incompatible idea of what a follower-history row means.
Output: Append-only CSV write logic, a deterministic sync command, runtime data utilities, and validation coverage for the new public history surface.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-historical-follower-tracking-growth-charts/11-CONTEXT.md
@.planning/phases/11-historical-follower-tracking-growth-charts/11-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define the per-platform CSV contract and append-only write rules</name>
  <files>scripts/follower-history/append-history.ts, scripts/follower-history/append-history.test.ts, scripts/sync-follower-history.ts</files>
  <action>Create a shared follower-history contract for `public/history/followers/{platform}.csv` with the audit-trail columns locked in from Phase 11 context: `observedAt`, `linkId`, `platform`, `handle`, `canonicalUrl`, `audienceKind`, `audienceCount`, `audienceCountRaw`, and `source` at minimum. Implement append-only writing so every nightly run adds a row even when the count is unchanged, and keep decreases exactly as observed. Add focused tests for header generation, append behavior, and row serialization.</action>
  <verify>Run `bun test scripts/follower-history/append-history.test.ts` and a local dry run of `bun scripts/sync-follower-history.ts --dry-run` once the CLI exists.</verify>
  <done>The repo has one explicit contract for what a follower-history row is and how it gets appended.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Build shared runtime parsing and analytics transforms on top of the same row contract</name>
  <files>src/lib/analytics/follower-history.ts, src/lib/analytics/follower-history.test.ts, scripts/sync-follower-history.ts</files>
  <action>Add shared parsing utilities that can load public follower-history artifacts, normalize them into typed records, filter by time window, and derive growth-rate views from the same raw rows. Keep the transformation layer reusable for both the eventual card-level modal and the all-platform analytics page instead of letting each surface parse CSV independently.</action>
  <verify>Run `bun test src/lib/analytics/follower-history.test.ts` and `bun run typecheck`.</verify>
  <done>Phase 11 has one reusable analytics-data layer instead of separate modal/page loaders.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add validation coverage so malformed public history never ships quietly</name>
  <files>scripts/validate-data.ts, scripts/validate-data.test.ts, scripts/sync-follower-history.ts</files>
  <action>Extend `validate:data` so it checks the new public history surface for required columns, file/index parity, and parseable timestamps/counts. Keep the checks focused on contract correctness rather than trying to police normal count decreases. Make sure the sync command can emit or refresh whatever index/manifest the analytics UI will rely on, and validate that contract too.</action>
  <verify>Run `bun test scripts/validate-data.test.ts`, `bun run validate:data`, and `bun run typecheck`.</verify>
  <done>The public CSV contract is enforceable before workflow automation starts committing it on a schedule.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The repo has one append-only CSV contract per platform under `public/history/followers/`.
- [ ] History rows include audit-trail identifiers and are appended even when counts stay unchanged.
- [ ] Runtime analytics utilities parse the same contract used by the snapshot writer.
- [ ] `validate:data` fails on malformed history artifacts or index drift.
</verification>

<success_criteria>
- All tasks completed.
- The Phase 11 data contract is stable enough for both automation and UI work to build on.
- History semantics from the Phase 11 context are encoded in shared code rather than left implicit.
</success_criteria>

<output>
After completion, create .planning/phases/11-historical-follower-tracking-growth-charts/11-01-SUMMARY.md
</output>
