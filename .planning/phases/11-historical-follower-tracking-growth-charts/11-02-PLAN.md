---
phase: 11-historical-follower-tracking-growth-charts
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - .github/workflows/nightly-follower-history.yml
  - package.json
  - scripts/sync-follower-history.ts
  - public/history/followers/
  - README.md
files-modified:
  - .github/workflows/nightly-follower-history.yml
  - package.json
  - scripts/sync-follower-history.ts
  - public/history/followers/
  - README.md
autonomous: true
requirements: []
user_setup: []
objective: Wire the nightly publication path so public follower-history CSV artifacts are refreshed, committed, and deployed safely without depending on suppressed workflow fan-out from `GITHUB_TOKEN`
must_haves:
  truths:
    - One scheduled workflow owns nightly follower-history capture, artifact refresh, commit behavior, and deployment handoff.
    - The publication path does not assume that a workflow-generated push to `main` will trigger the existing CI-to-Pages pipeline.
    - The nightly workflow uses the same deterministic sync command and contract validation introduced in Plan 11-01.
    - Public follower-history artifacts are committed to `main` as human-readable CSV files plus any needed manifest/index file.
    - Maintainers have one documented local command to reproduce the nightly history refresh before the workflow runs it.
  artifacts:
    - path: .github/workflows/nightly-follower-history.yml
      provides: scheduled GitHub Actions workflow for capture, commit, build, and deploy of follower-history updates
      min_lines: 220
    - path: package.json
      provides: maintainer-facing `followers:history:sync` command and any deployment helper scripts needed by the workflow
      min_lines: 80
    - path: scripts/sync-follower-history.ts
      provides: workflow-ready flags such as dry-run, changed-only summaries, and deterministic artifact output
      min_lines: 320
    - path: public/history/followers/
      provides: checked-in initial public follower-history CSVs plus the generated index/manifest the app will fetch
      min_lines: 1
    - path: README.md
      provides: maintainer documentation for the nightly history workflow and local reproduction path
      min_lines: 180
  key_links:
    - from: package.json
      to: .github/workflows/nightly-follower-history.yml
      via: the workflow calling the same local sync command maintainers use
      pattern: "followers:history:sync"
    - from: scripts/sync-follower-history.ts
      to: public/history/followers/
      via: generated CSV and index artifacts committed by the nightly workflow
      pattern: "history/followers"
    - from: .github/workflows/nightly-follower-history.yml
      to: README.md
      via: documented schedule, deploy strategy, and remediation path
      pattern: "follower history"
    - from: .github/workflows/nightly-follower-history.yml
      to: .github/workflows/deploy-pages.yml
      via: explicit choice to deploy in the nightly run instead of relying on suppressed push-trigger fan-out
      pattern: "pages"
---

<objective>
Automate the public artifact pipeline after the history contract is locked.

Purpose: Make nightly follower snapshots actually reach `main` and GitHub Pages without relying on a workflow-trigger pattern GitHub explicitly suppresses for `GITHUB_TOKEN`.
Output: A scheduled workflow, a local parity command, initial committed history artifacts, and maintainer docs for the publication flow.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-historical-follower-tracking-growth-charts/11-CONTEXT.md
@.planning/phases/11-historical-follower-tracking-growth-charts/11-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Expose a workflow-ready local sync command and seed the initial public history artifacts</name>
  <files>package.json, scripts/sync-follower-history.ts, public/history/followers/, README.md</files>
  <action>Add a stable local command such as `bun run followers:history:sync` that resolves the current audience counts from the repo's existing enriched metadata path, appends one row per eligible platform file, and refreshes the generated index/manifest. Run it to create the initial checked-in follower-history artifacts under `public/history/followers/`, keeping the files human-readable and aligned with the append-only contract from Plan 11-01.</action>
  <verify>Run `bun run followers:history:sync -- --dry-run`, `bun run followers:history:sync`, `bun run validate:data`, and inspect the generated CSV/index diffs for stable headers and append-only row semantics.</verify>
  <done>The nightly workflow will have one stable command to call, and the repo already contains the first public history artifacts.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add a scheduled workflow that captures, commits, builds, and deploys in one explicit path</name>
  <files>.github/workflows/nightly-follower-history.yml, scripts/sync-follower-history.ts, package.json</files>
  <action>Create a nightly GitHub Actions workflow that checks out the repo, installs dependencies, refreshes any upstream metadata needed for current counts, runs the follower-history sync command, commits changed artifacts directly to `main`, and handles the GitHub Pages deployment path explicitly in the same workflow run. Do not rely on the existing `push -> CI -> workflow_run -> Deploy Pages` chain for these bot-authored commits unless the implementation intentionally switches to a token strategy that can trigger downstream workflows.</action>
  <verify>Use workflow-level dry-run or manual-dispatch-friendly branches where practical, then locally verify parity with `bun run followers:history:sync`, `bun run validate:data`, and `bun run build`.</verify>
  <done>The repo has a safe nightly publication path that does not silently stall on GitHub Actions event recursion rules.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Document the nightly history publication path and failure recovery</name>
  <files>README.md, .github/workflows/nightly-follower-history.yml</files>
  <action>Document where the public history CSVs live, which local command reproduces the nightly update, how the workflow handles deploys, and what a maintainer should inspect when a nightly run fails or stops updating public history. Keep the documentation concise and operational rather than turning this into a full analytics guide.</action>
  <verify>Review the README copy against the final workflow steps, then run `bun run build` so docs changes ship with the rest of the site artifact expectations intact.</verify>
  <done>Maintainers can understand and repair the new nightly follower-history path without reverse-engineering the workflow YAML.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The repo exposes one stable local sync command for follower-history artifacts.
- [ ] The scheduled workflow updates and commits public history artifacts without relying on suppressed `GITHUB_TOKEN` workflow fan-out.
- [ ] Initial follower-history CSVs and the generated index/manifest are checked in under `public/history/followers/`.
- [ ] Maintainer docs explain the nightly path and the local reproduction command.
</verification>

<success_criteria>
- All tasks completed.
- Public follower-history artifacts can be refreshed and published automatically every night.
- The publication path is explicit enough that future maintenance does not depend on undocumented GitHub Actions behavior.
</success_criteria>

<output>
After completion, create .planning/phases/11-historical-follower-tracking-growth-charts/11-02-SUMMARY.md
</output>
