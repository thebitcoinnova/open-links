---
phase: 08-social-profile-card-ui-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/cards/RichLinkCard.tsx
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/social-profile-metadata.ts
  - src/lib/ui/social-profile-metadata.test.ts
  - src/styles/base.css
  - src/styles/responsive.css
files-modified:
  - src/components/cards/RichLinkCard.tsx
  - src/lib/ui/rich-card-policy.ts
  - src/lib/ui/social-profile-metadata.ts
  - src/lib/ui/social-profile-metadata.test.ts
  - src/styles/base.css
  - src/styles/responsive.css
autonomous: true
requirements:
  - UI-07
  - UI-09
user_setup: []
objective: Redesign rich cards around a profile-style header while preserving preview media, content description, and source context
must_haves:
  truths:
    - Rich cards with profile metadata render a circular avatar header separate from the existing preview-media slot.
    - Rich-card header copy exposes title or identity text, handle, and inline platform-native metrics in a mobile-first layout.
    - When both avatar and preview image exist, the avatar stays in the header and the preview image remains in the rich-card body/media area.
    - Rich cards with partial or missing profile metadata still render a stable content-first layout without broken spacing or crashes.
  artifacts:
    - path: src/components/cards/RichLinkCard.tsx
      provides: Rich-card markup for profile header, content body, preview media, and source context
      min_lines: 90
    - path: src/lib/ui/rich-card-policy.ts
      provides: Rich-card view model enriched with display-ready profile header data
      min_lines: 110
    - path: src/lib/ui/social-profile-metadata.ts
      provides: Shared display-ready social profile data including compact metric copy
      min_lines: 70
    - path: src/styles/base.css
      provides: Base rich-card profile header, avatar, and inline metric styling
      min_lines: 430
  key_links:
    - from: src/lib/ui/rich-card-policy.ts
      to: src/lib/ui/social-profile-metadata.ts
      via: display-ready social profile metadata consumed by the rich-card view model
      pattern: "socialProfile"
    - from: src/components/cards/RichLinkCard.tsx
      to: src/lib/ui/rich-card-policy.ts
      via: profile-style header fields and preview-media separation
      pattern: "viewModel"
    - from: src/styles/base.css
      to: src/components/cards/RichLinkCard.tsx
      via: avatar-header, inline-metric, and rich-body class styling
      pattern: "rich-card"
---

<objective>
Deliver the rich-card half of the Phase 8 redesign.

Purpose: Make rich cards feel like real social profile cards while preserving the existing preview-media and source-context contract established in earlier phases.
Output: Rich-card view model updates, profile-style rich markup, and supporting responsive styling.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-social-profile-card-ui-refresh/08-CONTEXT.md
@.planning/phases/08-social-profile-card-ui-refresh/08-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Extend social-profile presentation data for rich-card header use</name>
  <files>src/lib/ui/social-profile-metadata.ts, src/lib/ui/social-profile-metadata.test.ts, src/lib/ui/rich-card-policy.ts</files>
  <action>Expand the shared social-profile helper so it can provide display-ready, compact-formatted metric text and any header-facing identity fields the rich card needs. Keep platform-native order and labels intact and avoid duplicating formatting rules in JSX.</action>
  <verify>Add focused automated coverage for formatted metrics and header-facing metadata, then run `bun test src/lib/ui/social-profile-metadata.test.ts` and `bun run typecheck`.</verify>
  <done>The rich-card view model can render platform-native profile counts and identity copy from one shared presentation surface.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Rebuild rich-card markup around profile identity first</name>
  <files>src/components/cards/RichLinkCard.tsx, src/lib/ui/rich-card-policy.ts</files>
  <action>Replace the current preview-first rich-card structure with a profile-style header row that uses a circular avatar on the left and a text stack on the right. Preserve description first and source branding after it, and keep preview media available below the header when a preview image exists.</action>
  <verify>Run `bun run typecheck` and `bun run build`, then manually inspect supported social rich cards plus a non-profile rich card to confirm the new structure renders without regressions.</verify>
  <done>Rich cards read as social profiles instead of generic previews while keeping their content and source context intact.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add rich-card CSS and responsive rules for avatar/header/media states</name>
  <files>src/styles/base.css, src/styles/responsive.css, src/components/cards/RichLinkCard.tsx</files>
  <action>Create the avatar-header, inline-metric, and dual-image styling needed for the new rich-card structure. Ensure mobile-first wrapping works for long handles and metrics, and that avatar-only, preview-only, full, and fallback states all remain visually intentional.</action>
  <verify>Check desktop and mobile viewport rendering for avatar+preview, avatar-only, preview-only, and no-profile rich-card states after `bun run build`.</verify>
  <done>Rich-card styling is stable across supported and fallback metadata combinations.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Rich cards render a circular-avatar profile header when profile metadata exists.
- [ ] Preview media remains available below the header when a rich preview image exists.
- [ ] Platform-native metrics display in compact inline text without duplicated formatting logic in the component.
- [ ] Partial and no-profile rich cards still render stable content/source structure.
</verification>

<success_criteria>
- All tasks completed.
- UI-07 is established for rich cards and UI-09 fallback behavior remains intact for the rich-card variant.
- The shared social-profile presentation layer is ready for simple-card reuse in 08-02.
</success_criteria>

<output>
After completion, create .planning/phases/08-social-profile-card-ui-refresh/08-01-SUMMARY.md
</output>
