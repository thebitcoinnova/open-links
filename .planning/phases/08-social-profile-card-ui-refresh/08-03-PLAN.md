---
phase: 08-social-profile-card-ui-refresh
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/components/cards/RichLinkCard.tsx
  - src/components/cards/SimpleLinkCard.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/routes/index.tsx
files-modified:
  - src/components/cards/RichLinkCard.tsx
  - src/components/cards/SimpleLinkCard.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/routes/index.tsx
autonomous: true
requirements:
  - UI-07
  - UI-08
  - UI-09
user_setup: []
objective: Finish responsive, fallback, and accessibility polish so the refreshed card system is stable across breakpoints and metadata states
must_haves:
  truths:
    - Rich and simple cards share a unified visual language while still scaling down gracefully when profile metadata is partial or absent.
    - Keyboard focus, reading order, and accessible descriptions remain meaningful after the card markup changes.
    - Mobile and desktop breakpoints handle avatar, metrics, and preview-media combinations without broken spacing or overlap.
    - The final card system remains stable for profile-rich, preview-only, avatar-only, and no-profile links.
  artifacts:
    - path: src/styles/base.css
      provides: Final cross-card visual polish and fallback-state rules
      min_lines: 430
    - path: src/styles/responsive.css
      provides: Breakpoint handling for rich/simple profile states
      min_lines: 120
    - path: src/components/cards/RichLinkCard.tsx
      provides: Final accessible rich-card semantics after profile redesign
      min_lines: 100
    - path: src/components/cards/SimpleLinkCard.tsx
      provides: Final accessible simple-card semantics after profile refresh
      min_lines: 80
  key_links:
    - from: src/styles/responsive.css
      to: src/components/cards/RichLinkCard.tsx
      via: mobile fallback handling for avatar plus preview-media combinations
      pattern: "rich-link-card"
    - from: src/styles/responsive.css
      to: src/components/cards/SimpleLinkCard.tsx
      via: compact wrapping and fallback handling for simple-card profile metadata
      pattern: "simple-link-card"
    - from: src/routes/index.tsx
      to: src/components/cards/RichLinkCard.tsx
      via: route-level rendering stability after final card-semantic changes
      pattern: "renderCard"
---

<objective>
Deliver the final polish pass for the Phase 8 card refresh.

Purpose: Stabilize responsive behavior, fallback states, and accessibility semantics once both card variants have adopted the new social-profile presentation.
Output: Cross-card styling refinement, fallback-state cleanup, and final verification for mobile/desktop stability.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-social-profile-card-ui-refresh/08-CONTEXT.md
@.planning/phases/08-social-profile-card-ui-refresh/08-RESEARCH.md
@.planning/phases/08-social-profile-card-ui-refresh/08-01-PLAN.md
@.planning/phases/08-social-profile-card-ui-refresh/08-02-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Refine mixed and fallback card states across both variants</name>
  <files>src/components/cards/RichLinkCard.tsx, src/components/cards/SimpleLinkCard.tsx, src/styles/base.css, src/styles/responsive.css</files>
  <action>Audit and refine the combined card system so profile-rich, partial-profile, preview-only, avatar-only, and no-profile links all render with intentional spacing and hierarchy. Remove any visual leftovers from the old preview-card layout that clash with the new profile treatment.</action>
  <verify>Run `bun run build` and manually inspect representative rich/simple cards across all major metadata combinations.</verify>
  <done>Fallback and mixed states look designed rather than incidental.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Tighten accessibility semantics and interaction polish after markup changes</name>
  <files>src/components/cards/RichLinkCard.tsx, src/components/cards/SimpleLinkCard.tsx, src/routes/index.tsx</files>
  <action>Review the final card markup for meaningful labels, descriptions, reading order, and focus behavior after the profile-header restructuring. Keep the interaction model unified and avoid duplicated or misleading accessible copy when profile metadata is partially missing.</action>
  <verify>Run `bun run typecheck`, `bun run build`, and a keyboard/focus smoke pass against the updated card list.</verify>
  <done>The refreshed cards remain accessible and coherent after the structural redesign.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Run final responsive and quality verification for the Phase 8 UI refresh</name>
  <files>src/styles/base.css, src/styles/responsive.css, src/components/cards/RichLinkCard.tsx, src/components/cards/SimpleLinkCard.tsx</files>
  <action>Finalize breakpoint tuning and run the phase's broad verification loop so the updated cards hold together on mobile and desktop. Fix any spacing, wrapping, or fallback regressions surfaced by the quality/build checks before closing the phase.</action>
  <verify>Run `bun run typecheck`, `bun run build`, and `bun run quality:check`, then confirm representative desktop/mobile renders for the final rich/simple card set.</verify>
  <done>The profile-card refresh is visually and behaviorally stable enough for Phase 9 documentation and regression hardening.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Rich and simple cards feel visually unified without over-forcing profile chrome onto non-profile links.
- [ ] Profile-rich, partial, and fallback states remain stable across mobile and desktop breakpoints.
- [ ] Focus, accessible naming, and reading order remain meaningful after markup changes.
- [ ] Final build and quality checks pass with the refreshed card UI.
</verification>

<success_criteria>
- All tasks completed.
- UI-07, UI-08, and UI-09 are satisfied together at the system level.
- Phase 9 can focus on docs and regression hardening instead of core UI polish.
</success_criteria>

<output>
After completion, create .planning/phases/08-social-profile-card-ui-refresh/08-03-SUMMARY.md
</output>
