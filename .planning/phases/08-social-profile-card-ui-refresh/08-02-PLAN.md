---
phase: 08-social-profile-card-ui-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/cards/SimpleLinkCard.tsx
  - src/lib/ui/social-profile-metadata.ts
  - src/styles/base.css
  - src/styles/responsive.css
files-modified:
  - src/components/cards/SimpleLinkCard.tsx
  - src/lib/ui/social-profile-metadata.ts
  - src/styles/base.css
  - src/styles/responsive.css
autonomous: true
requirements:
  - UI-08
  - UI-09
user_setup: []
objective: Refresh simple cards around compact profile identity while preserving scanability and source recognition
must_haves:
  truths:
    - Simple cards keep the current left-hand leading slot, but use a circular avatar there when profile metadata exists.
    - Simple cards can render a two-line header block with handle and inline audience stats without losing fast scanability.
    - Mobile behavior wraps metadata rather than dropping handle, stats, or source context.
    - Non-profile simple cards inherit subtle profile-card visual cues without being forced into the full profile-card structure.
  artifacts:
    - path: src/components/cards/SimpleLinkCard.tsx
      provides: Compact simple-card rendering with avatar-capable leading slot and profile-aware metadata lines
      min_lines: 70
    - path: src/lib/ui/social-profile-metadata.ts
      provides: Shared metric and identity presentation rules reused by simple cards
      min_lines: 80
    - path: src/styles/base.css
      provides: Simple-card profile-aware layout and subtle fallback styling
      min_lines: 430
    - path: src/styles/responsive.css
      provides: Wrapping and compact breakpoint behavior for profile-aware simple cards
      min_lines: 120
  key_links:
    - from: src/components/cards/SimpleLinkCard.tsx
      to: src/lib/ui/social-profile-metadata.ts
      via: shared social profile display data reused from the rich-card plan
      pattern: "resolveSocialProfileMetadata"
    - from: src/styles/base.css
      to: src/components/cards/SimpleLinkCard.tsx
      via: compact profile header, avatar slot, and metadata wrapping styles
      pattern: "simple-link-card"
    - from: src/styles/responsive.css
      to: src/components/cards/SimpleLinkCard.tsx
      via: mobile wrapping rules that preserve handle, stats, and source context
      pattern: "simple-link-card"
---

<objective>
Deliver the simple-card half of the Phase 8 redesign.

Purpose: Make simple cards feel profile-aware when metadata exists without sacrificing compactness, fast scanning, or fallback stability.
Output: Updated simple-card markup and styling that reuse the Phase 8 social-profile presentation rules.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-social-profile-card-ui-refresh/08-CONTEXT.md
@.planning/phases/08-social-profile-card-ui-refresh/08-RESEARCH.md
@.planning/phases/08-social-profile-card-ui-refresh/08-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Rebuild simple-card content hierarchy for avatar-first profile metadata</name>
  <files>src/components/cards/SimpleLinkCard.tsx, src/lib/ui/social-profile-metadata.ts</files>
  <action>Teach simple cards to consume the shared social-profile presentation data so the left slot can show a circular avatar when available. Rehome source-brand context into the compact text area when the brand icon is no longer the leading visual, and keep non-profile cards coherent with the same component shell.</action>
  <verify>Run `bun run typecheck` and manually inspect simple cards with profile metadata versus generic links to confirm scanability and source recognition remain intact.</verify>
  <done>Simple cards can show profile identity without losing the compact "quick links" reading flow.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add two-line simple-card header and inline metric wrapping</name>
  <files>src/components/cards/SimpleLinkCard.tsx, src/styles/base.css, src/styles/responsive.css</files>
  <action>Implement a two-line header block for handle and platform-native metrics when present, and use wrapping instead of truncation or hiding as space gets tight. Preserve compactness by making layout changes deliberate rather than just letting content spill unpredictably.</action>
  <verify>Run `bun run build`, then inspect desktop and mobile simple-card layouts with long handles and multiple metrics to confirm the wrapping behavior is intentional.</verify>
  <done>Simple cards stay readable on mobile and desktop without dropping important social-profile metadata.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Carry subtle profile-card visual language into fallback simple cards</name>
  <files>src/styles/base.css, src/styles/responsive.css, src/components/cards/SimpleLinkCard.tsx</files>
  <action>Apply restrained profile-inspired styling cues to simple cards that do not have social profile metadata so the overall list feels unified without forcing every simple card into the full profile-card treatment.</action>
  <verify>Compare simple cards with and without profile metadata after `bun run build` and confirm the fallback treatment feels intentional but quieter.</verify>
  <done>Simple-card fallbacks align visually with the new design without overwhelming non-profile links.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Simple cards use the existing left slot for avatars when available.
- [ ] Handle, metrics, and source context can coexist in a compact two-line simple-card layout.
- [ ] Mobile wrapping preserves data instead of dropping it.
- [ ] Non-profile simple cards retain a lighter version of the new profile-card language.
</verification>

<success_criteria>
- All tasks completed.
- UI-08 is satisfied for simple cards and UI-09 fallback behavior remains intact for simple-card states.
- Simple-card design now matches the Phase 8 direction without losing quick-scan behavior.
</success_criteria>

<output>
After completion, create .planning/phases/08-social-profile-card-ui-refresh/08-02-SUMMARY.md
</output>
