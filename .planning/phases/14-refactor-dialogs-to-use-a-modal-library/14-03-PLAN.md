---
phase: 14-refactor-dialogs-to-use-a-modal-library
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - src/components/analytics/FollowerHistoryModal.tsx
  - src/routes/index.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/analytics/FollowerHistoryModal.test.tsx
files-modified:
  - src/components/analytics/FollowerHistoryModal.tsx
  - src/routes/index.tsx
  - src/styles/base.css
  - src/styles/responsive.css
  - src/components/analytics/FollowerHistoryModal.test.tsx
autonomous: true
requirements: []
user_setup: []
objective: Migrate the follower-history analytics modal onto the shared dialog shell, preserve the lazy chart flow, and add the final regression/browser verification for the full dialog-library refactor
must_haves:
  truths:
    - The follower-history modal no longer owns its own focus-trap, escape handling, body-scroll locking, or backdrop dismissal logic.
    - Analytics modal open/close behavior remains controlled from `src/routes/index.tsx`.
    - The existing chart lazy-load and analytics controls still behave correctly inside the library-backed dialog.
    - Focus restoration, overlay close behavior, and responsive modal sizing are covered by regression tests and built-preview verification.
  artifacts:
    - path: src/components/analytics/FollowerHistoryModal.tsx
      provides: analytics modal content hosted by the shared dialog shell
      min_lines: 150
    - path: src/routes/index.tsx
      provides: preserved controlled analytics modal state with the migrated dialog component
      min_lines: 560
    - path: src/components/analytics/FollowerHistoryModal.test.tsx
      provides: focused regression coverage for analytics modal open/close structure and controls
      min_lines: 160
    - path: src/styles/base.css
      provides: analytics modal styling adapted to the shared wrapper and portal content
      min_lines: 1000
    - path: src/styles/responsive.css
      provides: mobile-safe analytics modal sizing after migration
      min_lines: 110
  key_links:
    - from: src/components/analytics/FollowerHistoryModal.tsx
      to: src/components/dialog/AppDialog.tsx
      via: analytics modal content hosted by the shared dialog shell
      pattern: "AppDialog"
    - from: src/routes/index.tsx
      to: src/components/analytics/FollowerHistoryModal.tsx
      via: controlled modal state and lazy chart/modal integration
      pattern: "selectedHistoryLinkId"
    - from: src/components/analytics/FollowerHistoryModal.test.tsx
      to: src/components/analytics/FollowerHistoryModal.tsx
      via: regression coverage for controls and close behavior
      pattern: "Close"
---

<objective>
Finish the phase by migrating the more complex analytics modal and verifying the full dialog-library adoption.

Purpose: remove the final custom dialog implementation while preserving analytics controls, lazy chart loading, and responsive modal behavior.
Output: analytics modal migrated, regression coverage added, and the dialog-library refactor verified end-to-end.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-refactor-dialogs-to-use-a-modal-library/14-RESEARCH.md
@.planning/phases/14-refactor-dialogs-to-use-a-modal-library/14-01-PLAN.md
@.planning/phases/14-refactor-dialogs-to-use-a-modal-library/14-02-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Port `FollowerHistoryModal` onto the shared dialog wrapper without regressing its controls</name>
  <files>src/components/analytics/FollowerHistoryModal.tsx, src/styles/base.css, src/styles/responsive.css</files>
  <action>Replace the custom analytics `<dialog>` plumbing with the shared `AppDialog` wrapper while preserving the range chips, mode chips, close affordance, and chart container structure. Keep the existing visual layout and responsive sizing close to current behavior.</action>
  <verify>Run focused analytics modal tests plus `bun run typecheck`.</verify>
  <done>The analytics modal no longer reimplements the low-level dialog mechanics.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Preserve route-controlled analytics modal behavior and lazy chart flow</name>
  <files>src/routes/index.tsx, src/components/analytics/FollowerHistoryModal.tsx</files>
  <action>Keep `selectedHistoryLinkId`, `modalRange`, and `modalMode` as the route-controlled seam, but adapt the migrated modal so open/close behavior still works with the existing lazy chart and analytics page flow. Avoid broad route-state changes or URL-based modal refactors in this phase.</action>
  <verify>Run focused analytics tests and `bun run build`.</verify>
  <done>The analytics modal still opens from cards and analytics-page buttons using the same controlled state model.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add final regression coverage and built-preview verification for the full dialog migration</name>
  <files>src/components/analytics/FollowerHistoryModal.test.tsx, src/routes/index.tsx</files>
  <action>Add focused tests for analytics modal structure, controls, and close behavior, then verify the full dialog-library migration on built previews. Confirm desktop and mobile behavior for both the analytics modal and the payment fullscreen modal now that both use the same library-backed shell.</action>
  <verify>Run `bun test src/components/analytics/FollowerHistoryModal.test.tsx src/components/cards/PaymentLinkCard.test.tsx`, `bun run biome:check`, `bun run typecheck`, `bun run build`, and a built-preview browser check covering both modal surfaces on desktop and mobile.</verify>
  <done>The phase closes with regression evidence and browser verification for both migrated modal surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The analytics modal no longer contains duplicated custom dialog mechanics.
- [ ] Route-controlled analytics modal state remains intact.
- [ ] Both modal surfaces now use the same shared library-backed wrapper.
- [ ] Browser verification covers desktop and mobile behavior for both modal surfaces.
</verification>

<success_criteria>
- All tasks completed.
- Both existing modal surfaces are migrated to the dialog library without reopening the wider UI system.
- Phase 14 fully removes the duplicated custom dialog implementation and locks the migration in with regression/browser evidence.
</success_criteria>

<output>
After completion, create .planning/phases/14-refactor-dialogs-to-use-a-modal-library/14-03-SUMMARY.md
</output>
