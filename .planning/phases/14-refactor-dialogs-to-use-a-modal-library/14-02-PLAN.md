---
phase: 14-refactor-dialogs-to-use-a-modal-library
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/components/payments/PaymentQrFullscreen.tsx
  - src/components/cards/PaymentLinkCard.tsx
  - src/components/cards/PaymentLinkCard.test.tsx
  - src/styles/base.css
  - src/styles/responsive.css
files-modified:
  - src/components/payments/PaymentQrFullscreen.tsx
  - src/components/cards/PaymentLinkCard.tsx
  - src/components/cards/PaymentLinkCard.test.tsx
  - src/styles/base.css
  - src/styles/responsive.css
autonomous: true
requirements: []
user_setup: []
objective: Migrate the simpler payment QR fullscreen modal onto the shared dialog shell and verify fullscreen-close behavior before touching the analytics modal
must_haves:
  truths:
    - Payment QR fullscreen no longer owns its own focus-trap, escape handling, body-scroll locking, or backdrop dismissal code.
    - The payment fullscreen trigger state stays controlled from `PaymentLinkCard`.
    - Fullscreen QR layout and responsive sizing remain intact after the library-backed migration.
    - Regression coverage proves the payment dialog still opens/closes correctly and keeps stable action semantics.
  artifacts:
    - path: src/components/payments/PaymentQrFullscreen.tsx
      provides: payment fullscreen rendered through the shared dialog shell
      min_lines: 110
    - path: src/components/cards/PaymentLinkCard.tsx
      provides: payment fullscreen open/close integration with the new wrapper contract
      min_lines: 360
    - path: src/components/cards/PaymentLinkCard.test.tsx
      provides: focused regression coverage for the fullscreen dialog trigger/close seam
      min_lines: 150
    - path: src/styles/base.css
      provides: payment fullscreen content sizing/alignment adapted to the library-backed dialog shell
      min_lines: 1000
    - path: src/styles/responsive.css
      provides: mobile-safe fullscreen sizing with the shared wrapper in place
      min_lines: 110
  key_links:
    - from: src/components/payments/PaymentQrFullscreen.tsx
      to: src/components/dialog/AppDialog.tsx
      via: payment fullscreen content hosted by the shared dialog shell
      pattern: "AppDialog"
    - from: src/components/cards/PaymentLinkCard.tsx
      to: src/components/payments/PaymentQrFullscreen.tsx
      via: controlled open/close state for payment fullscreen
      pattern: "fullscreen"
    - from: src/components/cards/PaymentLinkCard.test.tsx
      to: src/components/payments/PaymentQrFullscreen.tsx
      via: regression coverage for fullscreen trigger and close behavior
      pattern: "Open Full Screen"
---

<objective>
Use the simpler modal as the first real migration target after the wrapper exists.

Purpose: validate the new dialog shell on the lower-risk payment surface before moving the more complex analytics dialog.
Output: payment fullscreen migrated to the shared dialog shell with focused coverage and stable responsive behavior.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-refactor-dialogs-to-use-a-modal-library/14-RESEARCH.md
@.planning/phases/14-refactor-dialogs-to-use-a-modal-library/14-01-PLAN.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Port `PaymentQrFullscreen` onto the shared dialog wrapper</name>
  <files>src/components/payments/PaymentQrFullscreen.tsx, src/styles/base.css, src/styles/responsive.css</files>
  <action>Replace the custom `<dialog>` and low-level event plumbing in `PaymentQrFullscreen.tsx` with the shared `AppDialog` wrapper. Keep the current QR content, close affordance, and sizing rules intact while removing the duplicated focus/escape/body-scroll logic.</action>
  <verify>Run focused payment fullscreen tests and `bun run typecheck`.</verify>
  <done>The payment fullscreen modal now rides on the shared library-backed dialog shell instead of its own custom implementation.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Preserve controlled fullscreen behavior from `PaymentLinkCard`</name>
  <files>src/components/cards/PaymentLinkCard.tsx, src/components/cards/PaymentLinkCard.test.tsx</files>
  <action>Keep the current `fullscreenRailId` controlled state in `PaymentLinkCard`, but adapt the integration so the library-backed dialog open/close callbacks preserve the same entry/exit behavior and action semantics.</action>
  <verify>Run `bun test src/components/cards/PaymentLinkCard.test.tsx` and `bun run typecheck`.</verify>
  <done>Payment fullscreen behavior stays controlled and regression-safe after the modal migration.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Verify fullscreen layout and close behavior on built previews</name>
  <files>src/components/payments/PaymentQrFullscreen.tsx, src/components/cards/PaymentLinkCard.test.tsx</files>
  <action>Add any missing focused assertions for the fullscreen trigger/close seam, then inspect the built preview on desktop and mobile to confirm the dialog still sizes correctly, closes predictably, and does not collide with other chrome.</action>
  <verify>Run `bun test src/components/cards/PaymentLinkCard.test.tsx`, `bun run biome:check`, `bun run build`, and a built-preview browser check covering desktop and mobile payment fullscreen.</verify>
  <done>The simpler modal migration is proven before the analytics modal adopts the same wrapper.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Payment fullscreen no longer contains duplicated custom dialog mechanics.
- [ ] `PaymentLinkCard` still controls fullscreen open/close state.
- [ ] Responsive QR fullscreen sizing remains intact.
- [ ] Browser verification confirms the migrated fullscreen dialog still behaves correctly.
</verification>

<success_criteria>
- All tasks completed.
- The payment fullscreen modal is migrated to the shared dialog shell without user-visible regressions.
- The analytics modal can now follow the same migration path with lower risk.
</success_criteria>

<output>
After completion, create .planning/phases/14-refactor-dialogs-to-use-a-modal-library/14-02-SUMMARY.md
</output>
