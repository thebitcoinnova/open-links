---
phase: 14-refactor-dialogs-to-use-a-modal-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/components/dialog/AppDialog.tsx
  - src/components/dialog/AppDialog.test.tsx
  - src/styles/base.css
  - src/styles/responsive.css
files-modified:
  - package.json
  - bun.lock
  - src/components/dialog/AppDialog.tsx
  - src/components/dialog/AppDialog.test.tsx
  - src/styles/base.css
  - src/styles/responsive.css
autonomous: true
requirements: []
user_setup: []
objective: Add the chosen dialog library and establish one shared public-site dialog shell that replaces the duplicated low-level modal plumbing
must_haves:
  truths:
    - The public site adopts one library-backed dialog primitive set instead of maintaining duplicated custom `<dialog>` focus/scroll/escape logic.
    - The new shared dialog shell preserves controlled open/close semantics and can support both analytics and payment fullscreen content.
    - Existing modal styling remains visually close to current behavior rather than triggering a broader redesign.
    - The dialog shell is testable in isolation before either consumer migration lands.
  artifacts:
    - path: package.json
      provides: the new dialog-library dependency added at the root public-site app
      min_lines: 100
    - path: bun.lock
      provides: locked dependency state for the chosen dialog library
      min_lines: 1
    - path: src/components/dialog/AppDialog.tsx
      provides: a shared wrapper around the chosen library's portal/overlay/content/close anatomy
      min_lines: 140
    - path: src/components/dialog/AppDialog.test.tsx
      provides: focused regression coverage for open/close, overlay anatomy, and close behavior
      min_lines: 120
    - path: src/styles/base.css
      provides: shared dialog-shell styling hooks aligned with the existing analytics/payment visual language
      min_lines: 1000
    - path: src/styles/responsive.css
      provides: responsive sizing hooks for the new shared dialog shell
      min_lines: 110
  key_links:
    - from: src/components/dialog/AppDialog.tsx
      to: src/components/analytics/FollowerHistoryModal.tsx
      via: shared library-backed wrapper ready for analytics modal content
      pattern: "Dialog"
    - from: src/components/dialog/AppDialog.tsx
      to: src/components/payments/PaymentQrFullscreen.tsx
      via: shared library-backed wrapper ready for payment fullscreen content
      pattern: "Dialog"
    - from: src/components/dialog/AppDialog.tsx
      to: src/styles/base.css
      via: shared overlay/content class hooks
      pattern: "dialog"
---

<objective>
Build the shared foundation before migrating either modal surface.

Purpose: remove the duplicated low-level dialog mechanics once, then reuse that shell for both existing modals.
Output: one shared `AppDialog` wrapper, isolated tests, and stable base/responsive styling hooks.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-refactor-dialogs-to-use-a-modal-library/14-RESEARCH.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add the dialog library and create one shared public-site dialog wrapper</name>
  <files>package.json, bun.lock, src/components/dialog/AppDialog.tsx</files>
  <action>Add the chosen dialog library (`@kobalte/core` unless execution finds a concrete blocker) and create a shared `AppDialog` wrapper that owns portal, overlay, content, close behavior, and the controlled open seam the current modals already use. Keep the wrapper headless and public-site-specific instead of turning it into a generalized design-system abstraction.</action>
  <verify>Run `bun install` if needed, then `bun run typecheck` and a focused wrapper test run.</verify>
  <done>The repo has one library-backed dialog shell ready to host both existing modal surfaces.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Port the current modal shell styling onto the shared dialog layer</name>
  <files>src/components/dialog/AppDialog.tsx, src/styles/base.css, src/styles/responsive.css</files>
  <action>Adapt the current analytics/payment modal CSS into shared overlay/content hooks for the new dialog shell while preserving the current layout language. Keep class names or aliases stable where practical so the surface migrations in later waves do not need to restyle everything from scratch.</action>
  <verify>Run `bun run build` and visually inspect the shared dialog shell in isolation if a small harness/test fixture is used.</verify>
  <done>The new wrapper can render with the repo's existing visual language across desktop and mobile constraints.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add focused wrapper-level regression coverage before consumer migration</name>
  <files>src/components/dialog/AppDialog.test.tsx</files>
  <action>Add focused tests proving the wrapper supports controlled open state, overlay/content rendering, and close affordances without reintroducing custom focus-trap code in each consumer. Keep the tests at the wrapper contract level so the later consumer migrations can rely on them.</action>
  <verify>Run `bun test src/components/dialog/AppDialog.test.tsx` and `bun run biome:check`.</verify>
  <done>The library-backed wrapper is stable enough that migrating the payment and analytics dialogs can focus on consumer content instead of the dialog mechanics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The chosen dialog library is added and locked.
- [ ] A shared wrapper now owns portal/overlay/content behavior instead of the per-surface custom code.
- [ ] Shared dialog styling hooks exist for desktop and mobile.
- [ ] Wrapper-level regression coverage exists before consumer migration begins.
</verification>

<success_criteria>
- All tasks completed.
- The repo has one library-backed dialog shell ready for both current modal surfaces.
- Later phase work can migrate consumers without duplicating dialog behavior again.
</success_criteria>

<output>
After completion, create .planning/phases/14-refactor-dialogs-to-use-a-modal-library/14-01-SUMMARY.md
</output>
